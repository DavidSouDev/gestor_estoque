---
phase: 07-gest-o-de-assinatura
plan: 06
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx
  - app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.test.tsx
  - app/[slug]/admin/(protected)/assinatura/page.tsx
autonomous: true
requirements: [SUB-01, SUB-02]

must_haves:
  truths:
    - "O usuario ve numa tela do admin o status atual da assinatura da empresa e a data ate quando o acesso esta pago"
    - "A proxima cobranca vem de uma consulta viva ao gateway a cada carregamento da tela"
    - "Se o gateway estiver indisponivel, a tela continua mostrando os fatos locais corretos e sinaliza so a parte que faltou"
    - "A tela nunca chama o gateway com um id de assinatura ja removida"
    - "Quem esta com assinatura vitalicia ve que nao ha cobranca e nao ve botao de cancelar"
  artifacts:
    - path: "app/[slug]/admin/(protected)/assinatura/page.tsx"
      provides: "RSC que decide os 5 estados na ordem normativa antes de qualquer I/O de gateway"
      contains: "requireAdminSession"
    - path: "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx"
      provides: "apresentacao dos 5 estados com as duas zonas de dado"
      contains: "AssinaturaCard"
      min_lines: 90
  key_links:
    - from: "app/[slug]/admin/(protected)/assinatura/page.tsx"
      to: "assinaturaService.fatosDeAssinatura"
      via: "leitura local anterior a qualquer decisao"
      pattern: "fatosDeAssinatura"
    - from: "app/[slug]/admin/(protected)/assinatura/page.tsx"
      to: "ultimoDiaDeAcessoEmSaoPaulo"
      via: "derivacao da data exibida, no servidor"
      pattern: "ultimoDiaDeAcessoEmSaoPaulo"
    - from: "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx"
      to: "CancelarAssinatura"
      via: "renderizacao condicional por podeCancelar"
      pattern: "CancelarAssinatura"
---

<objective>
A tela: `/{slug}/admin/assinatura` (D-06), cinco estados, duas zonas de dado.

**A ordem de avaliacao dos estados e normativa e inverte-la quebra o produto.** `acessoVitalicio` ->
`canceladoEm` -> `asaasSubscriptionId === null` -> consulta ao gateway. Os tres primeiros ramos NAO
chamam o Asaas. Se o ramo de `canceladoEm` viesse depois da consulta, o produto chamaria o gateway com o
id de uma assinatura que ele mesmo acabou de remover — e a doc e ambigua sobre o que aquele `GET`
devolve depois da remocao (suposicao A2 da pesquisa, ainda nao confirmada contra o sandbox). Ramificar
antes torna a tela independente dessa resposta.

**Estado A nao e caso de borda: e o primeiro minuto de todo assinante novo.** `Empresa.asaasSubscriptionId`
so e escrito no ramo `SUBSCRIPTION_CREATED` do webhook, e a homologacao `[03-07]` provou que
`PAYMENT_CONFIRMED` chega ANTES dele. Logo, no instante em que o usuario volta do checkout, a "consulta
viva ao Asaas" de D-02 literalmente nao tem id para consultar. E por isso que o poller do plano 07-02
tambem monta aqui.

**Duas zonas, e so uma degrada.** Zona 1 — status e "acesso ate" — vem de fatos locais e NUNCA fica
indisponivel. Zona 2 — proxima cobranca e ciclo — vem da leitura viva. No Estado D a Zona 1 renderiza
identica e completa, e so a Zona 2 vira um `InlineError`. A conta do usuario nao esta quebrada e a tela
nao pode sugerir que esta: nao esmaecer a Zona 1, e nao desabilitar o botao de cancelar — uma leitura
que falhou nao implica uma escrita que vai falhar, e pre-desabilitar negaria a acao exatamente na tela
que existe para oferece-la.

**A tela nao e alcancavel por quem esta bloqueado, e isso esta certo.** `(protected)` e fechado por
`acessoBloqueado`, entao `BLOQUEADO`/`CANCELADO`-expirado caem em `/bloqueado`, que ja tem o botao de
pagar da Fase 4 e agora tambem o poller. `/assinatura` serve TRIAL, EM_DIA, CARENCIA, VITALICIO e
cancelado-com-acesso-residual — exatamente o publico dos criterios #1 e #3 (§ Pitfall 5). Mover a rota
para fora de `(protected)` reintroduz a classe `ERR_TOO_MANY_REDIRECTS`.

Purpose: e a tela inteira de SUB-01 e o unico ponto de entrada de SUB-02.
Output: um server component de apresentacao e a page que toma as cinco decisoes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-gest-o-de-assinatura/07-CONTEXT.md
@.planning/phases/07-gest-o-de-assinatura/07-RESEARCH.md
@.planning/phases/07-gest-o-de-assinatura/07-PATTERNS.md
@.planning/phases/07-gest-o-de-assinatura/07-UI-SPEC.md
@.planning/phases/07-gest-o-de-assinatura/07-04-SUMMARY.md
@.planning/phases/07-gest-o-de-assinatura/07-05-SUMMARY.md
@app/[slug]/admin/(protected)/marca/page.tsx
@app/[slug]/admin/bloqueado/_components/bloqueado-card.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AssinaturaCard — os cinco estados e as duas zonas de dado</name>
  <files>app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx, app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.test.tsx</files>
  <read_first>
    - app/[slug]/admin/bloqueado/_components/bloqueado-card.tsx (arquivo inteiro: o contrato de props com actions ja vinculadas, o JSDoc "a prop que chega e a decisao ja tomada, nao o estado bruto", e o bloco `InlineError` das linhas 76-93 a ser reproduzido caractere-a-caractere)
    - app/[slug]/admin/(protected)/_components/aviso-carencia.tsx (o molde de copy por variante como funcao pura de dado ja calculado, e o uso de `role="status"`)
    - app/[slug]/admin/(protected)/page.tsx (o bloco `StatCard` — a origem do tile de valor; note que ele usa `font-bold` 700, e esta fase normaliza para `font-semibold` 600)
    - app/[slug]/admin/(protected)/assinatura/_components/cancelar-assinatura.tsx (o componente do plano 07-05 e seu contrato de props)
    - app/[slug]/admin/_components/poller-de-status.tsx (o componente do plano 07-02 e seu contrato de props)
    - app/[slug]/admin/_components/pagar-button.tsx (as props `variant`, `primaryColor`, `label`, `pendingLabel` apos o plano 07-03)
    - lib/format.ts (`formatCurrency`; note que `formatDate` NAO deve ser usada nesta fase — sem `timeZone`)
    - .planning/phases/07-gest-o-de-assinatura/07-UI-SPEC.md secoes "### 1. `AssinaturaCard` — the five states" (props, tabela dos 5 estados, estrutura de zonas com as classes exatas), "### 2. Status pill glyphs", "### 6. `InlineError`", § Color, § Typography, § Spacing Scale e § Interaction States
  </read_first>
  <behavior>
    - Estado "vitalicio": pill "Acesso permanente" com tom positivo; sem tile de data ("Sem data de término" no lugar); sem botao de pagar; sem botao de cancelar; Zona 2 ausente
    - Estado "aguardando": pill "Sem assinatura" tom neutro; heading "Sem assinatura ativa" e o corpo correspondente; botao com rotulo "Assinar agora"; a tira do poller presente; Zona 2 ausente
    - Estado "cancelada": pill "Cancelada" tom neutro; a data em "Acesso até"; botao "Reativar assinatura"; sem botao de cancelar; Zona 2 ausente; sem tira de poller
    - Estado "ativa": pill vindo de `rotuloStatus`/`tomStatus`; Zona 2 com "Próxima cobrança" preenchida; botao "Cancelar assinatura" presente
    - Estado "degradado": Zona 1 identica a do estado "ativa" (mesma data, mesmo pill, sem esmaecimento) e, no lugar da Zona 2, o `InlineError` com a copy E1; o botao "Cancelar assinatura" continua presente e habilitado
    - `erroCheckout` true renderiza o `InlineError` com a copy E3, byte-identica a de `bloqueado-card.tsx`
    - O componente nunca chama `new Date()`, nunca constroi `Intl.DateTimeFormat` e nunca compara `StatusAcesso`
    - Existe exatamente um `<h1>` na arvore renderizada, em todos os estados
  </behavior>
  <action>
Criar `app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx` como SERVER component
(sem `"use client"` — ele compoe dois client components, mas nao tem estado proprio).

Contrato de props exportado como `AssinaturaCardProps`, todos ja serializados e todas as decisoes ja
tomadas pelo servidor:
`estado: "vitalicio" | "aguardando" | "cancelada" | "ativa" | "degradado"`;
`rotuloStatus: string`; `tomStatus: "positivo" | "atencao" | "neutro"`;
`ultimoDiaDeAcesso: string | null` (JA formatado `dd/mm/aaaa` em America/Sao_Paulo);
`precoMensal: string` (JA formatado por `formatCurrency`);
`gateway: { proximaCobranca: string | null; ciclo: string | null; disponivel: boolean }`;
`podePagar: boolean`; `rotuloPagar: string`; `podeCancelar: boolean`;
`primaryColor: string`; `erroCheckout: boolean`;
`pagarAction: () => Promise<void>`;
`cancelarAction: () => Promise<{ error?: string }>`;
`consultarStatusAction: () => Promise<{ liberado: boolean }>`.

Mesma disciplina de `BloqueadoCard` e `AvisoCarencia`, e registrar em JSDoc: o componente nao contem
`new Date()`, nem construtor de `Intl`, nem comparacao de `StatusAcesso`, nem ramo sobre `canceladoEm`.
A page toma as cinco decisoes; aqui so se desenha.

Estrutura, com as classes literais da UI-SPEC:
- Wrapper `space-y-6`; cabecalho com `<h1 className="text-xl/[1.2] font-semibold text-slate-800">Assinatura</h1>`
  e subtitulo `<p className="mt-2 text-sm/[1.5] text-slate-500">` com a copy
  "Veja o status da sua assinatura, até quando o acesso está pago e cancele quando quiser."
- Card `rounded-2xl border border-slate-100 bg-white p-6 shadow-sm` — o card do dashboard/estoque, NAO o
  shell `rounded-3xl ... shadow-xl` de tela cheia de `bloqueado-card.tsx`: esta tela vive DENTRO do
  chrome do admin, e uma pagina, nao um takeover.
- Pill: `inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-semibold`, com a rampa
  escolhida por `tomStatus` — positivo `bg-emerald-50 text-emerald-700`, atencao `bg-amber-50
  text-amber-800`, neutro `bg-slate-100 text-slate-600`. Um `<svg className="h-4 w-4">` por tom, cor
  herdada por `currentColor` (nenhum `text-*` no `<svg>`): positivo = check-circle
  `M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z`; atencao = triangulo de aviso, COPIADO de
  `aviso-carencia.tsx`; neutro = minus-circle `M15 12H9m12 0a9 9 0 11-18 0 9 9 0 0118 0z`. O cadeado de
  `bloqueado-card.tsx` NAO aparece aqui: e o vocabulario da Fase 4 para acesso revogado, e ninguem que
  alcanca esta tela teve acesso revogado.
- ZONA 1 (fatos locais, nunca degrada): `mt-6 grid gap-4 sm:grid-cols-2` com dois tiles
  `rounded-xl bg-slate-50 p-4`. Tile "Acesso até" com label
  `text-xs font-semibold uppercase tracking-wide text-slate-500` e valor
  `mt-1 text-2xl/[1.2] font-semibold text-slate-800` — este e o UNICO valor em tamanho Display da tela.
  Quando `ultimoDiaDeAcesso` e `null` (vitalicio), o valor e substituido pela linha "Sem data de término"
  em tamanho Body (`text-sm/[1.5]`), nunca por travessao nem por tile vazio. Tile "Plano" com valor
  `{precoMensal} por mês` em Body.
- ZONA 2 (fatos do gateway, a unica que degrada): renderizada apenas nos estados "ativa" e "degradado".
  Com `gateway.disponivel`, um tile `mt-4 rounded-xl bg-slate-50 p-4` com label "Próxima cobrança" e o
  valor. Sem disponibilidade, o `InlineError` com a copy E1:
  "Não foi possível carregar os dados de cobrança agora. Seu acesso não foi afetado." — as duas frases
  sao obrigatorias; a primeira sozinha e lida como "minha conta quebrou", e nao quebrou.
- Estado "aguardando": alem do pill, um bloco com heading secundario "Sem assinatura ativa" (NAO um
  segundo `<h1>`) e o corpo "Não encontramos uma assinatura para esta loja. Se você acabou de pagar, a
  confirmação pode levar alguns minutos.", e o `<PollerDeStatus consultarAction={consultarStatusAction} />`.
- `erroCheckout` renderiza o `InlineError` com a copy E3 byte-identica a de `bloqueado-card.tsx:90-92`
  ("Não foi possível abrir o pagamento agora. Tente novamente em alguns instantes.").
- `podePagar` renderiza `<form action={pagarAction} className="mt-6">` com
  `<PagarButton variant="bloqueado" primaryColor={primaryColor} label={rotuloPagar} />`. A variante
  `banner` (amber-600 fixo) pertence a tira de carencia e nao pode aparecer aqui.
- `podeCancelar` renderiza `<CancelarAssinatura ultimoDiaDeAcesso={...} cancelarAction={cancelarAction} />`.
  Como `CancelarAssinatura` exige a data nao-nula, esta renderizacao so acontece quando `podeCancelar` e
  true — e a page so o marca true com data disponivel.

PROIBIDO neste componente (UI-SPEC § Non-Goals): duplicar `AvisoCarencia` (o layout ja o renderiza
acima de `children` em toda tela do admin — um segundo bloco ambar dobraria o aviso para quem esta em
carencia); exibir o enum `ACTIVE`/`EXPIRED`/`INACTIVE` do Asaas; variante SIMPLES da tela; historico de
cobrancas; qualquer UI de meio de pagamento; qualquer peso de fonte 700; `dangerouslySetInnerHTML`;
qualquer dependencia nova.

Escrever `assinatura-card.test.tsx` cobrindo os cinco estados de `<behavior>` com um objeto de props base
compartilhado e sobrescritas por caso, no molde de `bloqueado-card.test.tsx`. Sera necessario
`vi.mock("next/navigation")` para `useRouter` (os dois client components filhos o consomem). Casos
obrigatorios: exatamente um `<h1>` em cada estado; Zona 1 presente e nao esmaecida no estado
"degradado"; botao de cancelar presente e HABILITADO no estado "degradado"; ausencia do botao de
cancelar no estado "vitalicio"; a tira de poller presente somente em "aguardando".
  </action>
  <verify>
    <automated>npx vitest run "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.test.tsx"` sai 0
    - `assinatura-card.tsx` NAO comeca com `"use client"`
    - `grep -cE 'new Date\(|Intl\.|StatusAcesso' "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx"` devolve 0
    - `grep -cE 'ACTIVE|EXPIRED|INACTIVE' "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx"` devolve 0
    - `grep -c 'font-bold' "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx"` devolve 0
    - `grep -c 'AvisoCarencia' "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx"` devolve 0
    - O arquivo contem a string exata `Não foi possível abrir o pagamento agora. Tente novamente em alguns instantes.` e essa string tambem existe em `bloqueado-card.tsx`
    - O arquivo contem `Não foi possível carregar os dados de cobrança agora. Seu acesso não foi afetado.`
    - Existe caso de teste afirmando exatamente um `<h1>` em cada um dos 5 estados
    - Existe caso de teste afirmando que, no estado "degradado", o tile de "Acesso até" tem o mesmo texto que no estado "ativa" e o botao de cancelar esta habilitado
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0
  </acceptance_criteria>
  <done>A tela desenha os cinco estados, degrada apenas a zona do gateway e nao toma nenhuma decisao por conta propria.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx — as cinco decisoes na ordem normativa, antes de qualquer I/O de gateway</name>
  <files>app/[slug]/admin/(protected)/assinatura/page.tsx</files>
  <read_first>
    - app/[slug]/admin/(protected)/marca/page.tsx (arquivo inteiro: `params` como `Promise` no Next 16, `requireAdminSession(slug)` chamado NA PAGE — layout nao e boundary, T-04-06 — e o padrao `action={fn.bind(null, slug)}`)
    - app/[slug]/admin/bloqueado/page.tsx (o `searchParams` tipado como `Promise<{ erro?: string }>` e o `erroCheckout={erro === "checkout"}`; e a guarda simetrica que NAO deve ser replicada aqui, porque dentro de `(protected)` quem faz isso e `requireAdminSession`)
    - lib/session.ts (linhas 124-166: o que `requireAdminSession` ja garante — sessao valida, nao bloqueado, termos aceitos — e o fato de devolver `AuthTokenPayload`, nao `ContaAtiva`)
    - lib/auth-guard.ts (a interface `ContaAtiva` com `statusAcesso`, e a memoizacao por `React.cache` que torna uma segunda chamada de `revalidarConta` no mesmo request gratuita)
    - app/services/assinatura.service.ts (os tres metodos do plano 07-04 e a interface `FatosDeAssinatura`)
    - lib/avaliar-acesso.ts (`ultimoDiaDeAcessoEmSaoPaulo` do plano 07-01)
    - lib/billing/asaas/config.ts (a const `VALOR_PLANO_MENSAL`, hoje 29.9 — o preco nunca aparece como literal na tela)
    - app/services/empresa.service.ts (`findHeaderData`, usado por `bloqueado/page.tsx` para obter `primaryColor` com fallback)
    - app/[slug]/admin/_lib/assinatura-actions.ts (`iniciarPagamento` e `consultarStatusAcesso`)
    - .planning/phases/07-gest-o-de-assinatura/07-RESEARCH.md § Pattern 3 (a tabela de ordem normativa) e § Pitfall 5
    - .planning/phases/07-gest-o-de-assinatura/07-UI-SPEC.md § Surface Inventory e a tabela dos cinco estados
  </read_first>
  <action>
Criar `app/[slug]/admin/(protected)/assinatura/page.tsx` como Server Component assincrono, com
`params: Promise<{ slug: string }>` e `searchParams: Promise<{ erro?: string }>` (Next 16).

Sequencia:
1. `const { slug } = await params;` e `const { erro } = await searchParams;`
2. `const session = await requireAdminSession(slug);` — chamado NA PAGE, nao no layout. Ele ja garante
   sessao valida, empresa nao bloqueada e termos aceitos; nao replicar nenhuma dessas guardas aqui.
3. `const conta = await revalidarConta(session.sub, session.empresaId);` — gratuito, memoizado por
   `React.cache` no mesmo request. Se `null`, `redirect(\`/${slug}/admin/login\`)` FORA de qualquer
   `try`. E daqui que sai `conta.statusAcesso`, a unica autoridade de status (BILL-01).
4. `const fatos = await assinaturaService.fatosDeAssinatura(session.empresaId);` — se `null`,
   `notFound()`.
5. `const empresa = await empresaService.findHeaderData(session.empresaId);` para `primaryColor`, com o
   mesmo fallback `?? "#18181b"` de `bloqueado/page.tsx`.
6. `const ultimoDiaDeAcesso = ultimoDiaDeAcessoEmSaoPaulo(fatos);` — a derivacao mora em `lib/`, nunca
   aqui e nunca no componente.
7. Decidir `estado` NA ORDEM NORMATIVA, e so o quarto ramo faz I/O de gateway:
   a. `fatos.acessoVitalicio === true` -> `"vitalicio"`;
   b. `fatos.canceladoEm !== null` -> `"cancelada"`;
   c. `fatos.asaasSubscriptionId === null` -> `"aguardando"`;
   d. senao `const statusGateway = await assinaturaService.consultarAssinatura(fatos.asaasSubscriptionId);`
      e `estado = statusGateway.origem === "asaas" ? "ativa" : "degradado"`.
   Comentar que inverter (a)/(b) com (d) faria o produto chamar o Asaas com o id de uma assinatura que
   ele mesmo removeu, cuja resposta a doc nao define (suposicao A2).
8. Derivar `rotuloStatus`/`tomStatus`: "vitalicio" -> "Acesso permanente"/positivo; "cancelada" ->
   "Cancelada"/neutro; "aguardando" -> "Sem assinatura"/neutro; "ativa" e "degradado" -> derivados do
   status LOCAL de `conta.statusAcesso`: `CARENCIA` -> "Pagamento pendente"/atencao, qualquer outro ->
   "Ativa"/positivo. O tom NUNCA vem do gateway.
9. `podePagar = estado === "aguardando" || estado === "cancelada"`, com
   `rotuloPagar = estado === "cancelada" ? "Reativar assinatura" : "Assinar agora"`.
   `podeCancelar = (estado === "ativa" || estado === "degradado") && ultimoDiaDeAcesso !== null` — sem
   data, D-03 e insatisfazivel e o botao nao pode existir.
10. `precoMensal = formatCurrency(VALOR_PLANO_MENSAL)` — nunca o literal "29,90".
11. Renderizar `<AssinaturaCard ... />` com as actions ja vinculadas:
    `pagarAction={iniciarPagamento.bind(null, slug)}`,
    `cancelarAction={cancelarAssinatura.bind(null, slug)}`,
    `consultarStatusAction={consultarStatusAcesso.bind(null, slug)}`,
    e `erroCheckout={erro === "checkout"}`.

PROIBIDO nesta page: qualquer acesso direto a `prisma` (a leitura passa pelo service); ler
`ultimoStatusAuditado`; comparar rotulos de status a mao para decidir bloqueio (o predicado unico e
`acessoBloqueado`, e quem o aplica e `requireAdminSession`); mover a rota para fora de `(protected)`;
editar `(protected)/layout.tsx`, que carrega comentario normativo dizendo que nao e e nao pode virar o
boundary de autorizacao.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0
    - `npm test` sai 0
    - `page.tsx` contem `requireAdminSession(slug)` e `assinaturaService.fatosDeAssinatura`
    - No corpo de `page.tsx`, a linha que contem `acessoVitalicio` e a que contem `canceladoEm` aparecem ANTES da que contem `consultarAssinatura`
    - `grep -c 'prisma' "app/[slug]/admin/(protected)/assinatura/page.tsx"` devolve 0
    - `grep -c 'ultimoStatusAuditado' "app/[slug]/admin/(protected)/assinatura/page.tsx"` devolve 0
    - `grep -c '29' "app/[slug]/admin/(protected)/assinatura/page.tsx"` devolve 0 (o preco vem de `VALOR_PLANO_MENSAL`)
    - `git status --porcelain "app/[slug]/admin/(protected)/layout.tsx"` vazio
    - `npm run dev` servindo `/{slug}/admin/assinatura` para uma empresa em trial renderiza a tela sem erro de servidor (verificacao manual anotada no SUMMARY; a prova automatizada e o e2e do plano 07-07)
  </acceptance_criteria>
  <done>A rota existe, decide os cinco estados na ordem certa e so fala com o gateway quando ha um id valido para consultar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> rota protegida | `requireAdminSession(slug)` na propria page e o boundary; layout nao e (T-04-06) |
| page -> API do Asaas | Uma chamada de rede externa por carregamento de tela (D-02), que pode falhar |
| fatos de billing -> catalogo publico | Fronteira que esta fase nao atravessa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-33 | Elevation of Privilege | acesso a `/assinatura` | mitigate | `requireAdminSession(slug)` chamado na page; a rota permanece em `(protected)`; nenhuma guarda propria reimplementada |
| T-07-34 | Information Disclosure | dados de outra empresa na tela | mitigate | Toda leitura passa por `assinaturaService.fatosDeAssinatura(session.empresaId)`, que usa `findFirst({ where: { id, deletedAt: null } })`; nenhum id vem de `params` ou `searchParams` |
| T-07-35 | Information Disclosure | `senhaHash` arrastado para a tela | mitigate | `select` explicito no service; a page nao toca `prisma` |
| T-07-36 | Denial of Service | gateway fora do ar derrubando a tela | mitigate | `consultarAssinatura` nunca relanca; Estado D renderiza Zona 1 completa e um `InlineError` apenas na Zona 2 |
| T-07-37 | Tampering | chamada ao gateway com id de assinatura removida | mitigate | Ordem normativa: os ramos vitalicio/cancelada/sem-id saem antes de qualquer I/O; acceptance criteria verifica a ordem das linhas |
| T-07-38 | Spoofing | segunda autoridade de status vinda do gateway | mitigate | `tomStatus` e `rotuloStatus` derivam de `conta.statusAcesso`; o enum do Asaas nao tem campo no tipo nem aparece no componente (grep = 0) |
| T-07-39 | Denial of Service | loop de redirect por rota mal posicionada | mitigate | A rota fica em `(protected)`; nenhuma guarda simetrica duplicada; `layout.tsx` intocado |
| T-07-SC | Tampering | dependencia de UI introduzida pela tela | mitigate | Markup Tailwind puro e SVG inline; `git diff package.json` vazio |
</threat_model>

<verification>
- `npx vitest run "app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.test.tsx"` verde
- `npm test` verde
- `npx tsc --noEmit` sai 0
- `npm run lint` sai 0
- `git diff package.json` vazio
- `git status --porcelain "app/[slug]/admin/(protected)/layout.tsx"` vazio
</verification>

<success_criteria>
1. Uma empresa em trial ou em dia ve status, "acesso ate" e proxima cobranca em `/{slug}/admin/assinatura`
2. Com o gateway indisponivel, a Zona 1 continua correta e completa e apenas a Zona 2 sinaliza a falta
3. Empresa cancelada ve "Cancelada", a data residual e "Reativar assinatura" — e o Asaas nao e chamado
4. Empresa vitalicia ve "Acesso permanente", sem data de termino e sem botao de cancelar
5. A tela nunca exibe o enum de status do Asaas e nunca calcula data por conta propria
</success_criteria>

## Artifacts this phase produces

Simbolos criados por ESTE plano:

| Simbolo | Tipo | Arquivo |
|---------|------|---------|
| `AssinaturaCard` | server component exportado | `app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx` |
| `AssinaturaCardProps` | interface exportada | `app/[slug]/admin/(protected)/assinatura/_components/assinatura-card.tsx` |
| `AdminAssinaturaPage` (default export) | rota RSC | `app/[slug]/admin/(protected)/assinatura/page.tsx` |
| rota `/{slug}/admin/assinatura` | rota nova | — |

Simbolos criados por OUTROS planos desta fase: `formatarDiaEmSaoPaulo`, `acessoEfetivoAte`,
`ultimoDiaDeAcessoEmSaoPaulo`, `AsaasAssinaturaRemovida`, `AsaasClient.removerAssinatura`,
`consultarStatusAcesso`, `PollerDeStatus`, `PagarButtonProps.label`, `PagarButtonProps.pendingLabel`,
`ICONS.assinatura`, `StatusAssinatura`, `FatosDeAssinatura`, `EMPRESA_ASSINATURA_SELECT`,
`AssinaturaService.fatosDeAssinatura`, `AssinaturaService.consultarAssinatura`,
`AssinaturaService.cancelar`, `cancelarAssinatura`, `CancelarAssinatura`,
`e2e/cancelamento-de-assinatura.spec.ts`, `scripts/gates-fase-07.mjs`, `gates:fase-07`.

<output>
Create `.planning/phases/07-gest-o-de-assinatura/07-06-SUMMARY.md` when done
</output>
