---
phase: 01-pr-requisitos-de-produ-o
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - .planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md
autonomous: false
requirements: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "lint, suíte unitária e suíte e2e passam juntos no mesmo estado do repositório"
    - "A contagem de conexões do Postgres permanece estável sob carga repetida de admin + rotas de API (critério de sucesso #1)"
    - "Admin, registro e catálogo público continuam funcionando como antes para uma empresa ativa (critério de sucesso #4)"
    - "proxy.ts e prisma/schema.prisma seguem intocados na fase inteira"
  artifacts:
    - path: ".planning/phases/01-pr-requisitos-de-produ-o/01-05-SUMMARY.md"
      provides: "Registro do gate da fase, resultado da contagem de conexões e dívidas repassadas às Fases 2 e 5"
  key_links:
    - from: "lib/prisma.ts"
      to: "PostgreSQL (pg_stat_activity)"
      via: "verificação humana de estabilidade de conexões sob carga"
      pattern: "pg_stat_activity"
---

<objective>
Fechar a Fase 1 provando os dois critérios de sucesso do roadmap que nenhum teste unitário consegue provar sozinho: (#1) a contagem de conexões do Postgres permanece estável sob carga repetida, e (#4) nenhuma regressão de comportamento em admin, registro e catálogo público.

Purpose: o teste de `lib/prisma.test.ts` prova o singleton **por processo**; ele não prova nada sobre conexões reais de banco. E a suíte automatizada não substitui um olhar humano sobre os três fluxos existentes depois de uma mudança que altera o comportamento de auth em runtime para todas as sessões vivas.
Output: gate automatizado completo executado e registrado, e um checkpoint humano com a evidência da contagem de conexões.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-CONTEXT.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-01-SUMMARY.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-03-SUMMARY.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-04-SUMMARY.md
</context>

## Artifacts this phase produces

Símbolos e arquivos criados pela **Fase 1** como um todo (nenhum destes existe hoje — não sinalizar como drift):

| Artefato | Tipo | Onde |
|----------|------|------|
| `createPrismaClient()` | função factory (module-local) | `lib/prisma.ts` |
| `lib/prisma.test.ts` | novo arquivo de teste | raiz de `lib/` |
| `lib/auth-guard.ts` | novo arquivo (DAL) | raiz de `lib/` |
| `revalidarConta(usuarioId, empresaId)` | const exportada (async, embrulhada em `React.cache`) | `lib/auth-guard.ts` |
| `ContaAtiva` | interface exportada (`usuarioId`, `empresaId`, `empresaSlug`, `email`, `role`) | `lib/auth-guard.ts` |
| `lib/auth-guard.test.ts` | novo arquivo de teste | raiz de `lib/` |
| `getVerifiedSession()` | função async exportada | `lib/session.ts` |
| stub default de conta ativa no `beforeEach` | fixture de teste | `tests/setup/prisma-mock.ts` |
| `e2e/sessao-revogada.spec.ts` | novo spec Playwright | `e2e/` |

Não são produzidos nesta fase: nenhuma dependência npm nova, nenhuma variável de ambiente nova, nenhuma alteração em `prisma/schema.prisma`, nenhuma migration.

<tasks>

<task type="auto">
  <name>Task 1: Executar o gate automatizado completo da fase</name>
  <files>(nenhum arquivo de produção é alterado — task de verificação)</files>
  <read_first>
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md` § "Sampling Rate" — o gate de fase é `npm run lint && npm test && npm run test:e2e` verdes
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md` § "Per-Task Verification Map" — as 13 linhas cujo status precisa sair de `pending`
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `01-03-SUMMARY.md`, `01-04-SUMMARY.md` — o que cada plano de fato entregou e quais desvios foram registrados
    - `package.json` (bloco `scripts`) — comandos exatos: `lint`, `test`, `test:coverage`, `test:e2e`
    - `.github/workflows/tests.yml` — como o CI roda os mesmos comandos e provisiona `postgres:16`
  </read_first>
  <action>
    Rodar, no mesmo estado do repositório e nesta ordem, registrando a saída resumida de cada um: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run test:e2e`.

    Se `npm run test:e2e` não puder rodar por ausência de Postgres local, NÃO declarar o gate como cumprido: registrar explicitamente que a suíte e2e foi delegada ao CI e anexar o link/identificador do run do GitHub Actions. Não marcar critérios como atendidos com base em suposição.

    Além dos comandos, confirmar por asserção de fonte que os limites da fase foram respeitados ao longo dos quatro planos anteriores.

    Atualizar `.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md`: marcar as linhas do "Per-Task Verification Map" com o status real (verde/vermelho) e virar `nyquist_compliant` para `true` no frontmatter apenas se todas as linhas automatizadas estiverem verdes e as manuais estiverem cobertas pelo checkpoint da Task 2.

    Não alterar nenhum arquivo de produção nesta task. Se algo estiver vermelho, o correto é reportar e voltar ao plano responsável, não remendar aqui.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` sai com código 0
    - `npx tsc --noEmit` sai com código 0
    - `npm test` sai com código 0 e reporta 0 testes falhando
    - `npm run test:e2e` sai com código 0 — ou, se Postgres local estiver indisponível, o run correspondente do CI está verde e seu identificador está registrado no SUMMARY
    - `git diff --name-only main -- proxy.ts` não retorna nada (D-06 respeitado na fase inteira)
    - `git diff --name-only main -- prisma/schema.prisma` não retorna nada (nenhuma migration, nenhuma mudança de schema)
    - `git diff main -- package.json package-lock.json` não mostra nenhuma dependência adicionada (zero instalações — RESEARCH § Package Legitimacy Audit)
    - `grep -rcE "acessoAte|trialFim|canceladoEm|acessoVitalicio" lib/ app/ | grep -v ':0$'` não retorna nada (D-04: nenhuma lógica de pagamento antecipada)
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md` não contém mais nenhuma ocorrência de `⬜ pending` nas linhas automatizadas
  </acceptance_criteria>
  <done>Todo o gate automatizado da fase foi executado no mesmo estado do repositório e o resultado está registrado, sem nenhuma suposição sobre execuções que não aconteceram.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao humana — estabilidade de conexoes do Postgres e ausencia de regressao</name>
  <files>.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Code Examples > Verificar contagem de conexões do Postgres" — a query exata de `pg_stat_activity` a executar
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md` § "Manual-Only Verifications" — por que esta verificação é manual e qual é o critério de aprovação
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Pitfall 7" — a consequência aceita de D-01 que precisa ser reconhecida
    - `.planning/ROADMAP.md` § "Phase 1" — os 4 critérios de sucesso, em especial #1 (conexões estáveis) e #4 (sem regressão)
  </read_first>
  <action>
    Esta é uma verificação humana bloqueante — nenhum código é escrito. Apresente ao desenvolvedor o bloco `<what-built>` e o roteiro de `<how-to-verify>` abaixo, execute o que for possível automaticamente para preparar o terreno (subir `npm run build` e `npm start`) e então PARE e aguarde a resposta.

    Após a aprovação, registrar no `.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md` a linha manual `01-01-03` (contagem de conexões) com o status real e marcar `nyquist_compliant: true` no frontmatter apenas se todas as demais linhas já estiverem verdes pela Task 1.

    Se o desenvolvedor reprovar, NÃO remendar aqui: reabrir o plano responsável (01-03 para as camadas de auth, 01-04 para as guardas públicas, 01-01 para o singleton) e registrar o motivo.
  </action>
  <what-built>
    Toda a Fase 1 já foi automatizada e está verde no gate da Task 1:
    - `lib/prisma.ts` publica um único `PrismaClient` em `globalThis` em todos os ambientes (INFRA-01), com gate em `lib/prisma.test.ts`
    - `lib/auth-guard.ts` revalida a conta no banco a cada request, fail-closed, com 5 testes em `lib/auth-guard.test.ts`
    - `lib/session.ts` e `lib/api-auth.ts` consultam o DAL antes de autorizar (INFRA-02), com testes de conta revogada e erro de banco
    - `app/[slug]/admin/login/page.tsx` e `app/registro/page.tsx` usam `getVerifiedSession` (anti-loop), provado por `e2e/sessao-revogada.spec.ts`
    - `proxy.ts` e `prisma/schema.prisma` seguem intocados; zero dependências novas

    O que NÃO é automatizável e precisa de você: a contagem de conexões reais do Postgres sob carga (critério de sucesso #1 do roadmap) e um olhar humano sobre os três fluxos existentes (critério de sucesso #4). Automatizar isso exigiria um harness de carga desproporcional para uma correção de 3 linhas.
  </what-built>
  <how-to-verify>
    **Parte A — estabilidade de conexões (critério de sucesso #1)**

    1. Suba a aplicação em modo de produção contra o seu Postgres: `npm run build` seguido de `npm start`. O fix do singleton só vale a partir de um build novo — não use `npm run dev` para esta medição.
    2. Em um cliente SQL conectado ao mesmo banco, rode e anote o resultado como linha de base:
       `SELECT count(*) AS conexoes, application_name, state FROM pg_stat_activity WHERE datname = current_database() GROUP BY application_name, state ORDER BY conexoes DESC;`
    3. Exercite a aplicação por ~2 minutos misturando os dois tipos de entrada, que é o ponto do bug (camadas de bundle diferentes): navegue e recarregue várias vezes uma página do admin logado (`/{slug}/admin`) E dispare repetidamente uma rota de API autenticada (por exemplo `GET /api/produtos` com Bearer token, em laço).
    4. Rode a mesma query do passo 2 novamente e compare.
    5. **Esperado:** o número total de conexões estabiliza num teto compatível com um único pool (o `max` default de `pg` é 10) e NÃO cresce de forma contínua com o volume de requests. Um crescimento contínuo, ou um teto que seja múltiplo de 10, indica que ainda há mais de um `PrismaClient` vivo — nesse caso o INFRA-01 não está fechado.

    **Parte B — ausência de regressão (critério de sucesso #4)**

    Com uma empresa ativa (não revogada), confirme que nada mudou para quem está em dia:
    6. `/registro`: cadastre uma empresa nova e confirme que você cai direto no painel administrativo.
    7. `/{slug}/admin`: navegue pelas telas do admin (produtos, estoque, combos, promoções) e confirme que tudo carrega normalmente, sem redirect inesperado.
    8. Faça logout e login de novo pelo formulário — deve entrar normalmente.
    9. `/{slug}` (catálogo público): abra em uma janela anônima, sem sessão, e confirme que o catálogo aparece normalmente. Esta fase **não** deve ter tocado no catálogo público (o gate por pagamento é a Fase 4).
    10. Confirme que, navegando pelo admin logado, você não vê lentidão perceptível em relação ao comportamento anterior.

    **Parte C — anotação operacional**

    11. Ciente e de acordo: a partir desta fase, por decisão travada D-01 (fail-closed), uma indisponibilidade do Postgres passa a expulsar todos os admins logados e a devolver 401 em todas as rotas de API. Um pico de 401/redirects em produção pode significar banco fora do ar, não sessões revogadas. Os logs do servidor distinguem os dois casos pelo prefixo `[auth-guard]`.
  </how-to-verify>
  <verify>
    <human-check>Contagem de conexões estável entre os passos 2 e 4; fluxos dos passos 6-10 funcionando como antes; ciência do item 11 registrada</human-check>
  </verify>
  <acceptance_criteria>
    - O valor de `count(*)` de `pg_stat_activity` antes e depois da carga está registrado no SUMMARY, com os dois números explícitos
    - O número final não cresce proporcionalmente ao volume de requests e não é um múltiplo do `max` do pool que indique múltiplos clients
    - Cada um dos fluxos dos passos 6 a 10 tem um resultado explícito registrado (ok / problema encontrado)
    - Se qualquer fluxo apresentar regressão, o checkpoint é reprovado e o plano responsável (01-03 para auth, 01-04 para as guardas públicas) é reaberto — não seguir para `/gsd-verify-work`
    - O item 11 (consequência aceita de D-01) está explicitamente reconhecido no SUMMARY
  </acceptance_criteria>
  <resume-signal>Responda "aprovado" com os dois números de conexões, ou descreva o problema encontrado</resume-signal>
  <done>O critério de sucesso #1 do roadmap está comprovado com números reais de `pg_stat_activity`, os fluxos do critério #4 foram confirmados manualmente e a consequência aceita de D-01 está reconhecida no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aplicação (processo Node) → PostgreSQL | Fronteira onde o custo de conexões é observável; é o que a Parte A do checkpoint mede |
| operador humano → decisão de fechar a fase | O checkpoint é bloqueante: sem evidência numérica, o critério de sucesso #1 não pode ser declarado atendido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | pool de conexões do Postgres | mitigate | Medição direta de `pg_stat_activity` antes/depois de carga mista (páginas do admin + rota de API), sob `npm run build` + `npm start` — o único caminho que exercita camadas de bundle distintas no mesmo processo (Task 2, Parte A) |
| T-01-10 | Denial of Service | fail-closed derrubando sessões quando o banco cai | accept | Consequência travada por D-01. Mitigação operacional: reconhecimento explícito no checkpoint (Parte C) e prefixo `[auth-guard]` nos logs para distinguir falha de infraestrutura de conta revogada |
| T-01-16 | Repudiation | fase declarada concluída sem evidência do critério #1 | mitigate | Checkpoint bloqueante que exige os dois números de conexões registrados no SUMMARY; `gate="blocking"` e não auto-aprovável |
| T-01-03 | Elevation of Privilege | regressão silenciosa que reintroduza confiança só na borda | mitigate | Asserção de fonte na Task 1 de que `proxy.ts` não tem diff em relação a `main` na fase inteira (D-06) |
| T-01-SC | Tampering | instalações npm/pip/cargo | accept | Verificado na Task 1 por diff de `package.json`/`package-lock.json` contra `main`: a fase deve terminar com zero dependências adicionadas — RESEARCH § Package Legitimacy Audit |
</threat_model>

<verification>
1. `npm run lint` — código 0
2. `npx tsc --noEmit` — código 0
3. `npm test` — código 0
4. `npm run test:e2e` — código 0 (local ou CI, com o run identificado)
5. Diffs contra `main`: `proxy.ts`, `prisma/schema.prisma`, `package.json` e `package-lock.json` sem alteração
6. Checkpoint humano aprovado com os dois números de `pg_stat_activity` registrados
</verification>

<success_criteria>
- Critério de sucesso #1 do roadmap comprovado com números reais de `pg_stat_activity`, não por inferência a partir do teste unitário
- Critério de sucesso #4 comprovado: admin, registro e catálogo público funcionam como antes para uma empresa ativa
- INFRA-01 e INFRA-02 declarados atendidos com evidência: INFRA-01 pelo singleton + medição de conexões; INFRA-02 pela **infraestrutura** de revalidação (a parte de status de pagamento do texto do requisito é plugada na Fase 2 por D-04)
- Zero dependências novas, `proxy.ts` e `prisma/schema.prisma` intocados
</success_criteria>

<output>
Create `.planning/phases/01-pr-requisitos-de-produ-o/01-05-SUMMARY.md` when done.

Registrar no SUMMARY (itens obrigatórios, todos apontados pela pesquisa como necessários para não virarem dívida implícita):
- Os dois números de `pg_stat_activity` (antes e depois da carga) e as condições da medição
- Resultado explícito de cada fluxo da Parte B
- **INFRA-02 e a Fase 2:** o texto do requisito fala em "status de pagamento", que não existe nesta fase. INFRA-02 é considerado atendido aqui pela **infraestrutura** de revalidação (D-02). A Fase 2 deve reabrir o requisito para plugar `avaliarAcesso` dentro de `revalidarConta` (D-04). Propagar isto para `.planning/STATE.md` em Accumulated Context → Decisions
- **Dívida repassada à fase de deploy/hosting:** dimensionar o `max` do pool (default 10) e decidir entre `DATABASE_URL` pooled vs. direta quando worker (Fase 5) e webhooks (Fase 3) existirem — RESEARCH § Open Questions #5
- **Nota operacional (D-01 / T-01-10):** pico de 401 e redirects em produção pode significar Postgres indisponível, não sessões revogadas; distinguir pelo prefixo `[auth-guard]` nos logs
- **Sessões vivas:** todo JWT de 7 dias já emitido passa a ser revalidado no primeiro request após o deploy; contas ativas não notam diferença, contas revogadas caem na hora (esse é o objetivo)
</output>
