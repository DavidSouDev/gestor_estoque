---
phase: 01-pr-requisitos-de-produ-o
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/setup/prisma-mock.ts
  - lib/session.ts
  - lib/session.test.ts
  - lib/api-auth.ts
  - lib/api-auth.test.ts
  - app/api/combos/route.test.ts
  - app/api/combos/[id]/route.test.ts
  - app/api/empresas/route.test.ts
  - app/api/empresas/[id]/route.test.ts
  - app/api/movimentacoes/route.test.ts
  - app/api/movimentacoes/[id]/route.test.ts
  - app/api/produtos/route.test.ts
  - app/api/produtos/[id]/route.test.ts
  - app/api/promocoes/route.test.ts
  - app/api/promocoes/[id]/route.test.ts
  - app/api/usuarios/route.test.ts
  - app/api/usuarios/[id]/route.test.ts
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "requireAdminSession redireciona para o login quando a revalidação no banco rejeita a conta"
    - "requireAuth lança AuthError 401 quando a revalidação no banco rejeita a conta"
    - "Uma mudança no banco vale já no request seguinte, sem novo login e sem esperar o JWT expirar"
    - "getVerifiedSession existe e devolve null para uma sessão cujo JWT é válido mas a conta foi revogada"
    - "proxy.ts continua sem qualquer acesso ao Prisma (D-06)"
    - "A suíte completa (npm test) permanece verde após a mudança fail-closed"
  artifacts:
    - path: "tests/setup/prisma-mock.ts"
      provides: "Stub default de conta ativa após o mockReset — pré-requisito dos 12 arquivos de teste autenticados"
      contains: "usuario.findFirst"
      min_lines: 20
    - path: "lib/session.ts"
      provides: "requireAdminSession revalidando no banco + getVerifiedSession"
      exports: ["getSession", "getVerifiedSession", "requireAdminSession", "createAdminSession", "destroySession"]
      contains: "revalidarConta"
    - path: "lib/api-auth.ts"
      provides: "requireAuth revalidando no banco antes de autorizar"
      exports: ["AuthError", "requireAuth"]
      contains: "revalidarConta"
  key_links:
    - from: "lib/session.ts"
      to: "lib/auth-guard.ts"
      via: "import de revalidarConta pelo alias @/"
      pattern: "from \"@/lib/auth-guard\""
    - from: "lib/api-auth.ts"
      to: "lib/auth-guard.ts"
      via: "import de revalidarConta pelo alias @/"
      pattern: "from \"@/lib/auth-guard\""
    - from: "app/[slug]/admin/(protected)/layout.tsx"
      to: "lib/session.ts"
      via: "requireAdminSession(slug) já chamado no layout — herda a revalidação sem edição"
      pattern: "requireAdminSession"
---

<objective>
Ligar o DAL de revalidação (`revalidarConta`, criado no plano 01-02) nas **duas** camadas de auth do lado servidor (D-05): `requireAdminSession` em `lib/session.ts` (cookie, Server Components/layouts/Server Actions) e `requireAuth` em `lib/api-auth.ts` (Bearer, todas as rotas `app/api/*`). É aqui que o INFRA-02 passa a ter efeito real.

Purpose: `app/[slug]/admin/(protected)/layout.tsx` já chama `requireAdminSession(slug)` e todas as rotas `app/api/*/route.ts` já chamam `requireAuth(request)` — mudar essas duas funções propaga a revalidação para toda a superfície protegida sem editar dezenas de arquivos. Este plano também instala o stub default de conta ativa em `tests/setup/prisma-mock.ts`, sem o qual 14 arquivos de teste existentes passam a falhar com 401/redirect (regressão do critério de sucesso #4).
Output: `tests/setup/prisma-mock.ts` com stub default, `lib/session.ts` com `getVerifiedSession` + `requireAdminSession` revalidando, `lib/api-auth.ts` com `requireAuth` revalidando, testes estendidos e a suíte completa verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-CONTEXT.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-PATTERNS.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

Símbolos e arquivos criados pela **Fase 1** como um todo (nenhum destes existe hoje — não sinalizar como drift):

| Artefato | Tipo | Onde |
|----------|------|------|
| `createPrismaClient()` | função factory (module-local) | `lib/prisma.ts` |
| `lib/prisma.test.ts` | novo arquivo de teste | raiz de `lib/` |
| `lib/auth-guard.ts` | novo arquivo (DAL) | raiz de `lib/` |
| `revalidarConta(usuarioId, empresaId)` | const exportada (async, embrulhada em `React.cache`) | `lib/auth-guard.ts` |
| `ContaAtiva` | interface exportada (`usuarioId`, `empresaId`, `empresaSlug`, `email`, `role`) | `lib/auth-guard.ts` |
| `lib/auth-guard.test.ts` | novo arquivo de teste | raiz de `lib/` |
| `getVerifiedSession()` | função async exportada | `lib/session.ts` |
| stub default de conta ativa no `beforeEach` | fixture de teste | `tests/setup/prisma-mock.ts` |
| `e2e/sessao-revogada.spec.ts` | novo spec Playwright | `e2e/` |

Não são produzidos nesta fase: nenhuma dependência npm nova, nenhuma variável de ambiente nova, nenhuma alteração em `prisma/schema.prisma`, nenhuma migration.

<tasks>

<task type="auto">
  <name>Task 1: Stub default de conta ativa em tests/setup/prisma-mock.ts (pré-requisito de Wave 0)</name>
  <files>tests/setup/prisma-mock.ts</files>
  <read_first>
    - `tests/setup/prisma-mock.ts` (arquivo inteiro, 15 linhas) — estado atual: `vi.mock("@/lib/prisma", () => ({ prisma: mockDeep<PrismaClient>() }))`, export de `prismaMock`, e um `beforeEach` que só faz `mockReset(prismaMock)`
    - `tests/helpers/auth.ts` — `testAuthPayload` com `sub: "user-1"`, `empresaId: "empresa-1"`, `empresaSlug: "empresa-teste"`, `email: "admin@teste.com"`, `role: "ADMIN"`; os valores do stub têm que casar exatamente com estes
    - `tests/setup/vitest.setup.ts` — ordem de carregamento (`import "./prisma-mock"` é a última linha)
    - `lib/auth-guard.ts` (criado no plano 01-02) — a projeção exata da query, para o stub devolver o mesmo shape
    - `lib/prisma.test.ts` (criado no plano 01-01) — usa `vi.unmock("@/lib/prisma")`; verificar se este stub interfere nele
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Pitfall 3" — lista dos 14 arquivos afetados e o snippet de referência do stub
  </read_first>
  <action>
    Adicionar o stub default de conta ativa DENTRO do `beforeEach` já existente em `tests/setup/prisma-mock.ts`, imediatamente APÓS a linha `mockReset(prismaMock);`. A ordem é crítica: um stub colocado no `beforeEach` de outro arquivo registrado antes seria apagado pelo `mockReset`.

    O stub é `prismaMock.usuario.findFirst.mockResolvedValue({ id: "user-1", email: "admin@teste.com", role: "ADMIN", empresaId: "empresa-1", empresa: { slug: "empresa-teste" } } as never)`. Os valores casam com `testAuthPayload` de `tests/helpers/auth.ts` e o shape casa com a projeção de `revalidarConta`.

    Comentar acima do stub o motivo: default de "a conta do `testAuthPayload` existe e está ativa"; testes do caminho revogado sobrescrevem com `mockResolvedValue(null)` ou `mockRejectedValue(...)` no próprio caso.

    Verificado durante o planejamento: **nenhum** teste existente usa `prismaMock.usuario.findFirst` (apenas `empresa.findFirst`, `combo.findFirst`, `usuario.findUnique` e `usuario.findMany`), então o stub não colide com asserções atuais.

    Risco conhecido a checar nesta task: `lib/prisma.test.ts` chama `vi.unmock("@/lib/prisma")`, o que pode fazer o `prismaMock` deste setup resolver para o client real naquele arquivo — e aí `prismaMock.usuario.findFirst.mockResolvedValue` lançaria (`mockResolvedValue is not a function`). Rodar `npx vitest run lib/prisma.test.ts` logo após a edição. Se quebrar, a correção é guardar o stub atrás de uma checagem de que o alvo é de fato um mock (por exemplo, testar que `prismaMock?.usuario?.findFirst` expõe `mockResolvedValue` como função antes de chamá-lo) — sem remover o stub e sem alterar `lib/prisma.test.ts`, que é o gate do INFRA-01.

    Não alterar o `vi.mock` do topo, nem o export de `prismaMock`, nem `tests/setup/vitest.setup.ts`.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` sai com código 0 (suíte inteira verde; nenhum consumidor de `revalidarConta` foi ligado ainda, então o stub é inerte por enquanto)
    - `npx vitest run lib/prisma.test.ts` sai com código 0 (o gate do INFRA-01 continua funcionando sob o novo stub)
    - `grep -c "usuario.findFirst" tests/setup/prisma-mock.ts` retorna `1`
    - Em `tests/setup/prisma-mock.ts`, a linha do stub aparece depois da linha `mockReset(prismaMock);` dentro do mesmo `beforeEach` (verificável por `grep -n`: número de linha do stub > número de linha do `mockReset`)
    - `grep -c '"empresa-teste"' tests/setup/prisma-mock.ts` retorna `1` e `grep -c '"user-1"' tests/setup/prisma-mock.ts` retorna `1`
    - `git diff --stat` mostra alteração somente em `tests/setup/prisma-mock.ts`
  </acceptance_criteria>
  <done>Existe um default de "conta ativa" no setup de testes, ele não quebra nada hoje e é o que vai manter os 12 arquivos de rota autenticada verdes nas Tasks 2 e 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revalidação no banco em lib/session.ts (requireAdminSession + getVerifiedSession)</name>
  <files>lib/session.ts, lib/session.test.ts</files>
  <read_first>
    - `lib/session.ts` (arquivo inteiro, 59 linhas) — `createAdminSession`, `destroySession`, `getSession` (padrão de early-return `null`), `requireAdminSession` (linhas 45-59, com `redirect()` como statement puro e retorno `Promise<AuthTokenPayload>`)
    - `lib/session.test.ts` (arquivo inteiro, 110 linhas) — header com `vi.hoisted` para `cookieStore`/`redirectMock`, mocks de `next/headers` e `next/navigation` declarados acima do import da unidade, e os 3 casos existentes de `requireAdminSession`
    - `lib/auth-guard.ts` — assinatura de `revalidarConta` e shape de `ContaAtiva` (produzidos no plano 01-02)
    - `tests/setup/prisma-mock.ts` (já editado na Task 1) — o stub default que faz os casos de sucesso continuarem verdes
    - `app/[slug]/admin/(protected)/layout.tsx` (linhas 1-16) — o consumidor mais alto: chama `requireAdminSession(slug)` uma vez e herda a mudança sem edição
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Pattern 3" — implementação de referência e a ordem obrigatória das checagens; § "Pitfall 2" (não chamar `destroySession` aqui) e § "Pitfall 5" (`redirect()` nunca dentro de `try`)
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-PATTERNS.md` § "`lib/session.ts` (MODIFIED)"
  </read_first>
  <behavior>
    - Teste (novo): JWT válido + slug batendo + `prismaMock.usuario.findFirst` resolve `null` → `requireAdminSession("empresa-teste")` rejeita com `REDIRECT:/empresa-teste/admin/login`.
    - Teste (novo): JWT válido + slug batendo + `prismaMock.usuario.findFirst` rejeita com erro de banco → `requireAdminSession("empresa-teste")` rejeita com `REDIRECT:/empresa-teste/admin/login` (fail-closed, D-01).
    - Teste (novo): sem sessão (cookie ausente) → `prismaMock.usuario.findFirst` NÃO é chamado (o banco não é tocado antes de o token ser válido e o slug bater — T-01-08).
    - Teste (novo): JWT com `empresaSlug` de outra empresa → `prismaMock.usuario.findFirst` NÃO é chamado.
    - Teste (novo): mudança no banco entre dois requests (critério de sucesso #2) — primeira chamada com o stub de conta ativa resolve o payload; troca-se o stub para `null`; a segunda chamada redireciona, sem novo login.
    - Teste (novo): `getVerifiedSession()` devolve o payload quando o banco confirma a conta, e `null` quando o banco devolve `null` ou rejeita.
    - Testes existentes (3 de `requireAdminSession` + 3 de `getSession` + `createAdminSession` + `destroySession`): continuam passando sem edição de asserção.
  </behavior>
  <action>
    Editar `lib/session.ts` importando `revalidarConta` de `@/lib/auth-guard` (alias `@/`, C-02).

    Adicionar a função exportada `getVerifiedSession(): Promise<AuthTokenPayload | null>` — chama `getSession()`; se for `null`, devolve `null`; caso contrário chama `revalidarConta(session.sub, session.empresaId)` e devolve `session` se a conta veio, ou `null` se veio `null`. JSDoc explicando que é a versão "verificada no banco" do `getSession`, destinada às telas públicas que precisam decidir "esse usuário já está logado?" (login e registro) — e que devolver o payload ali sem revalidar recria o loop de redirect do § Pitfall 1.

    Alterar `requireAdminSession(slug)`: manter `const session = await getSession();`, depois obter `conta` chamando `revalidarConta(session.sub, session.empresaId)` **somente** quando `session` existe **e** `session.empresaSlug === slug`; caso contrário `conta` é `null`. A condição de falha passa a ser `!session || !conta || conta.empresaSlug !== slug`, e o `redirect(\`/${slug}/admin/login\`)` continua sendo um statement puro logo abaixo. O retorno continua sendo `session` (tipo `Promise<AuthTokenPayload>`) — trocar o tipo de retorno para `ContaAtiva` quebraria ~40 call sites e violaria o critério de sucesso #4.

    Três detalhes de ordem que não podem ser perdidos:
    1. O banco só é consultado depois de o token ser válido E o slug bater — um cookie forjado ou de outro tenant nunca gera query (T-01-08: auth não vira vetor de carga no banco).
    2. A comparação extra `conta.empresaSlug !== slug` cumpre D-03 (comparar o dado fresco do banco contra o token) e fecha a janela do slug obsoleto — `slug` é mutável via `PATCH /api/empresas/[id]` (`UpdateEmpresaDTO.slug` em `app/services/empresa.service.ts`), então um token de 7 dias pode carregar um slug que não existe mais.
    3. Nenhum `try/catch` novo neste arquivo. O único `try` da fase mora dentro de `revalidarConta`. Um `try` em volta de `redirect()` engoliria o `NEXT_REDIRECT` e deixaria o usuário revogado passar silenciosamente (§ Pitfall 5).

    PROIBIDO: chamar `destroySession()` de dentro de `requireAdminSession` — `cookies().delete()` só é legal em Server Function ou Route Handler, e este código roda em layout/page; chamado dali, lança (§ Pitfall 2). O cookie stale é inofensivo desde que toda superfície que o consome revalide, que é exatamente o que o plano 01-04 completa. `destroySession` continua alcançável apenas pela Server Action `logout`.

    Estender `lib/session.test.ts` com os casos do bloco `<behavior>`, reaproveitando o header de mocks já existente (`vi.hoisted`, `cookieStore`, `redirectMock`) e importando `prismaMock` de `@/tests/setup/prisma-mock`. Atenção ao `beforeEach` já presente no arquivo, que faz `vi.clearAllMocks()`: isso limpa histórico de chamadas mas preserva implementações, então o stub default do setup sobrevive — confirmar isso na prática ao rodar, e se não sobreviver, re-aplicar o stub dentro do `beforeEach` deste arquivo em vez de alterar o setup global.
  </action>
  <verify>
    <automated>npx vitest run lib/session.test.ts lib/auth-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/session.test.ts` sai com código 0, e o arquivo contém no mínimo 6 blocos `it(` a mais do que antes desta task
    - `grep -c "export async function getVerifiedSession" lib/session.ts` retorna `1`
    - `grep -c 'from "@/lib/auth-guard"' lib/session.ts` retorna `1`
    - `grep -vE '^\s*(//|\*|/\*)' lib/session.ts | grep -c "try {"` retorna `1` (apenas o `try` pré-existente dentro de `getSession`, nenhum novo)
    - `grep -c "destroySession" lib/session.ts` retorna `1` (somente a definição; `requireAdminSession` não a chama)
    - `grep -c "conta.empresaSlug" lib/session.ts` retorna `1`
    - `grep -vE '^\s*(//|\*|/\*)' lib/session.ts | grep -c "redirect("` retorna `1` e a linha não está indentada dentro de nenhum bloco `try`
    - A assinatura pública não mudou: `grep -c "requireAdminSession(slug: string): Promise<AuthTokenPayload>" lib/session.ts` retorna `1`
    - `npx tsc --noEmit` não reporta erro novo
  </acceptance_criteria>
  <done>Toda page/layout/Server Action do admin passa a exigir uma conta confirmada no banco; uma sessão revogada é redirecionada no request seguinte, e o login/registro ganham a função (`getVerifiedSession`) que o plano 01-04 vai consumir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revalidação no banco em lib/api-auth.ts e reconciliação da suíte completa</name>
  <files>lib/api-auth.ts, lib/api-auth.test.ts, app/api/combos/route.test.ts, app/api/combos/[id]/route.test.ts, app/api/empresas/route.test.ts, app/api/empresas/[id]/route.test.ts, app/api/movimentacoes/route.test.ts, app/api/movimentacoes/[id]/route.test.ts, app/api/produtos/route.test.ts, app/api/produtos/[id]/route.test.ts, app/api/promocoes/route.test.ts, app/api/promocoes/[id]/route.test.ts, app/api/usuarios/route.test.ts, app/api/usuarios/[id]/route.test.ts</files>
  <read_first>
    - `lib/api-auth.ts` (arquivo inteiro, 25 linhas) — `AuthError` (com `status`, default 401) e `requireAuth`, que hoje faz `return await verifyAuthToken(token)` de dentro do `try`
    - `lib/api-auth.test.ts` (arquivo inteiro, 40 linhas) — helper local `buildRequest`, fixture `payload` e os 4 casos existentes
    - `lib/auth-guard.ts` — assinatura de `revalidarConta`
    - `tests/setup/prisma-mock.ts` (já editado na Task 1) — o stub default
    - `app/api/produtos/route.test.ts` — representante do padrão dos 12 arquivos de rota autenticada (uso de `buildAuthToken`/Bearer e expectativa de 200)
    - `app/api/usuarios/[id]/route.ts` (linhas 52-95) — exemplo de como `AuthError` é capturado e convertido em resposta HTTP pelas rotas
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Pattern 3" (trecho de `api-auth.ts`) e § "Pitfall 3" (lista dos arquivos afetados)
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-PATTERNS.md` § "`lib/api-auth.ts` (MODIFIED)"
  </read_first>
  <behavior>
    - Teste (novo): Bearer válido + `prismaMock.usuario.findFirst` resolve `null` → `requireAuth` rejeita com `AuthError` de `status` 401 e mensagem `"Sessão inválida."`.
    - Teste (novo): Bearer válido + `prismaMock.usuario.findFirst` rejeita com erro de banco → `requireAuth` rejeita com `AuthError` 401 (fail-closed, D-01), com a MESMA mensagem do caso anterior (sem distinguir inativo / removido / banco fora do ar — T-01-06).
    - Teste (novo): sem header `authorization` → `prismaMock.usuario.findFirst` NÃO é chamado.
    - Teste (novo): mudança no banco entre dois requests (critério de sucesso #2) — primeira chamada com o stub ativo resolve o payload; troca-se o stub para `null`; a segunda chamada com o MESMO token rejeita com 401.
    - Teste existente ("retorna o payload decodificado quando o Bearer token é válido"): continua passando, agora graças ao stub default do setup.
  </behavior>
  <action>
    Editar `lib/api-auth.ts` importando `revalidarConta` de `@/lib/auth-guard`.

    Mudança estrutural obrigatória em `requireAuth`: o `return await verifyAuthToken(token)` de dentro do `try` vira `payload = await verifyAuthToken(token);`, com `let payload: AuthTokenPayload;` declarado antes do `try`. O `catch` continua lançando `new AuthError("Token inválido ou expirado.")`. Depois do bloco `try/catch`, chamar `const conta = await revalidarConta(payload.sub, payload.empresaId);` e, se `conta` for falsy, lançar `new AuthError("Sessão inválida.", 401)`. Retornar `payload` (não `conta`) — o tipo de retorno continua `Promise<AuthTokenPayload>`, consumido por todas as rotas `app/api/*`.

    Reutilizar a classe `AuthError` já existente neste arquivo (C-05) — não criar classe nova. A mensagem é única e genérica para conta inativa, empresa removida e erro de banco: não vazar estado da conta na resposta (C-04, ASVS V3/V7, T-01-06). Estilo das mensagens segue as vizinhas: português, curtas, com ponto final.

    Não adicionar `try/catch` novo neste arquivo além do já existente em volta de `verifyAuthToken` — o `catch` do fail-closed vive dentro de `revalidarConta`.

    Estender `lib/api-auth.test.ts` com os casos do bloco `<behavior>`, importando `prismaMock` de `@/tests/setup/prisma-mock` e reaproveitando o `buildRequest` local e a fixture `payload`.

    Depois, rodar `npx vitest run` (suíte completa) e reconciliar o fallout do fail-closed. O esperado é que o stub default da Task 1 mantenha os 12 arquivos de rota autenticada verdes sem nenhuma edição. Se algum deles falhar com 401 onde esperava 200, a correção é adicionar o stub de conta ativa no `beforeEach` daquele arquivo específico (mesmos valores de `testAuthPayload`) — NÃO relaxar o fail-closed, NÃO remover a revalidação, NÃO alterar as asserções de status esperadas. Registrar no SUMMARY quais arquivos precisaram de stub local, se algum.

    Não tocar em `proxy.ts` (D-06): ele continua fazendo apenas a checagem otimista de JWT no Edge, sem Prisma.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` sai com código 0 — suíte completa verde, incluindo os 12 arquivos `app/api/**/route.test.ts` autenticados
    - `npx vitest run lib/api-auth.test.ts` sai com código 0, e o arquivo contém no mínimo 4 blocos `it(` a mais do que antes desta task
    - `grep -c 'from "@/lib/auth-guard"' lib/api-auth.ts` retorna `1`
    - `grep -c '"Sessão inválida."' lib/api-auth.ts` retorna `1`
    - `grep -c "class AuthError" lib/api-auth.ts` retorna `1` (a classe existente foi reutilizada, nenhuma nova foi criada)
    - `grep -vE '^\s*(//|\*|/\*)' lib/api-auth.ts | grep -c "try {"` retorna `1`
    - `grep -c "requireAuth(request: Request): Promise<AuthTokenPayload>" lib/api-auth.ts` retorna `1` (assinatura pública inalterada)
    - `grep -cE "prisma|auth-guard|revalidarConta" proxy.ts` retorna `0` (D-06: o Proxy continua sem acesso ao banco)
    - `git diff --name-only proxy.ts` não retorna nada
    - `npm run lint` sai com código 0
    - `npx tsc --noEmit` não reporta erro novo
  </acceptance_criteria>
  <done>Todas as rotas `app/api/*` passam a exigir uma conta confirmada no banco a cada request, com mensagem única e fail-closed; a suíte completa continua verde e `proxy.ts` permanece intocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Component (cookie `admin_session`) | Cookie httpOnly de 7 dias; entrada não confiável até ser verificada E revalidada no banco |
| cliente externo → Route Handler `app/api/*` (Bearer) | Token de 7 dias apresentado por qualquer cliente HTTP; mesma exigência de revalidação |
| Edge (`proxy.ts`) → Node server | O Proxy é otimista por design (sem Prisma); a autoridade fica nas camadas server |
| aplicação → PostgreSQL | Fonte da verdade sobre `Usuario.ativo` e `Empresa.deletedAt` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | `requireAdminSession` / `requireAuth` — janela de 7 dias do JWT | mitigate | Chamar `revalidarConta` em ambas as camadas antes de autorizar (D-05, Tasks 2 e 3). Testes dedicados de conta revogada em `lib/session.test.ts` e `lib/api-auth.test.ts` |
| T-01-02 | Elevation of Privilege | caminho de erro de banco nas duas camadas | mitigate | `null` de `revalidarConta` cobre erro de infraestrutura; `requireAdminSession` redireciona e `requireAuth` lança `AuthError` 401 (D-01). Sem `try/catch` novo que possa engolir `NEXT_REDIRECT` |
| T-01-03 | Elevation of Privilege | confiar apenas na checagem de borda | mitigate | `proxy.ts` permanece otimista e sem Prisma (D-06); a decisão autoritativa fica nas camadas server. Verificado por asserção de fonte (`proxy.ts` sem `prisma`/`auth-guard` e sem diff) |
| T-01-04 | Elevation of Privilege | token de um tenant autorizando em outro | mitigate | `revalidarConta` filtra por `id` + `empresaId`; `requireAdminSession` compara adicionalmente `conta.empresaSlug !== slug` (D-03) — fecha a janela do slug obsoleto, já que `slug` é mutável via `PATCH /api/empresas/[id]` |
| T-01-06 | Information Disclosure | mensagem de erro de `requireAuth` | mitigate | Mensagem única `"Sessão inválida."` para conta inativa, empresa removida e erro de banco — sem enumeração de estado da conta (C-04, ASVS V7) |
| T-01-08 | Denial of Service | auth como vetor de carga no banco | mitigate | Só consultar o banco após assinatura de JWT válida (`requireAuth`) e, no admin, também após o slug bater (`requireAdminSession`). Testes assertam que `usuario.findFirst` NÃO é chamado nesses caminhos |
| T-01-10 | Denial of Service | Postgres indisponível derruba todas as sessões | accept | Consequência aceita de D-01. Registrar no VERIFICATION da fase (plano 01-05) que um pico de 401/redirects pode significar banco fora do ar, não sessões revogadas |
| T-01-13 | Elevation of Privilege | `cookies().delete()` inválido em Server Component mascarando a falha | mitigate | `destroySession()` proibido dentro de `requireAdminSession` (§ Pitfall 2); a limpeza de cookie só na Server Action `logout`. Verificado por asserção de fonte |
| T-01-SC | Tampering | instalações npm/pip/cargo | accept | Zero instalações neste plano — RESEARCH § Package Legitimacy Audit. Qualquer proposta de `npm install` exige checkpoint humano bloqueante |
</threat_model>

<verification>
1. `npx vitest run` — suíte completa verde (é aqui que o § Pitfall 3 apareceria)
2. `npx vitest run lib/session.test.ts lib/api-auth.test.ts lib/auth-guard.test.ts lib/prisma.test.ts` — todos verdes
3. `npm run lint` — código 0
4. `npx tsc --noEmit` — sem erro novo
5. `git diff --name-only` não inclui `proxy.ts` nem `prisma/schema.prisma`
6. Asserções de fonte: nenhum `try {` novo em `lib/session.ts` / `lib/api-auth.ts`; `destroySession` não é chamada de `requireAdminSession`
</verification>

<success_criteria>
- INFRA-02 (infraestrutura) atendido nas duas camadas: `requireAdminSession` redireciona e `requireAuth` devolve 401 quando a conta foi revogada ou o banco falhou
- Critério de sucesso #2 comprovado por teste: uma mudança no banco vale no request seguinte, sem novo login
- Critério de sucesso #4 preservado: `npx vitest run` continua verde nos 12 arquivos de rota autenticada
- `proxy.ts` e `prisma/schema.prisma` intocados
- Assinaturas públicas de `requireAdminSession` e `requireAuth` inalteradas (`Promise<AuthTokenPayload>`)
</success_criteria>

<output>
Create `.planning/phases/01-pr-requisitos-de-produ-o/01-03-SUMMARY.md` when done.

Registrar no SUMMARY:
- Se algum dos 12 arquivos de rota autenticada precisou de stub local (e quais), ou se o stub global bastou
- Se o `vi.clearAllMocks()` de `lib/session.test.ts` preservou o stub global, ou se foi preciso re-aplicá-lo no arquivo
- Se o stub global interferiu em `lib/prisma.test.ts` sob `vi.unmock` e como foi resolvido
- Nota operacional (T-01-10 / § Pitfall 7): a partir deste ponto, um pico de 401/redirects em produção pode significar Postgres indisponível, não sessões revogadas
</output>
