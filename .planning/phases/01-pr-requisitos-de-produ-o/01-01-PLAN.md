---
phase: 01-pr-requisitos-de-produ-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/prisma.ts
  - lib/prisma.test.ts
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "Reavaliar o módulo lib/prisma.ts sob NODE_ENV=production devolve a MESMA instância de PrismaClient"
    - "O client é publicado em globalThis.prisma em produção, não apenas em desenvolvimento"
    - "process.env.DATABASE_URL deixa de ser lido em tempo de import do módulo"
    - "CONCERNS.md descreve o mecanismo real do bug (camadas de bundle do Next), não o mecanismo falso ('todo import cria um client')"
  artifacts:
    - path: "lib/prisma.ts"
      provides: "Singleton de PrismaClient válido em todos os ambientes (INFRA-01)"
      contains: "createPrismaClient"
      min_lines: 15
    - path: "lib/prisma.test.ts"
      provides: "Gate automatizado do singleton (falha no código atual, passa após o fix)"
      contains: "vi.unmock"
      min_lines: 25
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Descrição corrigida do mecanismo do bug do singleton"
      contains: "camadas"
  key_links:
    - from: "lib/prisma.ts"
      to: "globalThis"
      via: "atribuição incondicional do client ao global"
      pattern: "globalForPrisma\\.prisma = prisma"
    - from: "lib/prisma.test.ts"
      to: "lib/prisma.ts"
      via: "import dinâmico após vi.resetModules() sob NODE_ENV=production"
      pattern: "await import\\(\"@/lib/prisma\"\\)"
---

<objective>
Corrigir o bug do singleton do Prisma (INFRA-01): hoje `lib/prisma.ts` só publica o client em `globalThis` quando `NODE_ENV !== "production"`, então em produção cada camada de bundle do servidor (`rsc`, `ssr`, `api-node`, `action-browser`, `instrument`) instancia o seu próprio `PrismaClient` — cada um com o seu próprio `pg.Pool` (`max` default = 10). Sob carga, e principalmente quando o worker diário (Fase 5) e os webhooks (Fase 3) entrarem, isso vira exaustão do pool do Postgres.

Purpose: nenhuma linha de código de billing pode entrar em produção sobre uma base que multiplica pools de conexão. Este é o pré-requisito citado em `.planning/research/STACK.md` para qualquer decisão de hosting.
Output: `lib/prisma.ts` corrigido, `lib/prisma.test.ts` novo (gate automatizado, provado nesta pesquisa como RED hoje / GREEN depois), e `.planning/codebase/CONCERNS.md` com o mecanismo real descrito.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-CONTEXT.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-PATTERNS.md
@.planning/phases/01-pr-requisitos-de-produ-o/01-VALIDATION.md
</context>

## Artifacts this phase produces

Símbolos e arquivos criados pela **Fase 1** como um todo (nenhum destes existe hoje — não sinalizar como drift):

| Artefato | Tipo | Onde |
|----------|------|------|
| `createPrismaClient()` | função factory (module-local) | `lib/prisma.ts` |
| `lib/prisma.test.ts` | novo arquivo de teste | raiz de `lib/` |
| `lib/auth-guard.ts` | novo arquivo (DAL) | raiz de `lib/` |
| `revalidarConta(usuarioId, empresaId)` | const exportada (async, embrulhada em `React.cache`) | `lib/auth-guard.ts` |
| `ContaAtiva` | interface exportada (`usuarioId`, `empresaId`, `empresaSlug`, `email`, `role`) | `lib/auth-guard.ts` |
| `lib/auth-guard.test.ts` | novo arquivo de teste | raiz de `lib/` |
| `getVerifiedSession()` | função async exportada | `lib/session.ts` |
| stub default de conta ativa no `beforeEach` | fixture de teste | `tests/setup/prisma-mock.ts` |
| `e2e/sessao-revogada.spec.ts` | novo spec Playwright | `e2e/` |

Não são produzidos nesta fase: nenhuma dependência npm nova, nenhuma variável de ambiente nova, nenhuma alteração em `prisma/schema.prisma`, nenhuma migration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provar e corrigir o singleton do Prisma (INFRA-01)</name>
  <files>lib/prisma.test.ts, lib/prisma.ts</files>
  <read_first>
    - `lib/prisma.ts` — o arquivo a corrigir (20 linhas; estado atual: `new PrismaPg(...)` em escopo de módulo, `export const prisma = globalForPrisma.prisma ?? new PrismaClient({ adapter })`, e a guarda `if (process.env.NODE_ENV !== "production")` na atribuição ao global)
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Pattern 1: Singleton do Prisma preso ao `globalThis`" — alvo exato, e § "Code Examples > Teste do singleton" — corpo do teste já validado por execução real
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-PATTERNS.md` § "`lib/prisma.ts` (MODIFIED)" e § "`lib/prisma.test.ts` (NEW)" — analogia de estrutura de header de teste
    - `lib/session.test.ts` (linhas 1-27) — analog do header de teste: primeira linha `// @vitest-environment node`, mocks de módulo declarados acima do import da unidade sob teste
    - `tests/setup/prisma-mock.ts` — o mock global de `@/lib/prisma` que este teste precisa desfazer com `vi.unmock`
    - `tests/setup/vitest.setup.ts` — confirma que `DATABASE_URL` e `JWT_SECRET` já são definidos em `beforeAll`
  </read_first>
  <behavior>
    - Teste 1: sob `NODE_ENV=production`, importar `@/lib/prisma`, rodar `vi.resetModules()` e importar de novo devolve a mesma referência (`segundo.prisma` toBe `primeiro.prisma`).
    - Teste 2: sob `NODE_ENV=production`, após importar `@/lib/prisma`, `globalThis.prisma` está definido.
    - Ambos os testes DEVEM falhar com o código atual (a pesquisa mediu `sameInstance: false`, `globalSet: false`) e passar após o fix.
  </behavior>
  <action>
    Criar `lib/prisma.test.ts` PRIMEIRO e rodá-lo contra o código atual para confirmar o RED, depois aplicar o fix em `lib/prisma.ts` e rodar de novo para o GREEN. Não inverter a ordem — a pesquisa (§ Pitfall 4) alerta que sem o RED observado alguém pode concluir que o bug não existe e reverter o fix.

    `lib/prisma.test.ts` — primeira linha `// @vitest-environment node`; importar `afterEach, describe, expect, it, vi` de `vitest`; chamar `vi.unmock("@/lib/prisma")` no topo do arquivo para desfazer o mock global instalado por `tests/setup/prisma-mock.ts`. Um `describe("prisma singleton", ...)` com `afterEach` que faz `delete (globalThis as { prisma?: unknown }).prisma`, `vi.unstubAllEnvs()` e `vi.resetModules()`. Cada `it` começa por `delete (globalThis as { prisma?: unknown }).prisma`, `vi.resetModules()` e `vi.stubEnv("NODE_ENV", "production")` — o `delete` no início de cada caso é obrigatório, senão o segundo teste herda o global do primeiro. Usar `await import("@/lib/prisma")` (import dinâmico), nunca import estático. NÃO fazer asserção sobre `constructor.name` (o build do Prisma é minificado e devolve `"t"`). O corpo completo e já validado está em RESEARCH § "Code Examples > Teste do singleton".

    `lib/prisma.ts` — três mudanças, todas descritas em RESEARCH § Pattern 1:
    1. Extrair a construção para uma factory `function createPrismaClient()` que instancia `new PrismaPg({ connectionString: process.env.DATABASE_URL! })` **dentro** dela e devolve `new PrismaClient({ adapter })`. Isso tira a leitura de `process.env.DATABASE_URL` do tempo de import.
    2. Manter `const globalForPrisma = globalThis as unknown as { prisma: PrismaClient | undefined };` e `export const prisma = globalForPrisma.prisma ?? createPrismaClient();` — o cast e o `??` continuam idênticos.
    3. Remover a guarda `if (process.env.NODE_ENV !== "production")` e tornar a atribuição incondicional: `globalForPrisma.prisma = prisma;`. Esta é a correção do INFRA-01.

    Deixar um comentário curto acima da atribuição explicando por que não há guarda de `NODE_ENV`: em Next.js o cache de módulos do Node não basta porque camadas de bundle distintas (`rsc`, `ssr`, `api-node`, `action-browser`, `instrument`) instanciam o módulo separadamente; `globalThis` é o único escopo compartilhado entre elas.

    NÃO tocar em `prisma/schema.prisma`, NÃO trocar o generator `prisma-client-js` por `prisma-client`, NÃO ajustar `max` do pool — tudo fora de escopo (RESEARCH § Anti-Patterns).
  </action>
  <verify>
    <automated>npx vitest run lib/prisma.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/prisma.test.ts` sai com código 0 e reporta 2 testes passando
    - `git stash` do fix em `lib/prisma.ts` faz o mesmo comando falhar (RED confirmado antes do commit; restaurar em seguida) — ou, equivalentemente, o RED foi observado e registrado no SUMMARY antes da edição de `lib/prisma.ts`
    - `grep -c 'NODE_ENV' lib/prisma.ts` retorna `0`
    - `grep -c 'function createPrismaClient' lib/prisma.ts` retorna `1`
    - `grep -vE '^\s*(//|\*|/\*)' lib/prisma.ts | grep -c 'globalForPrisma.prisma = prisma'` retorna `1`
    - `grep -c 'DATABASE_URL' lib/prisma.ts` retorna `1` e a ocorrência está dentro do corpo de `createPrismaClient`
    - `npx tsc --noEmit` não reporta erro novo em `lib/prisma.ts` nem em `lib/prisma.test.ts`
    - `npm run lint` sai com código 0
  </acceptance_criteria>
  <done>O gate automatizado do singleton existe, falhava no código anterior e passa no código novo; `lib/prisma.ts` publica o client em `globalThis` em todos os ambientes.</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir a descrição do mecanismo do bug em CONCERNS.md</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` § "Prisma Client Singleton Misconfiguration (Production Bug)" (a partir da linha 7) — texto atual, incluindo o bloco "Impact" que afirma "every module that imports `lib/prisma.ts` creates a NEW `PrismaClient` instance" e o bloco "Fix approach"
    - `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Pitfall 4: assumir que o mecanismo descrito no CONCERNS.md é literal" — mecanismo correto e a referência verificada `node_modules/next/dist/lib/constants.js:331-372`
    - `lib/prisma.ts` (já corrigido na Task 1) — o "Fix approach" documentado deve bater com o código real
  </read_first>
  <action>
    Reescrever apenas a seção "Prisma Client Singleton Misconfiguration (Production Bug)" de `.planning/codebase/CONCERNS.md`, preservando o título, a severidade e a listagem de arquivo (`lib/prisma.ts`).

    Substituir a afirmação "every module that imports `lib/prisma.ts` creates a NEW `PrismaClient` instance" pelo mecanismo real: o cache de módulos do Node impede duplicação **dentro de uma mesma camada de bundle**, mas o Next.js 16 compila o servidor em camadas separadas (`rsc`, `ssr`, `api-node`, `api-edge`, `action-browser`, `instrument`, `middleware`, `shared` — verificado em `node_modules/next/dist/lib/constants.js:331-372`) e módulos em camadas diferentes são instâncias distintas em runtime. Como `lib/prisma.ts` é importado tanto de Server Components/Actions quanto de todas as rotas `app/api/*`, em produção o processo abre pools redundantes. Somar os dois multiplicadores que agravam: serverless (cada lambda é um processo novo) e os pontos de entrada extras que chegam nas Fases 3 e 5 (webhooks e worker diário).

    Manter a conclusão e a prioridade atuais (é bug de produção, precisa ser corrigido antes de billing). Atualizar o bloco "Fix approach" para refletir o que foi de fato implementado na Task 1: factory `createPrismaClient()` + atribuição incondicional a `globalThis`, sem guarda de `NODE_ENV`. Marcar a seção como resolvida referenciando a fase (`Resolvido na Fase 1 — plano 01-01`).

    Não alterar nenhuma outra seção do arquivo.
  </action>
  <verify>
    <automated>grep -c "camadas" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "every module that imports" .planning/codebase/CONCERNS.md` retorna `0`
    - `grep -c "constants.js" .planning/codebase/CONCERNS.md` é `>= 1` (a referência verificável do mecanismo real está citada)
    - `grep -c "createPrismaClient" .planning/codebase/CONCERNS.md` é `>= 1` (o "Fix approach" bate com o código de `lib/prisma.ts`)
    - `grep -c "Fase 1" .planning/codebase/CONCERNS.md` é `>= 1`
    - `git diff --stat .planning/codebase/CONCERNS.md` mostra alteração apenas neste arquivo, e as demais seções do arquivo permanecem byte-idênticas (`git diff` não toca em outros cabeçalhos `###`)
  </acceptance_criteria>
  <done>Um revisor futuro lendo CONCERNS.md encontra o mecanismo verificável do bug e não tem base para reverter o fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processo Node → PostgreSQL | Cada `PrismaClient` cria um `pg.Pool` próprio (`max` default = 10); o número de pools é o que esta fase controla |
| camadas de bundle do Next (`rsc` / `ssr` / `api-node` / `action-browser` / `instrument`) | Fronteira de instância de módulo em runtime; `globalThis` é o único escopo compartilhado entre elas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | `lib/prisma.ts` → `pg.Pool` → PostgreSQL | mitigate | Publicar o `PrismaClient` em `globalThis` sem guarda de `NODE_ENV` (Task 1) — um pool por processo em vez de um por camada de bundle. Gate automatizado em `lib/prisma.test.ts`; verificação de conexões reais no plano 01-05 |
| T-01-11 | Information Disclosure | `lib/prisma.ts` (leitura de `DATABASE_URL` em tempo de import) | mitigate | Mover `process.env.DATABASE_URL` para dentro de `createPrismaClient()` (Task 1) — a connection string deixa de ser materializada em tempo de import por qualquer camada que apenas importe o módulo |
| T-01-12 | Tampering | Documentação de segurança (`CONCERNS.md`) descrevendo mecanismo falso | mitigate | Corrigir o mecanismo com referência verificável (Task 2) — evita reversão futura do fix por revisor que "desmente" o bug |
| T-01-SC | Tampering | instalações npm/pip/cargo | accept | Esta fase não instala nenhum pacote. `.planning/phases/01-pr-requisitos-de-produ-o/01-RESEARCH.md` § "Package Legitimacy Audit" registra zero instalações e um único pacote considerado e descartado (`server-only`, verdict `[SUS]`). Se qualquer tarefa aqui propuser um `npm install`, ela deve parar e exigir checkpoint humano bloqueante |
</threat_model>

<verification>
1. `npx vitest run lib/prisma.test.ts` — 2 testes verdes
2. `npx vitest run lib/` — nenhuma regressão nos demais testes de `lib/`
3. `npm run lint` — código 0
4. `npx tsc --noEmit` — sem erro novo
5. Inspeção: `lib/prisma.ts` não contém a string `NODE_ENV`
</verification>

<success_criteria>
- INFRA-01 atendido: reavaliar `lib/prisma.ts` sob `NODE_ENV=production` devolve a mesma instância e o client está em `globalThis`
- O gate automatizado existe e é reprodutível (`npx vitest run lib/prisma.test.ts`)
- `.planning/codebase/CONCERNS.md` descreve o mecanismo real e aponta o fix implementado
- Zero dependências novas, zero alterações em `prisma/schema.prisma`
</success_criteria>

<output>
Create `.planning/phases/01-pr-requisitos-de-produ-o/01-01-SUMMARY.md` when done.

Registrar no SUMMARY:
- O resultado do RED observado antes do fix (saída do `vitest` com o código antigo)
- Que o dimensionamento de `max` do pool e a escolha de URL *pooled* vs. direta continuam fora de escopo e são entrada da fase de deploy/hosting (RESEARCH § Open Questions #5)
</output>
