---
phase: 06-termos-de-uso-e-aceite
plan: 08
type: execute
wave: 5
depends_on: ["06-03", "06-05", "06-07"]
files_modified:
  - e2e/aceite-de-termos.spec.ts
  - e2e/helpers.ts
  - scripts/gates-fase-06.mjs
  - package.json
  - .planning/phases/06-termos-de-uso-e-aceite/06-VALIDATION.md
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-04]

must_haves:
  truths:
    - "O ciclo completo do gate — cair na tela, aceitar, voltar ao painel — funciona contra um Postgres real"
    - "Um usuario gateado consegue deslogar da tela de aceite sem entrar em loop"
    - "Nenhum codigo de producao autoriza pela role do JWT"
    - "Nenhum caminho HTTP escreve o campo role"
    - "O service de termos nao expoe nenhuma escrita sobre uma versao publicada"
    - "Nenhum arquivo novo desta fase usa a paleta ambar reservada a linguagem de pagamento"
  artifacts:
    - path: "e2e/aceite-de-termos.spec.ts"
      provides: "TERM-04 fim a fim contra banco real, inclusive logout"
    - path: "scripts/gates-fase-06.mjs"
      provides: "os gates estaticos da fase como codigo executavel"
      min_lines: 150
    - path: "package.json"
      provides: "script npm gates:fase-06"
      contains: "gates:fase-06"
  key_links:
    - from: "scripts/gates-fase-06.mjs"
      to: "app/ e lib/"
      via: "varredura de linhas de codigo (comentarios descartados)"
      pattern: "linhasDeCodigo"
    - from: "e2e/aceite-de-termos.spec.ts"
      to: "e2e/helpers.ts"
      via: "helper de publicacao de termo por script/API"
      pattern: "execFileSync|request"
---

<objective>
Fechamento da fase: o e2e que prova TERM-04 fim a fim contra Postgres real, os gates estaticos que
travam as quatro decisoes invisiveis em code review, e o fechamento de `06-VALIDATION.md` com as
verificacoes manuais que nenhum teste pode cobrir.

Os gates existem porque quatro violacoes desta fase sao linhas curtas e plausiveis que NAO quebram
nenhum teste: trocar `revalidarConta().role` por `auth.role` parece simplificacao; acrescentar um
`prisma.termoDeUso.update` parece conveniencia; deixar `role` entrar num allowlist de DTO parece
completude; usar `bg-amber-50` na tela de aceite parece consistencia com a tela de bloqueio. A Fase 4
resolveu essa classe de problema com `scripts/gates-fase-04.mjs` e a Fase 5 reusou a forma; esta fase
reusa de novo, incluindo a regra de higiene que descarta linhas de comentario antes de contar — sem
ela os JSDoc normativos desta fase, que citam LITERALMENTE os identificadores proibidos para explicar
por que sao proibidos, tornariam os gates vermelhos.

Purpose: sem este plano a fase tem cobertura unitaria mas nenhuma prova de que a mecanica funciona
contra um banco real, e nenhuma defesa contra as regressoes silenciosas.
Output: spec e2e novo, script de gates, entrada em `package.json`, `06-VALIDATION.md` fechado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-termos-de-uso-e-aceite/06-CONTEXT.md
@.planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md
@.planning/phases/06-termos-de-uso-e-aceite/06-PATTERNS.md
@.planning/phases/06-termos-de-uso-e-aceite/06-VALIDATION.md
@.planning/phases/06-termos-de-uso-e-aceite/06-03-SUMMARY.md
@.planning/phases/06-termos-de-uso-e-aceite/06-05-SUMMARY.md
@.planning/phases/06-termos-de-uso-e-aceite/06-07-SUMMARY.md
@scripts/gates-fase-05.mjs
@e2e/helpers.ts
@e2e/bloqueio-por-inadimplencia.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/aceite-de-termos.spec.ts — o ciclo completo do gate contra Postgres real</name>
  <files>e2e/aceite-de-termos.spec.ts, e2e/helpers.ts</files>
  <read_first>
    - e2e/bloqueio-por-inadimplencia.spec.ts (arquivo inteiro: o spec mais proximo — registra pela UI, muda o estado do banco por script, navega e afirma o redirect; e autentica por `POST /api/auth/login` num contexto separado do browser para exercitar a superficie REST)
    - e2e/helpers.ts (linhas 40-58: `seedFatosBilling` com `execFileSync` sincrono e argumentos opcionais no fim do array — o modelo para qualquer helper novo; e `uniqueEmpresa`)
    - e2e/cadastro-e-login.spec.ts (o helper privado de registro, ja atualizado com o checkbox no plano 06-07)
    - app/api/termos/route.ts e scripts/seed-superadmin.ts (as duas formas de publicar uma versao nova)
    - playwright.config.ts (baseURL e configuracao de contexto)
  </read_first>
  <action>
Criar `e2e/aceite-de-termos.spec.ts` cobrindo TERM-04 fim a fim contra o Postgres real.

Estrutura do spec (todos os casos partem de um registro real pela UI, ja com o checkbox marcado, que
grava o aceite da versao vigente):

Caso 1 — GATE E ACEITE: registrar uma empresa nova; publicar uma versao NOVA dos termos (ver a nota de
mecanismo abaixo); navegar para `/{slug}/admin` e afirmar que a URL final e
`/{slug}/admin/aceitar-termos` e que o heading `Termos de Uso` esta visivel; clicar em
`Aceitar e continuar`; afirmar que a URL final e `/{slug}/admin` e que uma tela do painel renderiza.
Navegar uma segunda vez para uma OUTRA page protegida do admin e afirmar que NAO ha redirect — e essa
segunda navegacao que prova que o aceite foi persistido, e nao apenas que o redirect da action
funcionou.

Caso 2 — SAIDA SEM LOOP (criterio de sucesso #4 do roadmap): registrar, publicar versao nova, cair na
tela de aceite, clicar em `Sair da conta`, e afirmar que a URL final e `/{slug}/admin/login` e que o
formulario de login esta visivel. Afirmar tambem que a navegacao terminou (nao houve cadeia de
redirects) — se o Playwright permitir, contar as respostas de redirect da navegacao; caso contrario,
afirmar que a pagina de login renderizou dentro do timeout padrao, e comentar que um loop se
manifestaria como `ERR_TOO_MANY_REDIRECTS` ou timeout.

Caso 3 — SUPERFICIE REST (Achado Critico 2): num contexto separado do browser, autenticar por
`POST /api/auth/login` com as credenciais da empresa registrada, publicar uma versao nova, e afirmar
que uma chamada autenticada a uma rota qualquer atras de `requireAuth` (por exemplo `GET /api/produtos`)
devolve **403**. Depois de aceitar pela UI, afirmar que a mesma chamada volta a devolver 200 —
provando que o gate REST le o estado fresco a cada request, sem novo login. Espelhar a tecnica de
contexto separado ja usada em `bloqueio-por-inadimplencia.spec.ts`.

Caso 4 — PRECEDENCIA DE BLOQUEIO: registrar, publicar versao nova, e usar `seedFatosBilling(slug,
"bloqueado")` para deixar a empresa bloqueada COM termos pendentes; navegar para `/{slug}/admin` e
afirmar que a URL final e `/{slug}/admin/bloqueado`, nao `/aceitar-termos`; navegar diretamente para
`/{slug}/admin/aceitar-termos` e afirmar que ela redireciona de volta para `/bloqueado`. Este e o caso
que provaria um loop se a guarda simetrica estivesse errada — se ele der timeout, o bug e o loop.

MECANISMO DE PUBLICACAO no e2e — decidir e implementar UMA das duas formas, registrando a escolha em
comentario no arquivo:
(a) Um helper novo em `e2e/helpers.ts` no molde exato de `seedFatosBilling` (`execFileSync` sincrono,
argumentos no fim do array), invocando um script que insere uma nova linha em `TermoDeUso`. Se essa
for a escolha e nao existir script adequado, o caminho mais barato e reusar `scripts/seed-superadmin.ts`
para criar o SUPERADMIN e publicar via `request` autenticado do Playwright contra `POST /api/termos`.
(b) Direto pelo `request` do Playwright contra `POST /api/termos`, autenticado com um Bearer token de
SUPERADMIN obtido por `POST /api/auth/login` — o que exige que o SUPERADMIN exista, criado por
`execFileSync` de `scripts/seed-superadmin.ts` num `beforeAll`, no mesmo padrao sincrono de
`seedFatosBilling`.
A opcao (b) tem a vantagem de exercitar TERM-02 de verdade no e2e (SUPERADMIN publica, e o resto da
suite observa o efeito), fechando os dois requisitos com um mecanismo so. Preferir (b) salvo obstaculo
concreto, e registrar o motivo se acabar em (a).

Qualquer alteracao em `e2e/helpers.ts` e ADITIVA: nao tocar em `seedFatosBilling`,
`expectGoneFromCatalogo` nem `uniqueEmpresa`, que 9 specs consomem.
  </action>
  <verify>
    <automated>npx playwright test e2e/aceite-de-termos.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/aceite-de-termos.spec.ts` sai 0 com os 4 casos passando
    - `npm run test:e2e` sai 0 com o total de testes >= 26 (os 22 existentes mais os 4 novos)
    - O Caso 2 afirma que a URL final apos `Sair da conta` e `/{slug}/admin/login`
    - O Caso 3 afirma 403 antes do aceite e 200 depois, na MESMA sessao de Bearer token (sem novo login entre os dois)
    - O Caso 4 afirma que `/{slug}/admin/aceitar-termos` redireciona para `/bloqueado` quando a empresa esta bloqueada
    - `git diff e2e/helpers.ts` contem apenas adicoes (nenhuma das tres funcoes existentes foi alterada)
    - `git diff --stat e2e/` alem de `aceite-de-termos.spec.ts` e `helpers.ts` esta vazio (os 9 specs do plano 06-07 nao sao tocados de novo)
  </acceptance_criteria>
  <done>O ciclo gate-aceite-retorno, a saida por logout, o espelho REST e a precedencia de bloqueio estao provados contra um Postgres real.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/gates-fase-06.mjs — as decisoes invisiveis da fase como codigo executavel</name>
  <files>scripts/gates-fase-06.mjs, package.json</files>
  <read_first>
    - scripts/gates-fase-05.mjs (arquivo inteiro, 440 linhas: o cabecalho normativo das linhas 1-44 com as tres camadas de higiene, as funcoes `linhasDeCodigo`, `ocorrencias`, `arquivosSob`, `ehFonte` e `fontesDaAplicacao`, o formato de `ok`/`falha`, e a politica de codigo de saida — 1 na primeira violacao mas TODOS os gates rodam)
    - scripts/gates-fase-04.mjs (o precedente original da forma)
    - package.json (as linhas de `gates:fase-04` e `gates:fase-05`, cujo formato deve ser replicado)
    - app/services/termo.service.ts, app/api/termos/route.ts, lib/auth-guard.ts, lib/session.ts, lib/api-auth.ts (os arquivos que os gates varrem)
    - .planning/phases/06-termos-de-uso-e-aceite/06-PATTERNS.md secao "### `scripts/gates-fase-06.mjs`"
  </read_first>
  <action>
Criar `scripts/gates-fase-06.mjs` e acrescentar a `package.json` a entrada
`"gates:fase-06": "node scripts/gates-fase-06.mjs"`, no formato exato das duas irmas. Nenhuma outra
alteracao em `package.json`.

Copiar de `gates-fase-05.mjs` a infraestrutura inteira — `RAIZ`, `ok`, `falha`, `linhasDeCodigo`,
`ocorrencias`, `arquivosSob`, `ehFonte`, `fontesDaAplicacao` — e as TRES camadas de higiene, que sao
obrigatorias e nao opcionais:
1. Antes de contar ocorrencias, DESCARTAR linhas de comentario (as que, apos trim, comecam com `//`,
   `*` ou `/*`). Sem isso os gates se auto-invalidam: os JSDoc normativos desta fase citam
   LITERALMENTE `auth.role ===` e `session.role ===` para explicar por que sao proibidos. Um gate que
   conta comentarios ficaria vermelho justamente porque o codigo documenta a proibicao corretamente
   (decisao `[04-09]`).
2. Agulhas montadas por CONCATENACAO de string, nunca escritas inteiras, para que ampliar o escopo de
   um gate no futuro nao transforme este arquivo na primeira violacao que ele mesmo reporta.
3. `ehFonte` descarta `*.test.ts`/`*.test.tsx` — os testes desta fase citam os identificadores
   proibidos DE PROPOSITO (o caso de nao-vacuidade de `app/api/termos/route.test.ts` existe justamente
   para provar que o codigo NAO usa a role do token). Um gate que varresse os testes acusaria como
   violacao exatamente a prova de que a invariante e respeitada.

Cinco gates:

GATE 1 — Autorizacao nunca pela role do JWT (Pitfall 2, T-06-01). Varrer `fontesDaAplicacao()` (todo
`app/` e `lib/`, so fontes) procurando as agulhas `"auth" + ".role ==="`, `"session" + ".role ==="`,
`"payload" + ".role ==="`. Zero ocorrencias esperadas. Mensagem de falha explicando: o payload do JWT
tem ate 7 dias; a role tem que vir de `revalidarConta`, que le o banco no request atual; um SUPERADMIN
rebaixado continuaria publicando termos por uma semana inteira.

GATE 2 — `TermoDeUso` e imutavel (TERM-03 / D-07, T-06-17). Varrer `fontesDaAplicacao()` procurando
`"termoDeUso" + ".update"`, `"termoDeUso" + ".delete"`, `"termoDeUso" + ".updateMany"`,
`"termoDeUso" + ".deleteMany"`, `"termoDeUso" + ".upsert"`. Zero ocorrencias esperadas. Mensagem de
falha: publicar e INSERT, sempre; a ausencia destes metodos e o contrato de TERM-03, e a FK
`onDelete: Restrict` e a segunda camada da mesma defesa.

GATE 3 — `role` nao e gravavel por HTTP (T-06-02). Varrer os arquivos-fonte de `app/api/` e
`app/services/` procurando escritas de `role` em `data` de mutacao. Como um grep cru de `role:` daria
falso positivo em toda projecao de `select`, restringir a busca as agulhas `"role" + ": data.role"`,
`"role" + ": body.role"`, `"role" + ": input.role"` e `"role" + ": dto.role"`, e ADICIONALMENTE
afirmar por leitura de arquivo que nem `CreateUsuarioDTO` nem `UpdateUsuarioDTO` em
`app/services/usuario.service.ts` declaram um campo `role`. Escrever a limitacao do gate no proprio
comentario, com honestidade: ele nao e um analisador de fluxo, e um conjunto de agulhas conhecidas
mais uma checagem de forma dos DTOs; um caminho novo e criativo escaparia. Exceao permitida:
`scripts/seed-superadmin.ts` NAO e varrido (nao esta em `app/` nem em `lib/`), e e o unico lugar
legitimo que escreve `role`.

GATE 4 — Nenhuma paleta ambar nos arquivos novos da fase (UI-SPEC, regra grep-checavel). Varrer
`app/[slug]/admin/aceitar-termos/` (recursivo, so fontes) e `app/registro/_components/register-form.tsx`
procurando a agulha `"amb" + "er-"`. Zero ocorrencias esperadas. Mensagem de falha explicando: a Fase 4
gastou seu orcamento de cor ensinando uma associacao — ambar significa "sua situacao de pagamento" —
e o gate de termos e outro evento, com outro remédio; nada esta vencido, nada esta suspenso, e pagar
nao resolve (que e exatamente por que o espelho REST devolve 403 e nao 402). Um usuario que esteja
TAMBEM em carencia veria duas telas ambar identicas na mesma sessao significando coisas diferentes.
IMPORTANTE: `app/registro/page.tsx` NAO entra no escopo deste gate — o gradiente `#f59e0b` da linha 21
e pre-existente e nao e utilitario Tailwind; varre-lo produziria um falso positivo permanente.
Registrar essa exclusao em comentario.

GATE 5 — Nenhum pacote instalado na fase (T-06-SC). Copiar a forma do Gate 6 de `gates-fase-05.mjs`,
com `DEPENDENCIAS_ESPERADAS = 11` e `DEV_DEPENDENCIAS_ESPERADAS = 20` (numeros MEDIDOS no inicio desta
fase, herdados inalterados da Fase 5). Reproduzir a mensagem sobre slopsquatting: um install que
aparece no meio de uma fase que declarou nao instalar nada e o sinal exato — pare, verifique a
legitimidade do pacote no registro, e so entao atualize o numero junto com o registro da verificacao.
A `## Package Legitimacy Audit` de `06-RESEARCH.md` esta VAZIA (nenhum `[ASSUMED]`, nenhum `[SUS]`,
nenhum `[SLOP]`), entao o gate de legitimidade e vacuamente satisfeito e nenhum checkpoint humano de
verificacao de pacote e disparado nesta fase.

Politica de saida identica a das irmas: `process.exitCode = 1` na primeira violacao, mas TODOS os
gates rodam — quem acabou de quebrar o build quer ver a lista inteira de uma vez. Sem dependencia
externa: so `node:fs` e `node:path`.

Cabecalho normativo do arquivo no formato de `gates-fase-05.mjs`: POR QUE ISTO EXISTE (as quatro
violacoes plausiveis que nao quebram teste nenhum), REGRA TRANSVERSAL DE HIGIENE, SEGUNDA CAMADA
(concatenacao), TERCEIRA CAMADA (fontes apenas), CODIGO DE SAIDA, e ESCOPO.
  </action>
  <verify>
    <automated>npm run gates:fase-06</automated>
  </verify>
  <acceptance_criteria>
    - `npm run gates:fase-06` sai com codigo 0 e imprime `5/5 gates OK`
    - `node -e "const p=require('./package.json');process.exit(p.scripts['gates:fase-06']?0:1)"` sai 0
    - Prova de NAO-VACUIDADE do Gate 1: introduzir temporariamente `if (auth.role === "X") {}` numa linha de codigo de `app/api/termos/route.ts`, rodar `npm run gates:fase-06`, confirmar saida 1 apontando o arquivo e a linha, e reverter
    - Prova de NAO-VACUIDADE do Gate 2: introduzir temporariamente `prisma.termoDeUso.update({})` numa linha de codigo de `app/services/termo.service.ts`, confirmar saida 1, e reverter
    - Prova de NAO-VACUIDADE do Gate 4: introduzir temporariamente `bg-amber-50` em `aceite-card.tsx`, confirmar saida 1, e reverter
    - `npm run gates:fase-04` e `npm run gates:fase-05` continuam saindo 0 (as fases anteriores nao regrediram)
    - `git diff package.json` mostra exatamente UMA linha adicionada nesta task
    - Nenhuma agulha proibida aparece escrita inteira no arquivo (todas montadas por concatenacao)
  </acceptance_criteria>
  <done>As quatro decisoes invisiveis em code review viraram build vermelho, e a nao-vacuidade de tres dos gates foi demonstrada por reversao temporaria.</done>
</task>

<task type="auto">
  <name>Task 3: Suite completa, fechamento de 06-VALIDATION.md e as verificacoes manuais da fase</name>
  <files>.planning/phases/06-termos-de-uso-e-aceite/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-termos-de-uso-e-aceite/06-VALIDATION.md (arquivo inteiro: o frontmatter com `nyquist_compliant: false` e `wave_0_complete: false`, a tabela Per-Task Verification Map com `Task ID` e `Plan` marcados como TBD, e a secao Manual-Only Verifications)
    - Os SUMMARYs dos planos 06-01 a 06-07 (para preencher os IDs reais de plano/task na tabela)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "## Runtime State Inventory" (o item de dados armazenados e a ordem de deploy) e "## Assumptions Log" (A1, A2, A4)
  </read_first>
  <action>
Rodar a bateria completa e registrar os numeros reais: `npm run test`, `npm run lint`,
`npx tsc --noEmit`, `npm run test:e2e`, `npm run gates:fase-04`, `npm run gates:fase-05`,
`npm run gates:fase-06`. Todos precisam sair 0. Transcrever as CONTAGENS reais (numero de testes
Vitest, numero de testes Playwright, numero de gates) — nunca inventar numeros; se um comando nao
puder rodar (por exemplo, e2e sem Postgres disponivel), registrar isso explicitamente como pendencia
em vez de reportar verde.

Atualizar `.planning/phases/06-termos-de-uso-e-aceite/06-VALIDATION.md`:
- Frontmatter: `wave_0_complete: true` e `nyquist_compliant: true` — este ultimo SO se todas as linhas
  da tabela tiverem um comando automatizado verde ou uma justificativa de manual explicita.
- Preencher as colunas `Task ID` e `Plan` da tabela Per-Task Verification Map com os identificadores
  reais (`06-01` a `06-08`) e marcar o `Status` de cada linha com o resultado observado.
- Marcar os itens de Wave 0 Requirements como concluidos, apontando o plano que os fechou.
- Marcar os itens de Validation Sign-Off.

Acrescentar as tres verificacoes manuais da fase como human-check na secao Manual-Only
Verifications, com instrucoes acionaveis (o modo de verificacao humana deste projeto e end-of-phase,
entao estas sao colhidas no UAT de fim de fase, nao como checkpoint bloqueante no meio da execucao):

1. TEXTO JURIDICO REAL (A4 do RESEARCH, TERM-01/TERM-03). A v1 semeada pela migration do plano 06-01
   carrega um placeholder explicitamente marcado. O texto real e conteudo juridico, nao codigo, e
   nenhum teste pode verifica-lo. Instrucao: antes do deploy em producao, o operador publica o texto
   legal real como uma NOVA versao via `POST /api/termos` autenticado como SUPERADMIN (publicar e
   INSERT, sempre — D-07; a v1 nao e editada), confirma que a tela de aceite mostra o texto correto, e
   registra a confirmacao. Consequencia de pular: todos os clientes terao aceitado um placeholder.

2. COMUNICACAO AOS CLIENTES EXISTENTES (Open Question 3 do RESEARCH, TERM-04). Apos a migration,
   100% dos usuarios existentes terao `termoAceitoId = NULL` e verao a tela de aceite no proximo
   request autenticado. Isso e o comportamento DESEJADO (e o goal da fase), mas e uma mudanca visivel
   e nao anunciada para todo cliente pagante. Instrucao: o operador confirma que esta ciente e decide
   se havera aviso previo. Nao ha acao de codigo associada — a decisao e de produto.

3. ORDEM DE DEPLOY (Runtime State Inventory). Instrucao: aplicar as migrations na ordem
   (enum, tabelas, seed da v1), depois fazer o deploy da aplicacao, e por fim rodar
   `npm run seed:superadmin` em producao. Rodar o seed antes do deploy e inofensivo; rodar a migration
   de enum DEPOIS do codigo que usa `SUPERADMIN` produz erro de runtime no Prisma. Registrar a ordem
   executada.

Registrar tambem, na secao de assuncoes ou numa nota nova do arquivo, os tres itens que a fase deixa
conscientemente em aberto, para que o verificador nao os trate como lacunas: A1 (o aceite nao captura
IP/user-agent; se a assessoria juridica exigir, e migration nova mais captura de dado pessoal
adicional), A2 (aceites desaparecem com o `Usuario` por cascade herdado de `Empresa`; se houver dever
de retencao da prova apos encerramento, isso muda), e o hardening pendente de `POST /api/usuarios`,
que segue sem `requireAuth` (pre-existente, fora do escopo desta fase, incapaz de criar SUPERADMIN mas
capaz de criar ADMIN em empresa sem usuario).
  </action>
  <verify>
    <automated>npm run test && npm run lint && npx tsc --noEmit && npm run gates:fase-04 && npm run gates:fase-05 && npm run gates:fase-06 && npm run test:e2e</automated>
    <human-check>Texto juridico real publicado como nova versao via POST /api/termos antes do deploy em producao (nao o placeholder da migration v1) — confirmar na tela de aceite</human-check>
    <human-check>Operador ciente de que 100% dos usuarios existentes verao a tela de aceite no proximo login apos o deploy, e decidiu se havera comunicacao previa</human-check>
    <human-check>Ordem de deploy respeitada: migrations (enum, tabelas, seed v1) -> deploy da aplicacao -> npm run seed:superadmin em producao</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run test` sai 0 e o numero de testes e >= 876 (baseline da Fase 4) mais os casos novos desta fase
    - `npm run test:e2e` sai 0 e o numero de testes e >= 26
    - `npm run gates:fase-04`, `npm run gates:fase-05` e `npm run gates:fase-06` saem 0
    - `npm run lint` sai 0 e `npx tsc --noEmit` sai 0
    - `06-VALIDATION.md` tem `wave_0_complete: true` no frontmatter
    - `06-VALIDATION.md` nao contem mais nenhuma celula `TBD` na coluna `Plan` da tabela Per-Task Verification Map
    - Os tres human-check estao registrados na secao Manual-Only Verifications com instrucoes acionaveis
    - As contagens registradas no arquivo sao as observadas na execucao, nao valores herdados do template
    - `git diff package.json` no plano inteiro contem exatamente UMA linha adicionada (`gates:fase-06`)
  </acceptance_criteria>
  <done>A fase tem cobertura automatizada verde de ponta a ponta, os gates estaticos travam as regressoes silenciosas, e as tres verificacoes que so um humano pode fazer estao registradas com instrucao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desenvolvedor futuro -> codigo de producao | Os gates estaticos sao o boundary contra regressoes que nenhum teste pega |
| e2e -> Postgres real | O unico ponto da fase onde a mecanica e exercitada contra um banco de verdade |
| operador -> producao | A ordem de deploy e o texto juridico so existem fora do repositorio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | qualquer fonte em `app/` ou `lib/` | mitigate | Gate 1 varre linhas de codigo (comentarios descartados) por `auth.role ===` / `session.role ===` / `payload.role ===`; nao-vacuidade demonstrada por reversao temporaria |
| T-06-17 | Repudiation | escrita sobre versao publicada | mitigate | Gate 2 varre por `termoDeUso.update/delete/updateMany/deleteMany/upsert`; complementa a FK `onDelete: Restrict` e o teste de ausencia de metodo do plano 06-02 |
| T-06-02 | Elevation of Privilege | escrita de `role` por HTTP | mitigate | Gate 3 (agulhas conhecidas + checagem de forma dos DTOs de `usuario.service.ts`). Limitacao documentada no proprio gate: nao e analise de fluxo; `scripts/seed-superadmin.ts` e a unica escrita legitima e fica fora do escopo varrido |
| T-06-37 | Tampering | ambar reusado no gate de termos | mitigate | Gate 4 varre os arquivos novos da fase por `amber-`; exclui `app/registro/page.tsx`, cujo `#f59e0b` e gradiente pre-existente e nao utilitario Tailwind |
| T-06-05 | Elevation of Privilege | espelho REST do gate | mitigate | Caso 3 do e2e prova 403 antes do aceite e 200 depois, na mesma sessao de Bearer token — a prova de que o gate le estado fresco a cada request |
| T-06-26 | Denial of Service | loop de redirect em producao | mitigate | Caso 4 do e2e exercita o estado (bloqueado, pendente) contra banco real; um loop se manifestaria como timeout ou `ERR_TOO_MANY_REDIRECTS` |
| T-06-30 | Denial of Service | usuario preso na tela de aceite | mitigate | Caso 2 do e2e prova a saida por `Sair da conta` ate o formulario de login renderizar |
| T-06-38 | Repudiation | placeholder juridico chegando a producao | mitigate | human-check obrigatorio: o texto real e publicado como NOVA versao via endpoint antes do deploy; a v1 nunca e editada (D-07) |
| T-06-39 | Denial of Service | ordem de deploy invertida | mitigate | human-check da ordem migrations -> deploy -> seed; rodar a migration de enum depois do codigo que usa `SUPERADMIN` produz erro de runtime no Prisma |
| T-06-03 | Elevation of Privilege | `POST /api/usuarios` sem `requireAuth` | accept | Pre-existente e fora do escopo. Registrado em `06-VALIDATION.md` como hardening pendente para que o verificador nao o trate como lacuna desta fase |
| T-06-40 | Repudiation | retencao da prova de consentimento (A1/A2) | accept | Nem IP/user-agent capturados, nem sobrevivencia do aceite ao encerramento da conta. Confianca LOW no RESEARCH (sem fonte juridica); registrado em `06-VALIDATION.md` como assuncao consciente, barata agora e cara depois |
| T-06-SC | Tampering | npm installs | mitigate | Gate 5 conta 11 deps / 20 devDeps com a mensagem sobre slopsquatting. A Package Legitimacy Audit do RESEARCH esta vazia: nenhum pacote `[ASSUMED]`, `[SUS]` ou `[SLOP]`, portanto nenhum checkpoint de legitimidade e disparado nesta fase |
</threat_model>

## Artifacts this phase produces

Simbolos e caminhos CRIADOS por este plano:

- Arquivos novos: `e2e/aceite-de-termos.spec.ts`, `scripts/gates-fase-06.mjs`
- Script npm: `gates:fase-06` em `package.json`
- Possivel helper novo em `e2e/helpers.ts` para publicar uma versao de termos no e2e (nome a definir na Task 1; aditivo, sem tocar `seedFatosBilling`, `expectGoneFromCatalogo` nem `uniqueEmpresa`)
- Constantes de gate em `scripts/gates-fase-06.mjs`: `DEPENDENCIAS_ESPERADAS`, `DEV_DEPENDENCIAS_ESPERADAS`, e as listas de agulhas concatenadas
- Frontmatter atualizado em `06-VALIDATION.md`: `wave_0_complete: true`, `nyquist_compliant: true`

<verification>
- `npm run test` verde, com contagem transcrita
- `npm run test:e2e` verde, com contagem transcrita (>= 26)
- `npm run gates:fase-04`, `gates:fase-05` e `gates:fase-06` verdes
- `npx tsc --noEmit` sai 0; `npm run lint` sai 0
- Nao-vacuidade dos Gates 1, 2 e 4 demonstrada por reversao temporaria e registrada no SUMMARY
- `git diff package.json` no plano contem exatamente uma linha adicionada
- `06-VALIDATION.md` sem celulas TBD e com os tres human-check registrados
</verification>

<success_criteria>
- Um desenvolvedor que troque `revalidarConta().role` por `auth.role` recebe build vermelho de `npm run gates:fase-06`, com arquivo e linha
- Um desenvolvedor que acrescente `prisma.termoDeUso.update` recebe build vermelho
- O ciclo completo (cair no gate, aceitar, voltar ao painel, e alternativamente sair da conta) esta provado contra Postgres real
- A fase nao pode ser declarada concluida sem as tres confirmacoes humanas registradas em `06-VALIDATION.md`
</success_criteria>

<output>
Create `.planning/phases/06-termos-de-uso-e-aceite/06-08-SUMMARY.md` when done
</output>
