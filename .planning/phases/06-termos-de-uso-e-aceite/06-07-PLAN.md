---
phase: 06-termos-de-uso-e-aceite
plan: 07
type: execute
wave: 4
depends_on: ["06-06"]
files_modified:
  - app/registro/_components/register-form.tsx
  - app/registro/_components/register-form.test.tsx
  - app/registro/page.tsx
  - e2e/cadastro-e-login.spec.ts
  - e2e/bloqueio-por-inadimplencia.spec.ts
  - e2e/worker-reconciliacao.spec.ts
  - e2e/movimentacao-estoque.spec.ts
  - e2e/combo-crud.spec.ts
  - e2e/promocao-crud.spec.ts
  - e2e/modo-simples.spec.ts
  - e2e/produto-crud-e-catalogo.spec.ts
  - e2e/sessao-revogada.spec.ts
autonomous: true
requirements: [TERM-01]

must_haves:
  truths:
    - "Quem se cadastra ve o texto completo dos termos sem sair da tela e marca um aceite obrigatorio"
    - "O formulario nao submete com o checkbox desmarcado"
    - "Um banco sem termos publicados mostra uma tela honesta em vez de um formulario que nao funciona"
    - "Os 9 specs e2e existentes continuam verdes com o checkbox obrigatorio no lugar"
  artifacts:
    - path: "app/registro/_components/register-form.tsx"
      provides: "disclosure <details> + checkbox obrigatorio + hidden termoId"
      contains: "aceiteTermos"
    - path: "app/registro/page.tsx"
      provides: "leitura do termo vigente e estado degradado quando nao ha nenhum"
      contains: "termoVigente"
  key_links:
    - from: "app/registro/page.tsx"
      to: "termoVigente"
      via: "leitura no server component, passada por prop"
      pattern: "termoVigente\\("
    - from: "app/registro/_components/register-form.tsx"
      to: "app/registro/actions.ts"
      via: "campos aceiteTermos e termoId no FormData"
      pattern: 'name="termoId"'
---

<objective>
A metade cliente de TERM-01 (D-11) e o raio de impacto que ela produz.

A UI e minima: um `<details>/<summary>` nativo com o texto completo, um checkbox `required` e um
hidden `termoId`. O projeto NAO tem componente de modal (confirmado duas vezes: Fase 4 e RESEARCH
desta fase), e estabelecer um sistema de modais — focus trap, scroll lock, Escape, return-focus,
portal — para revelar um bloco de texto seria inventar um design system dentro de uma fase proibida
de inventar um. `<details>` satisfaz D-11 literalmente e degrada perfeitamente com JS desabilitado, o
que importa na unica tela do produto que ainda nao tem sessao.

O risco real deste plano nao e a UI, e o BLAST RADIUS: os 9 arquivos de spec e2e registram pela UI de
`/registro` (13 ocorrencias do locator `Criar minha loja`) e CADA UM tem seu proprio helper privado de
registro — nao existe helper compartilhado em `e2e/helpers.ts`. Um checkbox `required` novo faz o
submit nao disparar e os 9 specs falham por timeout, com a mensagem apontando para o lugar errado. Os
9 sao atualizados NESTE plano, junto do checkbox.

Purpose: fechar TERM-01 fim a fim e a janela vermelha aberta pelo plano 06-06.
Output: formulario + page com estado degradado + 9 specs atualizados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-termos-de-uso-e-aceite/06-CONTEXT.md
@.planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md
@.planning/phases/06-termos-de-uso-e-aceite/06-PATTERNS.md
@.planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md
@.planning/phases/06-termos-de-uso-e-aceite/06-06-SUMMARY.md
@app/registro/page.tsx
@app/registro/_components/register-form.tsx
@app/[slug]/admin/(protected)/_components/collapsible-section.tsx
@lib/termo-vigente.ts
@e2e/cadastro-e-login.spec.ts
@e2e/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco de termos no RegisterForm — details, checkbox obrigatorio e hidden termoId</name>
  <files>app/registro/_components/register-form.tsx, app/registro/_components/register-form.test.tsx</files>
  <read_first>
    - app/registro/_components/register-form.tsx (arquivo inteiro, 133 linhas: a assinatura de props da linha 11-15, o `INPUT_CLASS` da linha 9, o ritmo `space-y-4` do `<form>`, o bloco `ModoInterfacePicker` das linhas 96-102, o bloco de erro das 104-121 e o botao de submit das 123-129)
    - app/[slug]/admin/(protected)/_components/collapsible-section.tsx (o padrao `<details>/<summary>` do projeto — 15 linhas, zero JS. REUSAR O PADRAO, NAO IMPORTAR O ARQUIVO: ele vive dentro de `(protected)`, uma arvore que `/registro` nao deve alcancar)
    - app/registro/_components/register-form.test.tsx (arquivo inteiro: como os casos existentes montam o componente e quais props passam)
    - .planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md secao "### 1. Registration terms block" — as strings de classe sao VINCULANTES; e as secoes "## Typography" (a regra de 14px que sobrescreve o esboco de 12px do RESEARCH), "## Spacing Scale" (proibicao de `py-2.5` e de `mt-0.5`) e "## Color" (accent do checkbox)
    - app/registro/actions.ts (estado apos o plano 06-06 — os nomes de campo `aceiteTermos` e `termoId` sao o contrato)
  </read_first>
  <action>
Modificar `app/registro/_components/register-form.tsx`.

(1) A assinatura de props ganha `termo: { id: string; versao: number; conteudo: string }` — REQUERIDO,
nao opcional. O caso nulo e tratado pela page (Task 2), entao este componente nunca precisa renderizar
meio estado.

(2) Inserir um `<div>` novo IMEDIATAMENTE DEPOIS do bloco do `ModoInterfacePicker` (que termina por
volta da linha 102) e IMEDIATAMENTE ANTES do bloco `{state.error && ...}` (linha 104). A posicao
importa: uma recusa server-side (E2/E3) precisa aparecer logo acima do botao de submit, onde o erro ja
aparece hoje. O bloco participa do `space-y-4` existente do form.

Conteudo do bloco, com as strings de classe VINCULANTES da UI-SPEC:
- `<details className="rounded-xl border border-slate-200 bg-slate-50/60 px-3">` — fechado por default.
  Aberto por default transformaria os termos numa parede de texto inline, que e exatamente o que D-11
  rejeitou.
- `<summary className="cursor-pointer select-none py-3 text-sm/[1.5] font-semibold text-slate-600">`
  com o texto `Ler os Termos de Uso (versao {termo.versao})`. Usar `font-semibold`, NAO o
  `font-medium` de `collapsible-section.tsx`: o contrato de peso desta fase e 400/600 apenas. Usar
  `py-3`, NAO o `py-2.5` daquele arquivo: `py-2.5` da 40px e o minimo de alvo de toque e 44px, e o
  `<summary>` e controle interativo. `collapsible-section.tsx` NAO e editado — as divergencias sao
  deliberadas e valem so para o markup novo.
- A caixa do texto:
  `<div role="region" aria-label="Texto dos Termos de Uso" tabIndex={0} className="mb-3 max-h-64 overflow-y-auto whitespace-pre-wrap rounded-lg border border-slate-200 bg-white p-4 text-sm/[1.5] text-slate-700">{termo.conteudo}</div>`.
  14px (`text-sm`), NAO 12px: o esboco do RESEARCH usa `text-xs`, e esta e uma sobrescrita deliberada
  da UI-SPEC — os termos sao a unica copy do produto que o usuario esta sendo juridicamente convidado
  a ter lido; poe-los dois passos abaixo do corpo, ao lado de labels do mesmo tamanho, sinaliza
  "boilerplate, pule" e e a forma exata de dark pattern que um registro de consentimento existe para
  refutar. `max-h-64` (256px) e mais curto que os 288px da tela de gate de proposito: aqui a caixa
  vive dentro de um formulario ja alto e nao pode empurrar `Criar minha loja` para fora da viewport.
  `dangerouslySetInnerHTML` e PROIBIDO — o conteudo e no de texto React, com escape automatico (D-05).
- O label do checkbox:
  `<label className="flex cursor-pointer items-start gap-3 py-3 text-sm/[1.5] text-slate-600">`
  contendo `<input type="checkbox" name="aceiteTermos" required className="h-5 w-5 shrink-0 rounded border-slate-300 accent-slate-800" />`
  e `<span>Li e aceito os <span className="font-semibold text-slate-800">Termos de Uso</span></span>`.
  O texto `Li e aceito os Termos de Uso` e travado verbatim por D-11. O input fica DENTRO do `<label>`,
  entao a linha inteira de 45px e area de clique e nao e preciso par `htmlFor`/`id`. NAO usar
  `mt-0.5` para alinhar opticamente: 2px nao e multiplo de 4 e o alinhamento ja cai dentro de 1px com
  `items-start` e nenhuma margem.
- `<input type="hidden" name="termoId" value={termo.id} />`.

(3) NAO alterar `Criar minha loja` nem `Criando conta...`. Os 9 specs e2e carregam 13 ocorrencias
desse locator; renomear transforma uma regressao de checkbox em 9 timeouts simultaneos apontando para
a causa errada. NAO alterar `INPUT_CLASS`, nenhum campo existente, nem o bloco de erro.

PROIBICOES vinculantes: nenhuma classe `amber-*`; nenhum peso 500 ou 700 no markup novo; nenhum
`py-2.5` em controle interativo novo; nenhum `<dialog>`, portal, focus trap ou `useState` para a
disclosure (o `<details>` nativo nao adiciona estado React a um componente que ja tem um `useState`
para o seletor de modo); nenhum icone no `<summary>` (o triangulo nativo e a affordance; um chevron
exigiria duplica-lo ou suprimir o nativo com `list-none`); nenhuma estilizacao custom do estado
invalido do checkbox (a bolha nativa do `required` basta, e a E3 do servidor cobre o caminho sem JS).

Estender `app/registro/_components/register-form.test.tsx` com casos que afirmem: o `<summary>`
renderiza com o numero de versao recebido; o conteudo do termo aparece no documento; o checkbox
`aceiteTermos` existe e tem o atributo `required`; o hidden `termoId` existe com o `value` do termo
recebido; e o botao `Criar minha loja` continua com o texto exato. Nao alterar os casos existentes
alem de passar a nova prop `termo` onde o componente e montado.
  </action>
  <verify>
    <automated>npx vitest run app/registro/_components/register-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/registro/_components/register-form.test.tsx` sai 0 com no minimo 5 casos novos
    - `grep -c 'name="aceiteTermos"' app/registro/_components/register-form.tsx` retorna 1 e a mesma tag contem `required`
    - `grep -c 'name="termoId"' app/registro/_components/register-form.tsx` retorna 1
    - `grep -c "<details" app/registro/_components/register-form.tsx` retorna 1 e `grep -c "open" nessa tag` nao encontra atributo `open`
    - `grep -c "whitespace-pre-wrap" app/registro/_components/register-form.tsx` retorna 1
    - `grep -c "dangerouslySetInnerHTML" app/registro/_components/register-form.tsx` retorna 0
    - `grep -c "Criar minha loja" app/registro/_components/register-form.tsx` retorna 1 e o texto e byte-identico ao anterior
    - `grep -cE "amber|font-medium|font-bold|mt-0.5" app/registro/_components/register-form.tsx` retorna 0
    - `grep -c "collapsible-section" app/registro/_components/register-form.tsx` retorna 0 (o padrao e reusado, o arquivo nao e importado)
    - `git diff --stat "app/[slug]/admin/(protected)/_components/collapsible-section.tsx"` vazio
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0
  </acceptance_criteria>
  <done>O formulario de registro exibe a versao vigente dos termos sem sair da tela e exige o aceite antes de submeter.</done>
</task>

<task type="auto">
  <name>Task 2: registro/page.tsx le o termo vigente e ganha o estado degradado</name>
  <files>app/registro/page.tsx</files>
  <read_first>
    - app/registro/page.tsx (arquivo inteiro, 43 linhas: a guarda de sessao das linhas 8-12, o shell do card das 15-25 com o gradiente hardcoded da linha 21, o cabecalho das 26-34 e a montagem de `<RegisterForm action={register} />` na linha 36)
    - lib/termo-vigente.ts (assinatura de `termoVigente()`)
    - app/registro/_components/register-form.tsx (estado apos a Task 1 — a prop `termo` e requerida)
    - app/[slug]/admin/bloqueado/_components/bloqueado-card.tsx linhas 76-93 (o bloco de erro inline, unico padrao de erro do projeto — copiar caractere a caractere)
    - .planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md secoes "### 5. Registration degraded state" e "### Why there is no empty state"
  </read_first>
  <action>
Modificar `app/registro/page.tsx`.

Depois da guarda de sessao existente, acrescentar `const termo = await termoVigente();`.

Se `termo` for `null`, renderizar o MESMO shell do card (fundo, barra de gradiente, tile do logo,
titulo `Crie sua loja` e o subtitulo — todos inalterados) e, NO LUGAR de `<RegisterForm />`, um unico
bloco de erro inline com a copy E4: `Nao foi possivel abrir o cadastro agora. Tente novamente em
alguns instantes.` O bloco e o padrao unico do projeto, copiado de `bloqueado-card.tsx:76-93`:
`flex items-center gap-2 rounded-xl border border-red-200 bg-red-50 px-3 py-2.5`, SVG
`h-4 w-4 shrink-0 text-red-500` com o path `M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z`, e
`<span className="text-xs text-red-600">`. Acrescentar tambem um `console.error` do lado do servidor
registrando a causa real ("nenhum termo publicado") — a copy ao usuario e generica de proposito
(CLAUDE.md, Error Handling), e a causa real nao vai para a tela.

SEM campos de formulario, SEM botao de submit, SEM disclosure nesse estado. Um formulario que renderiza
e nao pode ter sucesso e pior que uma parada honesta. Comentario no branch registrando: este estado e
INALCANCAVEL em qualquer ambiente que rodou a migration de seed da v1 (plano 06-01); ele existe para
que um banco em estado inesperado produza uma tela legivel em vez de um crash ou um submit quebrado em
silencio. E registrando tambem, explicitamente, que construir aqui um "estado vazio" amigavel
(`Nenhum termo publicado`, com o formulario funcionando assim mesmo) e VIOLACAO DE CONTRATO, nao
gentileza: seria uma segunda oportunidade de o sistema falhar aberto onde TERM-01 exige que ele falhe
fechado.

Caso contrario, renderizar `<RegisterForm action={register} termo={termo} />` — passar apenas
`{ id, versao, conteudo }`; `publicadoEm` nao e usado nesta tela.

NAO alterar o gradiente hardcoded da linha 21 (`linear-gradient(90deg, #18181b, #f59e0b)`), o `<h1>`
`Crie sua loja` (que usa `text-xl font-bold`, carve-out legado documentado na UI-SPEC), nem a guarda
de sessao.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "termoVigente" app/registro/page.tsx` retorna >= 1
    - `grep -c "termo={termo}" app/registro/page.tsx` retorna 1
    - `grep -c "console.error" app/registro/page.tsx` retorna 1 (a causa real so no servidor)
    - No branch degradado nao existe `<RegisterForm` nem `<form` nem `<input` (verificavel lendo o branch)
    - `grep -c "Nenhum termo" app/registro/page.tsx` retorna 0 (nenhum estado vazio amigavel foi construido)
    - `grep -c "linear-gradient(90deg, #18181b, #f59e0b)" app/registro/page.tsx` retorna 1 (gradiente existente intacto)
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0
  </acceptance_criteria>
  <done>A page busca o termo vigente e, na ausencia dele, para de forma honesta em vez de mostrar um formulario que nao funciona.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar os helpers de registro dos 9 specs e2e (blast radius do Pitfall 6)</name>
  <files>e2e/cadastro-e-login.spec.ts, e2e/bloqueio-por-inadimplencia.spec.ts, e2e/worker-reconciliacao.spec.ts, e2e/movimentacao-estoque.spec.ts, e2e/combo-crud.spec.ts, e2e/promocao-crud.spec.ts, e2e/modo-simples.spec.ts, e2e/produto-crud-e-catalogo.spec.ts, e2e/sessao-revogada.spec.ts</files>
  <read_first>
    - e2e/cadastro-e-login.spec.ts (linhas 5-16: o helper privado de registro, com a sequencia `getByLabel(...).fill(...)` terminando em `getByRole("button", { name: "Criar minha loja" }).click()`)
    - e2e/helpers.ts (arquivo inteiro: confirmar que NAO existe helper de registro compartilhado — so `seedFatosBilling`, `expectGoneFromCatalogo` e `uniqueEmpresa`)
    - app/registro/_components/register-form.tsx (estado apos a Task 1 — o label exato do checkbox e o que os specs precisam localizar)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "### Pitfall 6"
  </read_first>
  <action>
Cada um dos 9 specs tem seu PROPRIO helper privado de registro — nao ha um compartilhado. Em cada um,
localizar a sequencia de preenchimento de `/registro` e inserir, IMEDIATAMENTE ANTES do
`getByRole("button", { name: "Criar minha loja" }).click()`, a marcacao do checkbox:
`await page.getByLabel(/Li e aceito os Termos de Uso/).check();`

Usar regex (nao string exata) no locator porque o label contem um `<span>` aninhado em volta de
"Termos de Uso" e a normalizacao de texto acessivel pode variar; a regex casa de qualquer forma.

Nenhuma outra alteracao nesses arquivos. NAO renomear o botao, NAO trocar assercoes, NAO extrair um
helper compartilhado nesta fase (extrair mudaria 9 arquivos por um motivo diferente do desta fase e
tornaria o diff ilegivel — se valer a pena, e item proprio).

Confirmar a lista completa por grep antes de comecar (`grep -lc "Criar minha loja" e2e/*.spec.ts`) e
depois de terminar, para garantir que nenhum dos 9 ficou para tras. `modo-simples.spec.ts` e
`sessao-revogada.spec.ts` tem DUAS ocorrencias cada e `cadastro-e-login.spec.ts` tem TRES — todas as
13 ocorrencias precisam do `check()` antes do respectivo `click()`, nao apenas a primeira de cada
arquivo.

Este trabalho vai no MESMO commit (ou na mesma sequencia imediata de commits) que a Task 1. Fazer o
checkbox sem os specs deixa a suite e2e vermelha por dois motivos simultaneos, e o diagnostico fica
caro — e a ordem de trabalho recomendada explicitamente pelo RESEARCH.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e` sai 0 com os 22 testes e2e existentes passando (numero registrado em STATE.md ao fim da Fase 4)
    - `grep -c "Li e aceito os Termos de Uso" e2e/*.spec.ts` soma 13 ocorrencias no total, distribuidas nos 9 arquivos
    - Para cada arquivo, o numero de `check()` de termos e igual ao numero de `Criar minha loja` (`grep -c` por arquivo bate)
    - `grep -c "Criar minha loja" e2e/*.spec.ts` continua somando 13 (nenhum locator renomeado)
    - `git diff --stat e2e/helpers.ts` vazio (nenhum helper compartilhado extraido nesta fase)
    - `git diff --stat e2e/` mostra alteracoes em exatamente 9 arquivos
  </acceptance_criteria>
  <done>Os 9 specs e2e voltam ao verde com o checkbox obrigatorio em producao, e a janela vermelha aberta pelo plano 06-06 esta fechada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante anonimo -> `/registro` (RSC) | Unica tela do produto sem sessao; o conteudo do termo sai no RSC Payload para qualquer visitante |
| browser -> `FormData` do registro | `termoId` e `aceiteTermos` sao dados controlados pelo cliente; a UI e apenas a primeira metade da validacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | texto dos termos em `/registro` | mitigate | Renderizado como no de texto React com `whitespace-pre-wrap`; `dangerouslySetInnerHTML` proibido e verificado por grep. D-05 torna o conteudo texto puro precisamente para eliminar essa superficie |
| T-06-32 | Repudiation | checkbox burlado | mitigate | `required` e a metade CLIENT; a recusa em `app/registro/actions.ts` (plano 06-06) e a metade SERVER. As duas, sempre — um submit por cliente proprio ou com JS desabilitado nao passa pela primeira |
| T-06-13 | Denial of Service | `/registro` sem termo vigente | mitigate | Estado degradado explicito, sem formulario. Construir um "estado vazio" com o formulario funcionando seria uma segunda oportunidade de falhar aberto onde TERM-01 exige falhar fechado |
| T-06-24 | Information Disclosure | copy E4 | mitigate | Mensagem generica ao usuario; a causa real ("nenhum termo publicado") so em `console.error` no servidor |
| T-06-35 | Denial of Service | disclosure implementada como modal | mitigate | `<details>` nativo: zero JS, zero portal, zero focus trap. Um modal aqui exigiria um sistema inteiro numa fase proibida de inventar design system, e quebraria com JS desabilitado na unica tela sem sessao |
| T-06-36 | Repudiation | regressao silenciosa dos 9 specs e2e | mitigate | Os 9 helpers privados sao atualizados no mesmo commit do checkbox; a contagem de 13 ocorrencias e verificada por grep antes e depois |
| T-06-SC | Tampering | npm installs / registry de componentes | mitigate | Zero pacotes novos; nenhum bloco de registry consumido (`components.json` ausente, shadcn nao inicializado). `git diff package.json` vazio |
</threat_model>

## Artifacts this phase produces

Simbolos e caminhos CRIADOS ou ESTENDIDOS por este plano:

- Prop nova no componente existente: `RegisterForm` passa a receber `termo: { id, versao, conteudo }`
- Elementos de formulario novos: `input[name="aceiteTermos"]` (checkbox `required`), `input[name="termoId"]` (hidden)
- Texto de UI novo: `Ler os Termos de Uso (versao {n})`, `Li e aceito os Termos de Uso`
- Branch novo em `app/registro/page.tsx`: estado degradado com a copy E4
- Locator novo nos specs e2e: `getByLabel(/Li e aceito os Termos de Uso/)`

<verification>
- `npx vitest run app/registro/_components/register-form.test.tsx` verde
- `npm run test` verde (suite completa)
- `npm run test:e2e` verde com os 22 testes existentes (exige Postgres com as migrations do plano 06-01 aplicadas)
- `npx tsc --noEmit` sai 0; `npm run lint` sai 0
- `grep -cE "amber|font-medium|font-bold" app/registro/_components/register-form.tsx` retorna 0
- `git diff --stat "app/[slug]/admin/(protected)/_components/collapsible-section.tsx" e2e/helpers.ts` vazio
- `git diff package.json` vazio
</verification>

<success_criteria>
- Abrir `/registro`, expandir `Ler os Termos de Uso`, ler o texto, marcar o aceite e concluir o cadastro leva ao painel com o aceite gravado
- Tentar submeter sem marcar o checkbox nao dispara o submit (validacao nativa do browser)
- Com a tabela de termos vazia, `/registro` mostra o card com a mensagem E4 e nenhum campo de formulario
</success_criteria>

<output>
Create `.planning/phases/06-termos-de-uso-e-aceite/06-07-SUMMARY.md` when done
</output>
