---
phase: 06-termos-de-uso-e-aceite
plan: 06
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - app/services/empresa.service.ts
  - app/services/empresa.service.test.ts
  - app/registro/actions.ts
  - app/registro/actions.test.ts
autonomous: true
requirements: [TERM-01]

must_haves:
  truths:
    - "Nao existe caminho de servidor que crie uma conta sem gravar o aceite dos termos"
    - "O aceite do cadastro fica na MESMA transacao que cria Empresa e Usuario — se uma falhar, nenhuma fica"
    - "Um cadastro submetido sem o checkbox marcado e recusado pelo servidor, com mensagem acionavel"
    - "Um cadastro cuja versao de termos mudou entre o render e o submit e recusado, sem gravar aceite errado"
    - "Um cadastro num banco sem nenhum termo publicado falha de forma explicita, nunca criando conta sem aceite"
  artifacts:
    - path: "app/services/empresa.service.ts"
      provides: "registerComUsuario grava AceiteTermo e Usuario.termoAceitoId na transacao existente"
      contains: "aceiteTermo"
    - path: "app/registro/actions.ts"
      provides: "validacao server-side do checkbox e do termoId"
      contains: "aceiteTermos"
    - path: "app/registro/actions.test.ts"
      provides: "primeiro teste de Server Action de registro do projeto"
  key_links:
    - from: "app/services/empresa.service.ts"
      to: "tx.aceiteTermo.create"
      via: "quarta escrita dentro da $transaction de registro"
      pattern: "aceiteTermo\\.create"
    - from: "app/registro/actions.ts"
      to: "empresaService.registerComUsuario"
      via: "termoAceitoId propagado no DTO"
      pattern: "termoAceitoId"
---

<objective>
A metade servidor de TERM-01: o registro so conclui com aceite, e o aceite nasce atomico com a conta.

`empresaService.registerComUsuario` ja e uma `prisma.$transaction` com tres escritas (Empresa,
Usuario, AuditoriaAcesso). A quarta entra ali, e o `termoAceitoId` do usuario sai ja preenchido — o
mesmo padrao que a Fase 2 usou para o trial inicial. Se o cadastro fizer rollback (P2002 de email
duplicado, por exemplo), o aceite some junto e nao fica fantasma na tabela de fatos.

Este plano implementa deliberadamente o lado FECHADO da assimetria do RESEARCH e fecha a borda que
D-12 deixou em aberto na discussao: sem termo vigente, o registro RECUSA (a outra metade de D-12 e a
migration de seed da v1, no plano 06-01, que torna esta recusa inalcancavel na pratica). O gate de TERM-04 (plano 06-04) faz o oposto e nao gateia ninguem. A assimetria e
intencional — a falha aqui atinge UM cadastro, enquanto gatear contra um documento inexistente
derrubaria a base inteira; e criar conta sem aceite viola TERM-01 diretamente. Precisa estar comentada
nos dois lugares, senao um leitor futuro "corrige" um deles para casar com o outro.

Purpose: a UI do plano 06-07 depende deste contrato de servidor existir primeiro (`termoAceitoId` no
DTO, nomes de campo `aceiteTermos` e `termoId`).
Output: service + Server Action + os dois arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-termos-de-uso-e-aceite/06-CONTEXT.md
@.planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md
@.planning/phases/06-termos-de-uso-e-aceite/06-PATTERNS.md
@.planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md
@.planning/phases/06-termos-de-uso-e-aceite/06-02-SUMMARY.md
@app/services/empresa.service.ts
@app/registro/actions.ts
@lib/termo-vigente.ts
@lib/http-error.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: registerComUsuario grava o aceite na mesma transacao que cria a conta</name>
  <files>app/services/empresa.service.ts, app/services/empresa.service.test.ts</files>
  <read_first>
    - app/services/empresa.service.ts (linhas 13-19: `RegisterComUsuarioDTO`; linhas 78-156: `registerComUsuario` inteiro — o I/O de leitura fora da transacao nas linhas 79-80, as tres escritas dentro dela, o comentario normativo das linhas 91-96 sobre por que nenhum caminho de aplicacao escreve os fatos de billing, e o `catch` de P2002 das linhas 138-155)
    - app/services/empresa.service.test.ts (arquivo inteiro: como os casos existentes de `registerComUsuario` montam o mock de transacao)
    - lib/termo-vigente.ts (assinatura de `termoVigente()` — devolve `null` num banco sem termos)
    - app/services/termo.service.ts (referencia da forma da escrita de aceite; NAO chamar `termoService.registrarAceite` daqui, ver a acao)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "## Ponto de Extensao 3 — Registro atomico (TERM-01)"
  </read_first>
  <behavior>
    - Com termo vigente e `termoAceitoId` igual ao dele: cria Empresa, Usuario (com `termoAceitoId` preenchido), AuditoriaAcesso e AceiteTermo — quatro escritas, uma transacao
    - Sem nenhum termo vigente: lanca `HttpError` ANTES de abrir a transacao, e nenhuma escrita acontece
    - `termoAceitoId` recebido diferente do vigente: lanca `HttpError` antes da transacao, e nenhuma escrita acontece
    - P2002 em `email` continua produzindo `HttpError` 409 com a mensagem existente (comportamento inalterado)
    - P2002 em `slug` continua produzindo `HttpError` 409 com a mensagem existente (comportamento inalterado)
    - `trialFim`, `ultimoStatusAuditado: TRIAL` e a linha de auditoria de causa `REGISTRO` continuam sendo escritos como antes
  </behavior>
  <action>
Modificar `app/services/empresa.service.ts` em tres pontos.

(1) `RegisterComUsuarioDTO` ganha `termoAceitoId: string;` — obrigatorio, nao opcional. E o id que o
USUARIO VIU no formulario, nao o que o servidor descobrir sozinho: o campo existe precisamente para
que a comparacao de TOCTOU seja possivel.

(2) Em `registerComUsuario`, ANTES de abrir a `$transaction` (junto de `generateUniqueSlug` e
`bcrypt.hash`, linhas 79-80): `const termo = await termoVigente();`. Manter I/O de leitura fora da
transacao interativa e a mesma disciplina de pool que o JSDoc de `aplicar()` em
`app/api/cron/reconciliacao-diaria/route.ts` documenta.
Em seguida, duas guardas, ambas antes da transacao:
- `if (!termo) throw new HttpError("Nao foi possivel abrir o cadastro agora. Tente novamente em alguns instantes.", 503);`
- `if (data.termoAceitoId !== termo.id) throw new HttpError("Os termos foram atualizados. Leia a nova versao e aceite novamente para criar sua conta.", 409);`
As duas mensagens sao as copies E4 e E2 da UI-SPEC, literalmente — a Server Action as devolve ao
formulario sem reescrever.

(3) Dentro da `$transaction`, duas mudancas: acrescentar `termoAceitoId: termo.id` ao `data` do
`tx.usuario.create` (ao lado de `nome`, `email`, `senhaHash`, `empresaId`); e acrescentar, DEPOIS do
`tx.auditoriaAcesso.create` existente, a quarta escrita
`await tx.aceiteTermo.create({ data: { usuarioId: usuario.id, termoId: termo.id } });`.

NAO chamar `termoService.registrarAceite` daqui. Aquele metodo opera sobre o `prisma` global, fora
desta transacao, e usa o proprio `usuario.update` de bookkeeping — que aqui e desnecessario porque o
`create` do usuario ja nasce com o campo preenchido. E o mesmo raciocinio ja registrado nas linhas
116-123 para `acessoService.registrarTransicao`; reproduzir o comentario nessa forma.

Comentario normativo sobre a assimetria, obrigatorio, junto da guarda de `!termo`: o registro falha
FECHADO e o gate de TERM-04 (`lib/auth-guard.ts`) falha ABERTO, e isso e deliberado. A falha aqui
atinge UM cadastro; gatear contra documento inexistente derrubaria TODOS os tenants de uma vez e a
recuperacao dependeria de alguem conseguir publicar. Criar conta sem aceite viola TERM-01
diretamente. Escrever explicitamente que os dois lados estao comentados de proposito para que ninguem
"corrija" um deles para casar com o outro. Registrar tambem que, na pratica, esta guarda e
inalcancavel em qualquer ambiente que tenha rodado a migration de seed da v1 (plano 06-01) — ela
existe como backstop para um banco em estado inesperado, nao como fluxo esperado.

Comentario junto da guarda de TOCTOU citando Pitfall 4: o usuario abre `/registro`, le a v3, e
enquanto preenche o formulario o SUPERADMIN publica a v4; resolver "a vigente" no momento do submit
gravaria um aceite da v4 contra alguem que leu a v3, destruindo a unica propriedade que justifica a
existencia da tabela. O `termoAceitoId` recebido e dado controlado pelo cliente — a checagem e
IGUALDADE contra o vigente do servidor, nunca "usa o que veio", e o valor gravado e sempre `termo.id`.

Estender `app/services/empresa.service.test.ts` com os seis comportamentos do bloco de comportamento, sem
alterar os casos existentes. Os casos de guarda precisam afirmar que `prismaMock.$transaction` NAO
foi chamado — provar que a recusa acontece antes de qualquer escrita, nao dentro dela.
  </action>
  <verify>
    <automated>npx vitest run app/services/empresa.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/services/empresa.service.test.ts` sai 0 com no minimo 6 casos novos alem dos existentes
    - `grep -c "aceiteTermo.create" app/services/empresa.service.ts` retorna 1
    - `grep -c "termoAceitoId" app/services/empresa.service.ts` retorna >= 3 (DTO, guarda de TOCTOU, `usuario.create`)
    - `grep -c "termoVigente" app/services/empresa.service.ts` retorna >= 1, e a chamada aparece em uma linha ANTERIOR a do `prisma.$transaction` no arquivo
    - `grep -c "registrarAceite" app/services/empresa.service.ts` retorna 0
    - Existe um caso de teste que afirma `prismaMock.$transaction` nao chamado quando `termoVigente()` devolve `null`
    - Existe um caso de teste que afirma `prismaMock.$transaction` nao chamado quando o `termoAceitoId` diverge do vigente
    - Os casos existentes de P2002 em `email` e em `slug` continuam passando sem edicao
    - `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>Nao existe caminho pelo qual `registerComUsuario` crie uma conta sem uma linha de `AceiteTermo` correspondente na mesma transacao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server Action de registro valida o checkbox e o termoId</name>
  <files>app/registro/actions.ts, app/registro/actions.test.ts</files>
  <read_first>
    - app/registro/actions.ts (arquivo inteiro, 81 linhas: a cadeia de seis validacoes das linhas 26-48 no estilo `if (!x) return { error: "..." }`, o `try/catch` das 52-78 que ja mapeia `HttpError`, e o `redirect(redirectTo)` da linha 80 FORA do `try`)
    - app/services/empresa.service.ts (estado apos a Task 1 — `RegisterComUsuarioDTO` com `termoAceitoId`)
    - app/services/acesso.service.test.ts (shape `// @vitest-environment node` + `prismaMock`, a aproximacao mais proxima que existe: NAO ha teste de Server Action de registro hoje)
    - app/[slug]/admin/bloqueado/page.test.ts (o padrao `vi.hoisted` para mockar `redirect` de forma que ele LANCE — necessario aqui, porque `register` termina em `redirect`)
    - .planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md secao "### Error strings" (copies E2, E3, E4 literais)
  </read_first>
  <behavior>
    - `aceiteTermos` ausente do `FormData`: retorna `{ error: "E preciso aceitar os Termos de Uso para criar a conta." }` e nao chama o service
    - `aceiteTermos` presente mas com valor diferente de `"on"`: mesmo retorno
    - `termoId` ausente ou string vazia: mesmo retorno (o par checkbox+hidden vem junto ou nao vem)
    - Com checkbox e `termoId`: chama `empresaService.registerComUsuario` com `termoAceitoId` igual ao `termoId` do formulario
    - `HttpError` vindo do service (503 de "sem termos", 409 de TOCTOU, 409 de email duplicado): retorna `{ error: <mensagem do erro> }`, sem reescrever
    - As seis validacoes existentes (nome da empresa, nome do responsavel, email, tamanho da senha, confirmacao, modo de interface) continuam retornando os mesmos erros, na mesma ordem
    - Sucesso: cria a sessao e redireciona para `/{slug}/admin`
  </behavior>
  <action>
Modificar `app/registro/actions.ts` acrescentando duas leituras e duas validacoes, no MESMO estilo das
seis que ja existem.

Leituras, junto das outras no topo da funcao:
`const aceiteTermos = formData.get("aceiteTermos");` e
`const termoId = String(formData.get("termoId") ?? "").trim();`

Validacoes, no FIM da cadeia existente (depois da checagem de `modoInterfaceRaw`), nesta ordem:
1. `if (aceiteTermos !== "on") return { error: "E preciso aceitar os Termos de Uso para criar a conta." };`
   — `"on"` e o valor nativo de um checkbox marcado, e a copy e a E3 da UI-SPEC, literal.
2. `if (!termoId) return { error: "E preciso aceitar os Termos de Uso para criar a conta." };`
   — mesma mensagem de proposito: o hidden input viaja junto do checkbox e sua ausencia so acontece
   com formulario adulterado ou renderizado num estado impossivel; expor uma mensagem tecnica ao
   usuario nao o ajudaria a agir.

Passar `termoAceitoId: termoId` no objeto entregue a `empresaService.registerComUsuario`.

Nao alterar o `catch` existente: ele ja mapeia `HttpError` para `{ error: error.message }`, e e por
isso que as copies E2 e E4 podem morar no service (Task 1) sem duplicacao aqui. Nao alterar o
`redirect(redirectTo)` fora do `try`.

Comentario acima do par de validacoes registrando a disciplina de D-11: `required` no checkbox e a
metade CLIENT (plano 06-07) e esta recusa e a metade SERVER; as duas, sempre — "nunca confiar so no
client" e o padrao explicito ja aplicado a email e senha neste mesmo arquivo, e um formulario
submetido com JS desabilitado ou por cliente proprio nao passa pela primeira metade.

Criar `app/registro/actions.test.ts` — nao existe teste de Server Action de registro hoje. Usar
`// @vitest-environment node`, `vi.hoisted` para mockar `redirect` (fazendo-o LANCAR `REDIRECT:<url>`,
como em `bloqueado/page.test.ts`), `createAdminSession` e `empresaService.registerComUsuario`. Cobrir
os sete comportamentos do bloco de comportamento, com um helper local que monta um `FormData` valido para
que cada caso so precise remover ou alterar um campo.

Os casos de recusa precisam afirmar que o mock de `registerComUsuario` NAO foi chamado. O caso de
sucesso precisa afirmar que `registerComUsuario` recebeu `termoAceitoId` exatamente igual ao `termoId`
do formulario — e essa assercao que amarra o contrato de nome de campo com o plano 06-07.
  </action>
  <verify>
    <automated>npx vitest run app/registro/actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/registro/actions.test.ts` sai 0 com no minimo 7 casos
    - `grep -c "aceiteTermos" app/registro/actions.ts` retorna >= 2 (leitura + validacao)
    - `grep -c "termoId" app/registro/actions.ts` retorna >= 3 (leitura, validacao, passagem ao service)
    - `grep -c "termoAceitoId" app/registro/actions.ts` retorna 1
    - Existe um caso que afirma que `registerComUsuario` nao foi chamado quando o checkbox falta
    - Existe um caso que afirma `registerComUsuario` recebendo `termoAceitoId` igual ao `termoId` do `FormData`
    - Existe um caso que afirma que a mensagem de um `HttpError` vindo do service chega intacta em `{ error }`
    - Os seis erros de validacao existentes continuam sendo retornados com o mesmo texto (um caso por erro, ou um caso parametrizado)
    - `npm run test` sai 0 e `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>A metade servidor de D-11 existe e esta provada: sem `aceiteTermos` e sem `termoId`, nao ha chamada de service e nao ha conta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante anonimo -> Server Action `register` | Superficie pre-autenticacao: todo o `FormData` e nao confiavel, inclusive o `termoId` |
| Server Action -> `empresaService.registerComUsuario` | O service e o unico ponto que pode criar conta; a atomicidade so existe dentro da transacao dele |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Repudiation | aceite do cadastro (TOCTOU) | mitigate | `termoAceitoId` viaja no `FormData` e e comparado por IGUALDADE contra `termoVigente()` do servidor antes de abrir a transacao; o valor gravado e sempre `termo.id`, nunca o recebido (Pitfall 4) |
| T-06-32 | Repudiation | conta criada sem aceite | mitigate | Validacao dupla de D-11: `required` no checkbox (metade client, plano 06-07) e recusa server-side aqui. Um submit com JS desabilitado ou por cliente proprio nao passa pela primeira metade |
| T-06-33 | Tampering | aceite orfao de conta (ou vice-versa) | mitigate | As quatro escritas moram na MESMA `$transaction`; um rollback por P2002 de email leva o `AceiteTermo` junto e nao deixa fantasma na tabela de fatos |
| T-06-13 | Denial of Service | cadastro num banco sem termos | accept (fail-closed deliberado) | O registro RECUSA com 503 e copy generica. E o lado oposto do gate de TERM-04 de proposito: aqui a falha atinge UM cadastro; la, gatear contra documento inexistente derrubaria a base inteira. Inalcancavel em qualquer ambiente que rodou a migration de seed da v1 |
| T-06-24 | Information Disclosure | mensagem de erro do cadastro | mitigate | Copy E4 generica ("Nao foi possivel abrir o cadastro agora...") — a causa real ("nenhum termo publicado") vai so para `console.error`, conforme CLAUDE.md. E3 e especifica porque o usuario pode agir sobre ela |
| T-06-34 | Elevation of Privilege | `role` ou `acessoVitalicio` via cadastro | mitigate | `RegisterComUsuarioDTO` continua sem `role` e sem os fatos de billing; o `usuario.create` monta `data` campo a campo. O unico campo novo e `termoAceitoId`, que aponta para um documento publico |
| T-06-SC | Tampering | npm installs | mitigate | Zero pacotes novos; `git diff package.json` vazio ao fim deste plano |
</threat_model>

## Artifacts this phase produces

Simbolos e caminhos CRIADOS ou ESTENDIDOS por este plano:

- Campo novo no DTO existente: `RegisterComUsuarioDTO.termoAceitoId`
- Arquivo novo: `app/registro/actions.test.ts`
- Nomes de campo de `FormData` lidos pela primeira vez: `aceiteTermos`, `termoId` (o markup que os produz nasce no plano 06-07)
- Strings de erro novas: `"E preciso aceitar os Termos de Uso para criar a conta."`, `"Os termos foram atualizados. Leia a nova versao e aceite novamente para criar sua conta."`, `"Nao foi possivel abrir o cadastro agora. Tente novamente em alguns instantes."`

<verification>
- `npx vitest run app/services/empresa.service.test.ts app/registro/actions.test.ts` verde
- `npm run test` verde (suite completa)
- `npx tsc --noEmit` sai 0; `npm run lint` sai 0
- `grep -c "registrarAceite" app/services/empresa.service.ts` retorna 0
- `git diff package.json` vazio
- NOTA DE COORDENACAO: ao fim deste plano a suite e2e fica temporariamente vermelha, porque o servidor
  passa a exigir `aceiteTermos` e o formulario ainda nao o envia. O plano 06-07 fecha essa janela no
  mesmo wave; nao tentar "consertar" a e2e aqui.
</verification>

<success_criteria>
- Submeter `/registro` sem aceite (por curl, contornando o client) nao cria conta e devolve a mensagem E3
- Um cadastro bem-sucedido produz exatamente uma linha em `AceiteTermo` e um `Usuario.termoAceitoId` apontando para o termo vigente
- Um email duplicado continua devolvendo 409 e nao deixa aceite orfao no banco
</success_criteria>

<output>
Create `.planning/phases/06-termos-de-uso-e-aceite/06-06-SUMMARY.md` when done
</output>
