---
phase: 06-termos-de-uso-e-aceite
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - app/[slug]/admin/aceitar-termos/actions.ts
  - app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx
  - app/[slug]/admin/aceitar-termos/_components/aceitar-button.tsx
  - app/[slug]/admin/aceitar-termos/page.tsx
  - app/[slug]/admin/aceitar-termos/page.test.ts
autonomous: true
requirements: [TERM-04]

must_haves:
  truths:
    - "O usuario com termos pendentes ve o texto completo da versao vigente numa tela sem chrome de admin"
    - "Aceitar grava o aceite contra a versao que o usuario viu e devolve o usuario ao painel"
    - "Se os termos forem republicados enquanto o usuario lia, o aceite e recusado e o texto novo e mostrado"
    - "O usuario consegue sair da conta a partir dessa tela, sem ficar preso"
    - "Nao existe combinacao de estados que produza loop de redirect entre /bloqueado e /aceitar-termos"
  artifacts:
    - path: "app/[slug]/admin/aceitar-termos/page.tsx"
      provides: "rota FORA do grupo (protected), com guarda simetrica propria"
      min_lines: 50
    - path: "app/[slug]/admin/aceitar-termos/actions.ts"
      provides: "Server Action de aceite que se autoriza sozinha"
      exports: ["aceitarTermos"]
    - path: "app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx"
      provides: "shell visual espelhando bloqueado-card, em slate e com glifo de documento"
      exports: ["AceiteCard"]
    - path: "app/[slug]/admin/aceitar-termos/_components/aceitar-button.tsx"
      provides: "CTA client com useFormStatus"
      exports: ["AceitarButton"]
  key_links:
    - from: "app/[slug]/admin/aceitar-termos/actions.ts"
      to: "getVerifiedSession"
      via: "auto-autorizacao da Server Action fora de (protected)"
      pattern: "getVerifiedSession\\("
    - from: "app/[slug]/admin/aceitar-termos/actions.ts"
      to: "termoService.registrarAceite"
      via: "usuarioId derivado de session.sub"
      pattern: "registrarAceite\\("
    - from: "app/[slug]/admin/aceitar-termos/page.tsx"
      to: "logout"
      via: "logout.bind(null, slug) reusando a action existente"
      pattern: "logout\\.bind"
---

<objective>
A tela de aceite de TERM-04: `/{slug}/admin/aceitar-termos`, replica exata da mecanica de
`/{slug}/admin/bloqueado` que a Fase 4 ja validou em producao, com um predicado diferente. Implementa
D-08 (rota dedicada fora do grupo `(protected)`, nunca um modal client-side sobreposto), D-09 (texto
completo da versao vigente + botao de aceitar + opcao de logout) e D-10 (apos aceitar, volta ao admin).

Tres pecas, todas obrigatorias e nenhuma opcional:
1. A pasta fica FORA do grupo `(protected)`, irma de `bloqueado/`. A posicao do arquivo na arvore
   E a mitigacao — mover a pasta para dentro do grupo reintroduz `ERR_TOO_MANY_REDIRECTS` sem alterar
   uma linha de codigo.
2. A page tem guarda simetrica propria, com tres condicoes na ordem exata da maquina de estados.
3. A Server Action se autoriza sozinha. Ela vive fora de `(protected)`, entao NENHUM guard a cobre —
   os docs do Next 16.3.0 sao explicitos que Server Actions sao endpoints publicos.

E uma proibicao que vale o mesmo: `bloqueado/page.tsx` NAO pode ganhar checagem de termos. Se ganhar,
o estado (bloqueado, pendente) vira `/bloqueado` -> `/aceitar-termos` -> `/bloqueado` indefinidamente.

Purpose: sem esta tela o redirect do plano 06-04 aponta para o vazio.
Output: quatro arquivos novos sob `aceitar-termos/` + teste dos 4 estados da maquina.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-termos-de-uso-e-aceite/06-CONTEXT.md
@.planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md
@.planning/phases/06-termos-de-uso-e-aceite/06-PATTERNS.md
@.planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md
@.planning/phases/06-termos-de-uso-e-aceite/06-04-SUMMARY.md
@app/[slug]/admin/bloqueado/page.tsx
@app/[slug]/admin/bloqueado/_components/bloqueado-card.tsx
@app/[slug]/admin/bloqueado/page.test.ts
@app/[slug]/admin/_components/pagar-button.tsx
@app/[slug]/admin/_lib/assinatura-actions.ts
@app/[slug]/admin/_lib/auth-actions.ts
@lib/termo-vigente.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Action de aceite, com autorizacao propria e guarda de TOCTOU</name>
  <files>app/[slug]/admin/aceitar-termos/actions.ts</files>
  <read_first>
    - app/[slug]/admin/_lib/assinatura-actions.ts (`iniciarPagamento`, linhas 1-55: a auto-autorizacao das linhas 29-37 com `getVerifiedSession` + conferencia de slug, e o `redirect` fora do `try` das linhas 50-54, com o comentario que explica por que)
    - app/[slug]/admin/_lib/auth-actions.ts (linhas 50-53: a action `logout` existente, que sera REUSADA e nao reescrita)
    - lib/termo-vigente.ts (assinatura de `termoVigente()`)
    - app/services/termo.service.ts (assinatura de `termoService.registrarAceite(usuarioId, termoId)`)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secoes "### Pitfall 8" e "### Pitfall 4"
    - .planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md secao "### Error strings" (a copy exata de E1)
  </read_first>
  <behavior>
    - Sem sessao valida, ou com sessao de outro slug: redirect para `/{slug}/admin/login`, sem nenhum I/O de termos
    - `termoId` do formulario diferente do id do vigente do servidor: redirect para `/{slug}/admin/aceitar-termos?erro=termos-atualizados`, sem gravar nada
    - `termoId` ausente ou vazio: mesmo tratamento do caso anterior (nao grava)
    - Sem termo vigente no banco: redirect para `/{slug}/admin` (nada a aceitar; consistente com o gate que falha aberto)
    - `termoId` igual ao vigente: grava o aceite com `usuarioId` derivado da sessao e redireciona para `/{slug}/admin`
    - Um erro do service (nao P2002) resulta em redirect com `?erro=termos-atualizados` apos `console.error`, nunca numa tela quebrada
  </behavior>
  <action>
Criar `app/[slug]/admin/aceitar-termos/actions.ts` com `"use server"` no topo, exportando
`aceitarTermos(slug: string, formData: FormData): Promise<void>` — a assinatura que permite ligar por
`aceitarTermos.bind(null, slug)`, no mesmo formato de `iniciarPagamento.bind(null, slug)`.

Sequencia obrigatoria:
1. `const session = await getVerifiedSession();`
2. `if (!session || session.empresaSlug !== slug) redirect(\`/${slug}/admin/login\`);` — antes de
   qualquer I/O de termos.
3. `const vigente = await termoVigente();`
4. `if (!vigente) redirect(\`/${slug}/admin\`);`
5. `const termoId = String(formData.get("termoId") ?? "");`
6. `if (termoId !== vigente.id) redirect(\`/${slug}/admin/aceitar-termos?erro=termos-atualizados\`);`
7. dentro de um `try`: `await termoService.registrarAceite(session.sub, vigente.id);`
   — passar `vigente.id`, NAO o `termoId` recebido, mesmo depois de comprovada a igualdade: e o valor
   do servidor que grava.
8. `catch`: `console.error(error)` e marcar que houve falha; NAO deixar a excecao subir para uma tela
   de erro.
9. FORA do `try`, o `redirect` final: `/{slug}/admin` no sucesso, ou
   `/{slug}/admin/aceitar-termos?erro=termos-atualizados` se houve falha.

Todos os `redirect()` ficam FORA de qualquer `try/catch` — `redirect` sinaliza lancando
`NEXT_REDIRECT` e um `catch` o engoliria, fazendo a navegacao simplesmente nao acontecer. Este e o
comentario literal ja presente em `assinatura-actions.ts:50-52`; reproduzi-lo.

JSDoc do arquivo cobrindo quatro decisoes:
(a) Pitfall 8 / docs do Next 16.3.0 — esta action mora FORA de `(protected)`, entao NENHUM guard a
cobre. Server Actions sao endpoints publicos e verificam autorizacao por conta propria. Sem o
`getVerifiedSession` acima, qualquer pessoa com um `admin_session` valido conseguiria disparar um
aceite; e sem derivar o `usuarioId` da sessao, conseguiria grava-lo em nome de outro usuario.
(b) O `usuarioId` vem de `session.sub` e de mais lugar nenhum. NAO existe e NAO pode existir input
`usuarioId` no formulario. Escrever isso como regra, nao como observacao.
(c) Pitfall 4 (TOCTOU) — o `termoId` do hidden input e dado CONTROLADO PELO CLIENTE. A checagem e
IGUALDADE contra o vigente do servidor, nunca "usa o que veio": nao ha caminho para o cliente escolher
qual versao aceitar. A janela e minuscula (publicacoes sao raras) e o custo do bug e um registro de
consentimento invalido — que e o pior tipo de bug num sistema cujo unico produto e o registro de
consentimento.
(d) D-10 — o retorno e `/{slug}/admin` fixo, sem parametro `?next=`. Um `?next=` seria vetor de open
redirect e exigiria validacao de destino (allowlist de path relativo, comecar com `/${slug}/admin`,
nao conter `//` nem `:`); custo desproporcional ao ganho de UX, e o RESEARCH ja rejeitou.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getVerifiedSession" "app/[slug]/admin/aceitar-termos/actions.ts"` retorna >= 1
    - `grep -c "session.sub" "app/[slug]/admin/aceitar-termos/actions.ts"` retorna >= 1
    - `grep -c 'formData.get("usuarioId")\|usuarioId"' "app/[slug]/admin/aceitar-termos/actions.ts"` nao encontra leitura de `usuarioId` do `FormData`
    - `grep -c "next=" "app/[slug]/admin/aceitar-termos/actions.ts"` retorna 0
    - `grep -c "use server" "app/[slug]/admin/aceitar-termos/actions.ts"` retorna 1
    - Nenhum `redirect(` aparece dentro de um bloco `try` no arquivo
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0
  </acceptance_criteria>
  <done>A action grava o aceite so para a sessao autenticada dona do slug, contra a versao do servidor, e nunca aceita `usuarioId` do formulario.</done>
</task>

<task type="auto">
  <name>Task 2: AceiteCard e AceitarButton — shell espelhando bloqueado-card, em slate, com glifo de documento</name>
  <files>app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx, app/[slug]/admin/aceitar-termos/_components/aceitar-button.tsx</files>
  <read_first>
    - app/[slug]/admin/bloqueado/_components/bloqueado-card.tsx (arquivo inteiro, 117 linhas: shell e gradiente das linhas 37-47, tile de status das 48-62, bloco de erro inline das 76-93, botao de logout das 100-110 INCLUINDO o comentario sobre nao ser acao destrutiva, e o JSDoc das 11-28 sobre props serem decisoes ja tomadas)
    - app/[slug]/admin/_components/pagar-button.tsx (arquivo inteiro, 65 linhas: `"use client"`, `useFormStatus` de `react-dom`, o JSDoc das linhas 1-15 explicando por que nao e o hook de estado de action, e a string de classe da variante `bloqueado`)
    - .planning/phases/06-termos-de-uso-e-aceite/06-UI-SPEC.md secoes "### 2. Acceptance gate — AceiteCard", "### 3. AceitarButton", "### 4. InlineError", "## Iconography", "## Color" e "## Typography" — as strings de classe sao VINCULANTES, nao sugestoes
    - lib/format.ts (helper de data existente, se houver, para formatar `publicadoEm`)
  </read_first>
  <action>
Criar dois componentes.

`_components/aceitar-button.tsx` — client component. `"use client"` no topo, `useFormStatus` de
`react-dom` (NAO `useActionState`: a action devolve `void` e termina em `redirect()`, entao nao existe
estado nenhum para renderizar de volta — so o "estou enviando" do form). Props: `{ primaryColor:
string }`. Renderiza `<button type="submit" disabled={pending}>` com a string de classe EXATA
`"w-full rounded-xl py-3 text-sm font-semibold text-white shadow-md transition-all hover:shadow-lg active:scale-[0.98] disabled:opacity-60 focus:ring-2"`
e `style={{ backgroundColor: primaryColor, "--tw-ring-color": primaryColor } as React.CSSProperties}`.
Rotulo `Aceitar e continuar`, virando `Registrando aceite...` quando `pending`. Reproduzir o JSDoc de
`pagar-button.tsx` explicando a escolha do hook.

`_components/aceite-card.tsx` — server component puro (sem `"use client"`), com as props sendo
DECISOES JA TOMADAS e nunca estado bruto:
`{ primaryColor: string; accentColor: string; termo: { id: string; versao: number; conteudo: string; publicadoEmFormatado: string }; erro: boolean; aceitarAction: (formData: FormData) => Promise<void>; logoutAction: () => Promise<void> }`.
Reproduzir o JSDoc de `bloqueado-card.tsx:20-23` adaptado: este componente nao recebe — e nao pode
receber — a informacao de "ja aceitou antes?"; um branch de texto aqui e a violacao exata que o
contrato de copy proibe (UI-SPEC: uma unica string para as duas populacoes, mesma disciplina de
`BLOQUEADO`/`CANCELADO` na Fase 4). Registrar tambem que `new Date()` NUNCA aparece dentro deste
componente — a data ja chega formatada como string, mesma regra que a Fase 4 pos em `AvisoCarencia`,
e pelo mesmo motivo (testavel sem fake timers).

Markup, seguindo o contrato vinculante da UI-SPEC:
- Shell e gradiente copiados literalmente de `bloqueado-card.tsx:37-47`:
  `flex min-h-screen items-center justify-center bg-slate-50 p-4`, `w-full max-w-md`,
  `overflow-hidden rounded-3xl border border-slate-100 bg-white shadow-xl`, barra `h-2` com
  `linear-gradient(90deg, primaryColor, accentColor)`, e o miolo em `p-8`.
- Tile de status: `mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-slate-100`
  com um SVG `h-8 w-8 text-slate-600`. O glifo e de DOCUMENTO, com o path
  `d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"`,
  atributos `fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeLinecap="round"
  strokeLinejoin="round" strokeWidth={2}`. NAO reusar o path de cadeado de `bloqueado-card.tsx:59` —
  o cadeado e o vocabulario da Fase 4 para acesso revogado, e este usuario nao perdeu nada.
- `<h1 class="text-center text-xl/[1.2] font-semibold text-slate-800">Termos de Uso</h1>`
- Linha de meta: `<p class="mt-1 text-center text-xs text-slate-500">Versao {versao} · publicada em {publicadoEmFormatado}</p>`
- Corpo: `<p class="mt-2 text-center text-sm/[1.5] text-slate-500">Publicamos uma versao dos termos que voce ainda nao aceitou. Leia o texto abaixo e aceite para voltar ao painel.</p>` — string UNICA, sem branch.
- Caixa do texto: `<div role="region" aria-label="Texto dos Termos de Uso" tabIndex={0} class="mt-4 max-h-72 overflow-y-auto whitespace-pre-wrap rounded-xl border border-slate-200 bg-slate-50 p-4 text-sm/[1.5] text-slate-700">{termo.conteudo}</div>`.
  O conteudo e no de texto React — escape automatico. `dangerouslySetInnerHTML` e PROIBIDO nesta fase,
  nas duas superficies (D-05 torna o conteudo texto puro precisamente para remover essa superficie).
  O `tabIndex={0}` nao e enfeite: uma caixa rolavel sem filho focavel e inalcancavel por teclado.
- Bloco de erro, so quando `erro` for verdadeiro, copiado caractere a caractere de
  `bloqueado-card.tsx:76-93` (`mt-4 flex items-center gap-2 rounded-xl border border-red-200 bg-red-50 px-3 py-2.5`,
  SVG `h-4 w-4 shrink-0 text-red-500` com o path de circulo-exclamacao, `span` `text-xs text-red-600`),
  com a copy E1: `Os termos foram atualizados enquanto voce lia. Leia a nova versao e aceite novamente.`
- Form de aceite: `<form action={aceitarAction} class="mt-6">` contendo
  `<input type="hidden" name="termoId" value={termo.id} />` e `<AceitarButton primaryColor={primaryColor} />`.
  NENHUM input `usuarioId` — a regra e do plano, e restringe o markup.
- Form de logout: copiado VERBATIM de `bloqueado-card.tsx:100-110`, incluindo o comentario sobre sair
  nao ser acao destrutiva, com a classe
  `mt-3 w-full rounded-xl border border-slate-200 py-3 text-sm font-semibold text-slate-500 transition-colors hover:bg-slate-50 hover:text-slate-700`
  e o rotulo `Sair da conta`.

PROIBICOES vinculantes da UI-SPEC: nenhuma classe utilitaria `amber-*` em nenhum dos dois arquivos
(ambar e reservado a linguagem de pagamento da Fase 4 e sera verificado por grep no plano 06-08);
nenhum peso de fonte 500 ou 700; nenhum `py-2.5` em controle interativo (o bloco de erro pode ter,
porque nao e interativo); nenhum import de `AdminNav` ou `SimplesTopBar`; nenhum `<dialog>`, portal ou
focus trap; nenhuma trava de "role ate o fim para habilitar o botao".
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "amber" "app/[slug]/admin/aceitar-termos/_components/"` retorna 0 em ambos os arquivos
    - `grep -c "dangerouslySetInnerHTML" "app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx"` retorna 0
    - `grep -c "whitespace-pre-wrap" "app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx"` retorna 1
    - `grep -c "new Date" "app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx"` retorna 0
    - `grep -c "usuarioId" "app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx"` retorna 0
    - `grep -c "Sair da conta" "app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx"` retorna 1
    - `grep -c 'role="region"' "app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx"` retorna 1 e `tabIndex={0}` esta presente na mesma div
    - `grep -cE "font-medium|font-bold" "app/[slug]/admin/aceitar-termos/_components/"*.tsx` retorna 0
    - `grep -c "useFormStatus" "app/[slug]/admin/aceitar-termos/_components/aceitar-button.tsx"` retorna 1 e `useActionState` nao aparece
    - `grep -rcE "AdminNav|SimplesTopBar" "app/[slug]/admin/aceitar-termos/"` retorna 0
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0
  </acceptance_criteria>
  <done>A tela usa o shell reconhecivel de `bloqueado`, em slate com glifo de documento, com o texto escapado por React e a saida de logout sempre disponivel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: page.tsx com guarda simetrica dos 4 estados + page.test.ts</name>
  <files>app/[slug]/admin/aceitar-termos/page.tsx, app/[slug]/admin/aceitar-termos/page.test.ts</files>
  <read_first>
    - app/[slug]/admin/bloqueado/page.tsx (arquivo inteiro, 66 linhas: o docblock das linhas 1-15 sobre a posicao na arvore, a guarda simetrica das 36-51 com os `redirect` fora de qualquer `try`, e o fallback de branding + bind das actions das 55-65)
    - app/[slug]/admin/bloqueado/page.test.ts (arquivo inteiro, 165 linhas: `// @vitest-environment node`, o `vi.hoisted` com `redirectMock` que LANCA `REDIRECT:<url>`, os `vi.mock` de `next/navigation`, `@/lib/session` e `@/lib/auth-guard`, e a assercao `rejects.toThrow`)
    - app/services/empresa.service.ts (assinatura de `findHeaderData`, linhas 345+)
    - lib/termo-vigente.ts, app/[slug]/admin/aceitar-termos/actions.ts (Task 1), _components/aceite-card.tsx (Task 2)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "## Maquina de Estados do Gate" — a tabela de 5 estados x 3 rotas e a especificacao desta task
  </read_first>
  <behavior>
    - Sem sessao, ou sessao de outro slug: redirect para `/{slug}/admin/login`, e `revalidarConta` nao chega a ser chamado
    - `revalidarConta` devolvendo `null`: redirect para `/{slug}/admin`
    - Empresa bloqueada (com ou sem termos pendentes): redirect para `/{slug}/admin/bloqueado`
    - `termosPendentes` falso: redirect para `/{slug}/admin`
    - `termosPendentes` verdadeiro e nao bloqueada: renderiza o `AceiteCard` com o termo vigente
    - Sem termo vigente (`termoVigente()` devolvendo `null`) mas com `termosPendentes` falso: redirect para `/{slug}/admin` — a tela e inalcancavel sem termo, por construcao
    - `?erro=termos-atualizados` na URL: card renderiza com `erro` verdadeiro
  </behavior>
  <action>
Criar `app/[slug]/admin/aceitar-termos/page.tsx` como espelho 1:1 de
`app/[slug]/admin/bloqueado/page.tsx`, com o predicado trocado.

Docblock de cabecalho adaptado palavra por palavra do de `bloqueado/page.tsx:1-15`: rota de aceite
obrigatorio de termos, deliberadamente FORA do grupo `(protected)`; o layout daquele grupo chama a DAL
de sessao, e e ela (a partir do plano 06-04) que manda o usuario com termos pendentes para ca; se esta
pagina morasse dentro do grupo, entrar nela dispararia o mesmo guard que acabou de mandar o usuario
para ela — `ERR_TOO_MANY_REDIRECTS`; a posicao deste arquivo na arvore E a mitigacao, e mover a pasta
para dentro de `(protected)` reintroduz o bug sem alterar uma linha de codigo. Acrescentar que
`proxy.ts` ja exige cookie valido para qualquer `/:slug/admin/:path*` que nao seja o login, entao esta
rota herda o pre-filtro otimista de graca — ele nao toca no banco e NAO substitui a guarda simetrica.
E acrescentar um paragrafo especifico desta rota: `bloqueado/page.tsx` NAO checa termos, e essa
ausencia e REQUISITO e nao esquecimento — se ela checasse, o estado (bloqueado, pendente) viraria
`/bloqueado` -> `/aceitar-termos` -> `/bloqueado` indefinidamente. Este arquivo NAO deve motivar
nenhuma edicao naquele.

Corpo da page, `async`, recebendo `params: Promise<{ slug: string }>` e
`searchParams: Promise<{ erro?: string }>`:
1. `const session = await getVerifiedSession(); if (!session || session.empresaSlug !== slug) redirect(\`/${slug}/admin/login\`);`
2. `const conta = await revalidarConta(session.sub, session.empresaId); if (!conta) redirect(\`/${slug}/admin\`);`
3. `if (acessoBloqueado(conta.statusAcesso)) redirect(\`/${slug}/admin/bloqueado\`);`
4. `if (!conta.termosPendentes) redirect(\`/${slug}/admin\`);`
5. `const vigente = await termoVigente(); if (!vigente) redirect(\`/${slug}/admin\`);`
6. `const empresa = await empresaService.findHeaderData(session.empresaId);`
7. renderizar `AceiteCard` com `primaryColor={empresa?.primaryColor ?? "#18181b"}`,
   `accentColor={empresa?.accentColor ?? "#3f3f46"}`, o termo (com `publicadoEm` ja formatado como
   `dd/mm/aaaa` NO SERVIDOR, usando o helper de data existente do projeto se houver),
   `erro={erro === "termos-atualizados"}`, `aceitarAction={aceitarTermos.bind(null, slug)}` e
   `logoutAction={logout.bind(null, slug)}`.

`logout` e a action EXISTENTE de `app/[slug]/admin/_lib/auth-actions.ts:50-53` — importar e ligar, nao
escrever uma nova. Ela nao chama `requireAdminSession`, entao funciona a partir de uma sessao gateada,
e e essa propriedade que satisfaz o criterio de sucesso #4 do roadmap ("consegue sair sem ficar preso
em um loop"). Comentar isso na linha do bind.

Todos os `redirect()` ficam fora de qualquer `try`. A ordem das cinco condicoes e a especificacao da
maquina de estados e nao pode ser reordenada.

Criar `app/[slug]/admin/aceitar-termos/page.test.ts` copiando literalmente o setup de
`bloqueado/page.test.ts` (o `// @vitest-environment node`, o `vi.hoisted` com o `redirectMock` que
lanca `REDIRECT:<url>`, e os `vi.mock` de `next/navigation`, `@/lib/session`, `@/lib/auth-guard`,
`@/app/services/empresa.service` e `@/lib/termo-vigente`), cobrindo os sete comportamentos do bloco
de comportamento. Para o caso "sem sessao", afirmar tambem `expect(revalidarContaMock).not
.toHaveBeenCalled()` — a ordem importa e essa e a assercao que a prova. Ao extrair props do resultado
renderizado, extrair apenas os props VISUAIS: as actions sao funcoes ligadas e portanto sempre
distintas, como o teste de `bloqueado` ja registra.

Acrescentar um caso final de REGRESSAO DA MAQUINA DE ESTADOS, com comentario explicativo: afirmar por
leitura de arquivo que `app/[slug]/admin/bloqueado/page.tsx` NAO contem a string `termosPendentes`. Um
dia alguem vai achar que falta "consistencia" ali, e este teste e o que transforma esse impulso num
build vermelho em vez de num loop de redirect em producao.
  </action>
  <verify>
    <automated>npx vitest run "app/[slug]/admin/aceitar-termos/page.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "app/[slug]/admin/aceitar-termos/page.test.ts"` sai 0 com no minimo 7 casos
    - A pasta `app/[slug]/admin/aceitar-termos/` e IRMA de `(protected)/` e de `bloqueado/` — `ls "app/[slug]/admin/"` lista as tres no mesmo nivel, e nao existe `app/[slug]/admin/(protected)/aceitar-termos`
    - `grep -c "termosPendentes" "app/[slug]/admin/bloqueado/page.tsx"` retorna 0 (proibicao verificada, e ha um teste que a afirma)
    - Em `page.tsx`, a ordem das linhas satisfaz: `getVerifiedSession` antes de `revalidarConta`, `acessoBloqueado` antes de `termosPendentes`
    - `grep -c "logout.bind" "app/[slug]/admin/aceitar-termos/page.tsx"` retorna 1 e nenhuma funcao de logout nova foi definida no diretorio
    - Existe um caso de teste que afirma redirect para `/{slug}/admin/bloqueado` quando bloqueio e termos pendentes coexistem
    - `git diff --stat "app/[slug]/admin/bloqueado/"` esta vazio (a tela da Fase 4 nao foi tocada)
    - `npm run test` sai 0 e `npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>Os 5 estados da maquina (incluindo SUPERADMIN) resolvem sem loop, e a tela renderiza o texto vigente com aceite e logout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (cookie) -> Server Action `aceitarTermos` | Endpoint publico por construcao: nenhum guard cobre uma action fora de `(protected)` |
| `FormData` do browser -> servidor | `termoId` e dado controlado pelo cliente |
| browser -> `/{slug}/admin/aceitar-termos` | Rota fora do grupo protegido; a guarda simetrica da propria page e o unico boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing / Tampering | `aceitarTermos` | mitigate | `usuarioId` derivado de `session.sub`; nenhum input `usuarioId` existe no formulario (restricao verificada por grep no de criterios de aceite das Tasks 1 e 2). Sem isso, qualquer sessao valida gravaria aceite em nome de outro usuario (Pitfall 8) |
| T-06-29 | Spoofing | `aceitarTermos` sem autorizacao propria | mitigate | `getVerifiedSession()` + conferencia de `session.empresaSlug !== slug` antes de qualquer I/O, exatamente como `iniciarPagamento` faz. Docs do Next 16.3.0: Server Actions sao endpoints publicos |
| T-06-07 | Repudiation | TOCTOU entre render e submit | mitigate | `termoId` hidden comparado por IGUALDADE contra `termoVigente()` do servidor; em divergencia, nada e gravado e a tela re-renderiza com o texto NOVO e a copy E1. O valor gravado e sempre `vigente.id`, nunca o recebido |
| T-06-08 | Tampering | XSS pelo texto dos termos | mitigate | Conteudo renderizado como no de texto React (escape automatico) com `whitespace-pre-wrap`; `dangerouslySetInnerHTML` proibido e verificado por grep (D-05) |
| T-06-10 | Tampering | open redirect pos-aceite | mitigate | Destino fixo `/{slug}/admin`; nenhum parametro `?next=` e nenhuma leitura de destino vinda do cliente |
| T-06-26 | Denial of Service | loop de redirect | mitigate | Guarda simetrica com as cinco condicoes na ordem da maquina de estados + proibicao verificada de `termosPendentes` em `bloqueado/page.tsx`, com teste de regressao que le o arquivo |
| T-06-30 | Denial of Service | usuario preso sem conseguir sair | mitigate | `Sair da conta` ligado a action `logout` EXISTENTE, que nao chama `requireAdminSession` e portanto funciona de uma sessao gateada; `proxy.ts` libera `rest === "login"` sem cookie. E o criterio de sucesso #4 do roadmap |
| T-06-04 | Information Disclosure | gate movido para layout | mitigate | A page tem guarda propria e a pasta e irma de `(protected)`; `(protected)/layout.tsx` nao e tocado (Pitfall 3) |
| T-06-31 | Information Disclosure | conteudo do termo no RSC Payload | accept | O texto vigente e publico por natureza (todo usuario precisa le-lo antes de aceitar). O teto de 200000 caracteres do plano 06-03 limita o tamanho do payload |
| T-06-SC | Tampering | npm installs | mitigate | Zero pacotes novos; nenhum componente vindo de registry (`components.json` ausente, shadcn nao inicializado). `git diff package.json` vazio |
</threat_model>

## Artifacts this phase produces

Simbolos e caminhos CRIADOS por este plano:

- Arquivos novos: `app/[slug]/admin/aceitar-termos/page.tsx`, `app/[slug]/admin/aceitar-termos/page.test.ts`, `app/[slug]/admin/aceitar-termos/actions.ts`, `app/[slug]/admin/aceitar-termos/_components/aceite-card.tsx`, `app/[slug]/admin/aceitar-termos/_components/aceitar-button.tsx`
- Segmento de rota novo: `/{slug}/admin/aceitar-termos`
- Exports: `aceitarTermos` (Server Action), `AceiteCard`, `AceitarButton`, `AceiteCardProps`, `AceitarButtonProps`
- Nomes de campo de formulario: `termoId` (hidden input do form de aceite)
- Parametro de query novo: `?erro=termos-atualizados`

<verification>
- `npx vitest run "app/[slug]/admin/aceitar-termos/page.test.ts"` verde com os 4 estados + SUPERADMIN
- `npm run test` verde (suite completa)
- `npx tsc --noEmit` sai 0; `npm run lint` sai 0
- `grep -rc "amber" "app/[slug]/admin/aceitar-termos/"` retorna 0
- `grep -c "termosPendentes" "app/[slug]/admin/bloqueado/page.tsx"` retorna 0
- `git diff --stat "app/[slug]/admin/(protected)/layout.tsx" "app/[slug]/admin/bloqueado/"` vazio
- `git diff package.json` vazio
</verification>

<success_criteria>
- Um ADMIN com termos pendentes abre o admin, cai na tela de aceite, le o texto, clica em `Aceitar e continuar` e volta ao painel funcionando
- O mesmo usuario, se em vez disso clicar em `Sair da conta`, chega ao login e nao volta a ser redirecionado em loop
- Uma empresa bloqueada E com termos pendentes ve a tela de bloqueio e nao entra em loop com a de termos
</success_criteria>

<output>
Create `.planning/phases/06-termos-de-uso-e-aceite/06-05-SUMMARY.md` when done
</output>
