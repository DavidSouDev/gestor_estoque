---
phase: 06-termos-de-uso-e-aceite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260902120000_add_superadmin_role/migration.sql
  - prisma/migrations/20260902120100_add_termos_de_uso/migration.sql
  - prisma/migrations/20260902120200_seed_termo_v1/migration.sql
  - tests/setup/prisma-mock.ts
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03]

must_haves:
  truths:
    - "O enum UserRole aceita o valor SUPERADMIN sem quebrar nenhuma compilacao existente"
    - "Uma versao de termo publicada nunca pode ser apagada enquanto houver um aceite apontando para ela"
    - "Um banco recem-migrado (inclusive o da CI) ja tem a versao 1 dos termos, sem passo de seed"
    - "A suite Vitest existente continua 100% verde depois da mudanca de schema"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "enum UserRole.SUPERADMIN, model TermoDeUso, model AceiteTermo, coluna Usuario.termoAceitoId"
      contains: "model TermoDeUso"
    - path: "prisma/migrations/20260902120000_add_superadmin_role/migration.sql"
      provides: "ALTER TYPE isolado (Pitfall 1)"
      contains: "ADD VALUE"
    - path: "prisma/migrations/20260902120200_seed_termo_v1/migration.sql"
      provides: "v1 dos termos semeada, idempotente"
      contains: "ON CONFLICT"
    - path: "tests/setup/prisma-mock.ts"
      provides: "stub default neutro (termosPendentes === false) para toda a suite"
      contains: "termoAceitoId"
  key_links:
    - from: "prisma/schema.prisma"
      to: "AceiteTermo.termo"
      via: "FK com onDelete: Restrict"
      pattern: "onDelete:\\s*Restrict"
    - from: "tests/setup/prisma-mock.ts"
      to: "prismaMock.termoDeUso.findFirst"
      via: "stub default do beforeEach global"
      pattern: "termoDeUso\\.findFirst"
---

<objective>
Fundacao de dados da Fase 6: o enum `UserRole` ganha `SUPERADMIN` (TERM-02), nascem os dois
modelos de termos (`TermoDeUso` imutavel + `AceiteTermo` append-only, TERM-03), o `Usuario`
ganha a coluna denormalizada `termoAceitoId` (Ponto de Extensao 2 do RESEARCH), e a v1 dos
termos e semeada por migration para que `/registro` funcione num banco limpo (D-12 / TERM-01).

Fecha com o item obrigatorio de Wave 0: o stub default de `tests/setup/prisma-mock.ts` passa a
carregar a combinacao NEUTRA de termos. Sem ele, os ~876 testes autenticados existentes ficariam
vermelhos de uma vez no momento em que o gate da Fase 6 entrar (mesma regressao em massa que a
Fase 2 causou ao adicionar fatos de billing a esse mesmo stub).

Purpose: nenhum outro plano da fase pode comecar antes de o Prisma Client ter os tipos novos.
Este e o unico plano da Wave 1 e todos os outros dependem dele, direta ou transitivamente.
Output: schema migrado, cliente Prisma regenerado, banco de dev com as tres migrations aplicadas
e suite Vitest existente verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-termos-de-uso-e-aceite/06-CONTEXT.md
@.planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md
@.planning/phases/06-termos-de-uso-e-aceite/06-PATTERNS.md
@prisma/schema.prisma
@prisma/migrations/20260901195303_add_worker_diario_causa/migration.sql
@tests/setup/prisma-mock.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelar TermoDeUso, AceiteTermo, UserRole.SUPERADMIN e Usuario.termoAceitoId no schema</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (arquivo inteiro — o `enum UserRole` esta na linha 9, o `model Usuario` na 102, e o comentario normativo de `Empresa.ultimoStatusAuditado` por volta da linha 74 e o precedente literal da coluna denormalizada que este plano cria)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "## Schema Recomendado" (o schema alvo, campo a campo, com a tabela "Decisoes de campo, com o porque")
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "## Ponto de Extensao 2" (justificativa completa da coluna `termoAceitoId` e a divergencia honesta em relacao a `ultimoStatusAuditado`)
  </read_first>
  <action>
Editar `prisma/schema.prisma` (e SOMENTE ele nesta task — nenhuma migration ainda) com quatro mudancas.

(1) `enum UserRole`: acrescentar o valor `SUPERADMIN` depois de `ADMIN`. Comentario de linha registrando que ele entra por migration PROPRIA (Pitfall 1 do RESEARCH) e que e o papel de TERM-02 / D-01.

(2) Novo `model TermoDeUso` com os campos exatos: `id String @id @default(uuid())`; `versao Int @unique`; `conteudo String`; `publicadoEm DateTime @default(now())`; `publicadoPorId String?`; relacao `publicadoPor Usuario? @relation("TermosPublicados", fields: [publicadoPorId], references: [id], onDelete: SetNull)`; relacao inversa `aceites AceiteTermo[]`; `@@index([publicadoEm])`.
JSDoc `///` normativo no modelo, na densidade de `app/services/acesso.service.ts:5-35`, cobrindo tres pontos: (a) DELIBERADAMENTE SEM `empresaId` — e a segunda tabela global do sistema (a primeira excecao ao isolamento por tenant do C-07 e o worker diario), porque os termos sao da PLATAFORMA e dar-lhe `empresaId` permitiria a um ADMIN publicar os proprios termos, que e exatamente o que D-06 proibe; (b) nenhum metodo `update`/`delete` existira em `termo.service.ts` e isso e o contrato de TERM-03 / D-07 — publicar e INSERT, sempre; (c) `versao Int @unique` e o que impede duas publicacoes concorrentes de nascerem com o mesmo numero — a segunda recebe P2002 e vira 409, em vez de duas "v3" diferentes no banco; e o mesmo argumento ja escrito para `EventoWebhookAsaas.eventoId`. Comentario `///` proprio em `conteudo` dizendo que e texto PURO (D-05), nunca Markdown nem HTML, renderizado como no de texto React com `white-space: pre-wrap`. Comentario `///` proprio em `publicadoPorId` dizendo que e nulo APENAS na v1 semeada por migration (que nao tem autor humano) e que `SetNull` e nao `Cascade` porque apagar o usuario que publicou nao pode apagar o documento legal que centenas de pessoas aceitaram.

(3) Novo `model AceiteTermo` com os campos exatos: `id String @id @default(uuid())`; `usuarioId String`; `usuario Usuario @relation(fields: [usuarioId], references: [id], onDelete: Cascade)`; `termoId String`; `termo TermoDeUso @relation(fields: [termoId], references: [id], onDelete: Restrict)`; `aceitoEm DateTime @default(now())`; `@@unique([usuarioId, termoId])`; `@@index([usuarioId, aceitoEm])`.
JSDoc `///` normativo cobrindo: (a) fato append-only, mesma natureza de `AuditoriaAcesso` — escrito uma vez, nunca reescrito, nunca apagado pela aplicacao (TERM-03 / D-07); (b) o escopo e o USUARIO, nao a empresa — PROJECT.md ("status de pagamento e por Empresa; aceite de termos e por Usuario"), e por isso NAO existe `empresaId` aqui; (c) `onDelete: Restrict` no `termo` e a imutabilidade imposta pelo Postgres, nao pela disciplina de quem le o codigo daqui a um ano; (d) `@@unique([usuarioId, termoId])` torna o aceite IDEMPOTENTE — duplo clique, retry de rede e reenvio de formulario colidem em P2002, que o service trata como "ja aceitou"; registrar que isso e uma divergencia CONSCIENTE do padrao de `AuditoriaAcesso` (que nao tem unique) e que a consequencia desejada e um usuario nao poder "reaceitar" a mesma versao.

(4) `model Usuario`: acrescentar `termoAceitoId String?`, `termoAceito TermoDeUso? @relation("UltimoAceite", fields: [termoAceitoId], references: [id], onDelete: SetNull)`, `aceitesTermos AceiteTermo[]` e `termosPublicados TermoDeUso[] @relation("TermosPublicados")`. NAO tocar em `@@unique([empresaId])`, `role`, `ativo` nem em nenhum campo existente.
JSDoc `///` em `termoAceitoId` que e o item mais importante deste arquivo, porque e onde um leitor futuro vai errar: dizer que e BOOKKEEPING para o gate custar 1 query por request e nao 2 (Ponto de Extensao 2 do RESEARCH); que a fonte da verdade auditavel continua sendo `AceiteTermo`; que a coluna tem a MESMA forma de `Empresa.ultimoStatusAuditado` mas regra OPOSTA — `ultimoStatusAuditado` nao pode ser lido para decidir acesso porque e uma derivacao de 4 fatos que mudam sozinhos com a passagem do tempo, enquanto `termoAceitoId` E o fato (um evento discreto que so muda quando alguem aceita) e portanto PODE ser lido para decidir; e que o invariante que mantem isso correto e um ESCRITOR UNICO: so a transacao de aceite escreve, e sempre junto do INSERT em `AceiteTermo`, nunca isolada. Registrar tambem que a coluna NAO esta no allowlist de `usuarioService.update` e nao e gravavel por HTTP.

Alem disso, acrescentar em `AceiteTermo` (ou no cabecalho de `TermoDeUso`) uma nota curta sobre a consequencia aceita A2 do RESEARCH: os aceites desaparecem junto com o `Usuario` por `onDelete: Cascade` herdado de `Empresa`; se um dever de retencao de prova de consentimento aparecer, isso vira migration nova.

Rodar `npx prisma format` e `npx prisma validate` ao final. NAO rodar `migrate` nesta task.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma format --check 2>/dev/null || npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` sai com codigo 0
    - `grep -c "SUPERADMIN" prisma/schema.prisma` retorna >= 1
    - `grep -c "model TermoDeUso" prisma/schema.prisma` retorna 1
    - `grep -c "model AceiteTermo" prisma/schema.prisma` retorna 1
    - `grep -c "onDelete: Restrict" prisma/schema.prisma` retorna >= 1
    - `grep -c "@@unique(\[usuarioId, termoId\])" prisma/schema.prisma` retorna 1
    - `grep -c "termoAceitoId" prisma/schema.prisma` retorna >= 2 (campo + relacao)
    - `grep -n "@@unique(\[empresaId\])" prisma/schema.prisma` continua presente (nenhuma constraint existente removida)
    - `git diff --stat prisma/schema.prisma` mostra apenas adicoes de linha alem do valor de enum — nenhuma linha existente de outro modelo removida
  </acceptance_criteria>
  <done>O schema valida, os dois modelos novos existem com as constraints exatas de TERM-03 (`versao @unique`, `onDelete: Restrict`, `@@unique([usuarioId, termoId])`), e `UserRole` tem dois valores.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Tres migrations versionadas + regeneracao do Prisma Client</name>
  <files>prisma/migrations/20260902120000_add_superadmin_role/migration.sql, prisma/migrations/20260902120100_add_termos_de_uso/migration.sql, prisma/migrations/20260902120200_seed_termo_v1/migration.sql</files>
  <read_first>
    - prisma/migrations/20260901195303_add_worker_diario_causa/migration.sql (o incidente do Pitfall 1 documentado no proprio arquivo — este e o formato de cabecalho a reproduzir)
    - prisma/schema.prisma (estado apos a Task 1)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "### Pitfall 1" e secao "### Decisao da Borda \"Banco Sem Termos\""
    - .github/workflows/tests.yml (confirmar que o job e2e roda `npx prisma migrate deploy` e NENHUM passo de seed — e essa ausencia que obriga a v1 a nascer em migration)
  </read_first>
  <action>
[BLOCKING] Esta task aplica o schema ao banco. Sem ela, `tsc` e `next build` passam (os tipos vem do arquivo de schema, nao do banco vivo) e a fase entra num estado de falso-positivo de verificacao.

Este projeto usa migrations versionadas (`prisma/migrations/` com 10 entradas), NAO `db push`. Sao TRES migrations, nesta ordem exata, e a separacao nao e estilo:

(1) `npx prisma migrate dev --name add_superadmin_role` — mas ANTES de rodar, isolar a mudanca: o schema da Task 1 ja tem tudo. Para que esta primeira migration contenha SOMENTE o `ALTER TYPE`, gere-a com `npx prisma migrate dev --create-only --name add_superadmin_role`, abra o arquivo gerado e REMOVA de la qualquer `CREATE TABLE`, `ALTER TABLE` ou `CREATE INDEX` que o Prisma tenha incluido, deixando apenas `ALTER TYPE "UserRole" ADD VALUE 'SUPERADMIN';`. Cabecalho em comentario SQL reproduzindo o de `20260901195303_add_worker_diario_causa/migration.sql`: esta migration APENAS adiciona o valor ao enum; nao ha e nao pode haver nenhum INSERT/UPDATE que USE esse valor aqui, porque o Prisma Migrate roda cada arquivo dentro de uma transacao e o PostgreSQL recusa usar um valor de enum adicionado dentro da mesma transacao (`ERROR: unsafe use of new value ... of enum type`); qualquer DML que precise do valor novo vai numa migration posterior. Aplicar com `npx prisma migrate dev`.

(2) `npx prisma migrate dev --create-only --name add_termos_de_uso` — o Prisma gera o diff restante: `CREATE TABLE "TermoDeUso"`, `CREATE TABLE "AceiteTermo"`, `ALTER TABLE "Usuario" ADD COLUMN "termoAceitoId"`, os indices unicos (`TermoDeUso_versao_key`, `AceiteTermo_usuarioId_termoId_key`), os indices (`TermoDeUso_publicadoEm_idx`, `AceiteTermo_usuarioId_aceitoEm_idx`) e as quatro FKs. CONFERIR no arquivo gerado que a FK de `AceiteTermo.termoId` saiu com `ON DELETE RESTRICT` e a de `Usuario.termoAceitoId` com `ON DELETE SET NULL` — se o Prisma tiver gerado outra coisa, o schema da Task 1 esta errado; corrija o schema e regenere, nunca edite a clausula na mao. Aplicar com `npx prisma migrate dev`.

(3) `npx prisma migrate dev --create-only --name seed_termo_v1` — o Prisma vai gerar um arquivo VAZIO (nao ha diff de schema). Escrever nele o DML idempotente, uma unica instrucao: `INSERT INTO "TermoDeUso" ("id", "versao", "conteudo", "publicadoEm", "publicadoPorId") VALUES (gen_random_uuid(), 1, '<conteudo>', now(), NULL) ON CONFLICT ("versao") DO NOTHING;`. O `<conteudo>` e um texto de bootstrap em portugues, com quebras de linha reais, comecando pela linha literal `[TEXTO PROVISORIO - substituir publicando uma nova versao via POST /api/termos antes do go-live]` seguida de um paragrafo curto descrevendo o servico. Escapar aspas simples duplicando-as, se houver. Cabecalho em comentario SQL explicando: a CI roda `prisma migrate deploy` e NADA MAIS (verificado em `.github/workflows/tests.yml`), os 9 specs e2e registram pela UI de `/registro`, e sem esta linha um banco limpo deixa o cadastro inoperante e derruba a suite e2e inteira — migration e o unico ponto que todo ambiente atravessa (D-12, resolvido). O comentario deve dizer tambem, explicitamente, que o texto real NAO substitui esta linha por UPDATE: publicar e INSERT sempre (D-07), entao a versao juridica real nasce como v2 pelo endpoint de TERM-02. `ON CONFLICT ("versao") DO NOTHING` torna a reaplicacao inofensiva num banco ja semeado. Aplicar com `npx prisma migrate dev`.

Fechar com `npx prisma generate` para o Prisma Client ganhar `prisma.termoDeUso`, `prisma.aceiteTermo` e `UserRole.SUPERADMIN`.

ESCALACAO (nao autonoma): se qualquer `prisma migrate dev` pedir confirmacao interativa para RESETAR o banco (drift detectado), NAO aceite e NAO rode `prisma migrate reset`. Pare, e reporte ao operador qual drift foi detectado — um reset destroi o banco de desenvolvimento e nenhuma decisao desta fase autoriza isso.
  </action>
  <verify>
    <automated>npx prisma migrate status && npx prisma generate && node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.termoDeUso.findFirst({orderBy:{versao:'desc'}}).then(t=>{if(!t||t.versao!==1)throw new Error('v1 ausente');console.log('v1 OK',t.id);return p.$disconnect()}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma migrate status` reporta que nao ha migrations pendentes ("Database schema is up to date")
    - Existem exatamente 3 diretorios novos em `prisma/migrations/` cujos nomes terminam em `_add_superadmin_role`, `_add_termos_de_uso` e `_seed_termo_v1`
    - `grep -c "ADD VALUE" prisma/migrations/*_add_superadmin_role/migration.sql` retorna 1 e `grep -ci "INSERT INTO" prisma/migrations/*_add_superadmin_role/migration.sql` retorna 0
    - `grep -c "ON DELETE RESTRICT" prisma/migrations/*_add_termos_de_uso/migration.sql` retorna >= 1
    - `grep -c "ON CONFLICT" prisma/migrations/*_seed_termo_v1/migration.sql` retorna 1
    - O script Node do de verificacao automatizada imprime `v1 OK <uuid>` — a linha de termo versao 1 existe no banco de dev
    - `node -e "const{UserRole}=require('@prisma/client');if(UserRole.SUPERADMIN!=='SUPERADMIN')process.exit(1)"` sai 0 (o client regenerado exporta o valor novo)
    - `npx tsc --noEmit` sai com codigo 0
  </acceptance_criteria>
  <done>As tres migrations estao aplicadas no banco de dev, o Prisma Client regenerado expoe `termoDeUso`/`aceiteTermo`/`UserRole.SUPERADMIN`, e a v1 dos termos existe no banco.</done>
</task>

<task type="auto">
  <name>Task 3: Stub default neutro de termos no prisma-mock global (Wave 0 obrigatorio)</name>
  <files>tests/setup/prisma-mock.ts</files>
  <read_first>
    - tests/setup/prisma-mock.ts (arquivo inteiro, 45 linhas — o comentario das linhas 24-30 ja documenta EXATAMENTE esta classe de regressao para os fatos de billing da Fase 2)
    - lib/auth-guard.ts (linhas 51-78: o `select` que a Task do plano 06-04 vai estender com `termoAceitoId`, e que define a forma do objeto que este stub precisa devolver)
    - .planning/phases/06-termos-de-uso-e-aceite/06-RESEARCH.md secao "### Test Framework" (o paragrafo com o aviso "Item de Wave 0 obrigatorio")
  </read_first>
  <action>
Estender o `beforeEach` global de `tests/setup/prisma-mock.ts` com a combinacao NEUTRA de termos — a que produz `termosPendentes === false` sem que nenhum teste existente precise mudar.

Duas mudancas:

(1) No objeto ja devolvido por `prismaMock.usuario.findFirst.mockResolvedValue`, acrescentar a chave `termoAceitoId: "termo-1"` no nivel raiz (irma de `role` e `empresaId`, NAO dentro de `empresa`).

(2) Acrescentar, no mesmo `beforeEach` e DEPOIS do `mockReset`, um stub novo: `prismaMock.termoDeUso.findFirst.mockResolvedValue({ id: "termo-1", versao: 1, conteudo: "Termos de teste.", publicadoEm: new Date("2026-01-01T03:00:00.000Z") } as never)`. O `id` tem que ser exatamente o mesmo string de `termoAceitoId` — e essa igualdade que produz o estado neutro.

Comentario acima do bloco novo, na densidade do comentario de billing que ja esta ali, registrando quatro coisas: (a) a combinacao escolhida e "existe termo vigente E o usuario ja o aceitou", e nao "nao ha termo publicado" (que tambem daria `false` pela assimetria deliberada do RESEARCH) — porque a primeira EXERCITA a comparacao do gate a cada teste, enquanto a segunda a curto-circuita e deixaria a regra sem cobertura de fundo; (b) a data e um literal fixo pelo mesmo motivo ja registrado para `trialFim` — varios testes daqui usam `vi.useFakeTimers()` e um valor relativo ao relogio tornaria a suite sensivel ao tempo; (c) sem estes dois stubs, no instante em que o plano 06-04 acrescentar `termosPendentes` a `ContaAtiva`, TODO teste autenticado existente passa a receber redirect/403 de uma vez — a mesma regressao em massa que a Fase 2 causou neste mesmo arquivo; (d) testes que querem o caminho de termos pendentes sobrescrevem localmente, trocando `termoAceitoId` para `null` ou para um id diferente do vigente.

Nao alterar mais nada do arquivo: `mockReset`, o `vi.mock` de `@/lib/prisma` e a exportacao de `prismaMock` ficam como estao.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` sai com codigo 0 e o total de testes passando e >= 876 (o numero registrado em STATE.md ao fim da Fase 4); nenhum teste novo falha e nenhum teste existente foi alterado neste plano
    - `grep -c "termoAceitoId" tests/setup/prisma-mock.ts` retorna 1
    - `grep -c "termoDeUso.findFirst" tests/setup/prisma-mock.ts` retorna 1
    - O valor de `id` no stub de `termoDeUso.findFirst` e identico ao valor de `termoAceitoId` no stub de `usuario.findFirst` (ambos `"termo-1"`) — verificavel por `grep -c '"termo-1"' tests/setup/prisma-mock.ts` retornando 2
    - `git diff --stat tests/setup/prisma-mock.ts` mostra apenas adicoes (nenhuma linha existente removida)
    - `npx tsc --noEmit` sai com codigo 0
  </acceptance_criteria>
  <done>A suite Vitest inteira continua verde com os tipos novos do Prisma Client, e o stub global entrega o estado neutro de termos para todo teste autenticado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador CLI -> banco (migrations) | DDL e DML versionados entram no banco fora do runtime da aplicacao |
| aplicacao -> tabelas de termos | Toda leitura/escrita futura de termos atravessa as constraints definidas aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | `AceiteTermo.termoId` | mitigate | FK `onDelete: Restrict` no schema — o Postgres recusa apagar uma versao de termo que alguem aceitou; a imutabilidade de TERM-03 e DDL, nao disciplina de codigo |
| T-06-16 | Tampering | `TermoDeUso.versao` | mitigate | `versao Int @unique` — duas publicacoes concorrentes nao podem nascer com o mesmo numero; a segunda recebe P2002 |
| T-06-17 | Repudiation | `AceiteTermo` | mitigate | `@@unique([usuarioId, termoId])` + tabela append-only sem metodo de escrita fora da transacao de aceite; nenhum `update`/`delete` sera exposto (contrato verificado por gate de grep no plano 06-08) |
| T-06-13 | Denial of Service | `/registro` num banco sem termos | mitigate | v1 semeada por migration (D-12): a CI roda `prisma migrate deploy` e nenhum passo de seed; sem isso o funil de aquisicao inteiro fica inoperante ate alguem rodar um curl |
| T-06-18 | Denial of Service | migration de enum | mitigate | `ALTER TYPE ... ADD VALUE` numa migration ISOLADA (Pitfall 1); qualquer DML que use `SUPERADMIN` vai depois — o Postgres recusa usar o valor dentro da mesma transacao |
| T-06-19 | Elevation of Privilege | `Usuario.termoAceitoId` | mitigate | Coluna fora do allowlist de `usuarioService.create`/`update` (verificado no RESEARCH: os DTOs montam `data` campo a campo) — nao e gravavel por HTTP; escritor unico e a transacao de aceite |
| T-06-15 | Denial of Service | rate limiting no publish | accept | Um unico chamador humano e nenhuma infra de rate limiting no projeto; registrado como aceito conscientemente no RESEARCH |
| T-06-SC | Tampering | npm installs | mitigate | Esta fase instala ZERO pacotes; `git diff package.json` vazio ao fim deste plano. A Package Legitimacy Audit do RESEARCH esta vazia (nenhum `[ASSUMED]`, nenhum `[SUS]`), entao nao ha checkpoint de legitimidade a disparar |
</threat_model>

## Artifacts this phase produces

Simbolos e caminhos CRIADOS por este plano (nao existem no codebase antes dele):

- Valor de enum: `UserRole.SUPERADMIN`
- Modelos Prisma: `TermoDeUso`, `AceiteTermo`
- Campos Prisma: `TermoDeUso.id`, `TermoDeUso.versao`, `TermoDeUso.conteudo`, `TermoDeUso.publicadoEm`, `TermoDeUso.publicadoPorId`, `TermoDeUso.publicadoPor`, `TermoDeUso.aceites`, `AceiteTermo.id`, `AceiteTermo.usuarioId`, `AceiteTermo.usuario`, `AceiteTermo.termoId`, `AceiteTermo.termo`, `AceiteTermo.aceitoEm`, `Usuario.termoAceitoId`, `Usuario.termoAceito`, `Usuario.aceitesTermos`, `Usuario.termosPublicados`
- Nomes de relacao Prisma: `"UltimoAceite"`, `"TermosPublicados"`
- Acessores do Prisma Client: `prisma.termoDeUso`, `prisma.aceiteTermo`
- Constraints/indices: `TermoDeUso_versao_key`, `AceiteTermo_usuarioId_termoId_key`, `TermoDeUso_publicadoEm_idx`, `AceiteTermo_usuarioId_aceitoEm_idx`
- Arquivos novos: `prisma/migrations/20260902120000_add_superadmin_role/migration.sql`, `prisma/migrations/20260902120100_add_termos_de_uso/migration.sql`, `prisma/migrations/20260902120200_seed_termo_v1/migration.sql`

Nota para a verificacao de drift: `prismaMock.termoDeUso` e `prismaMock.aceiteTermo` passam a existir por derivacao de tipo (`mockDeep<PrismaClient>`), sem declaracao explicita em `tests/setup/prisma-mock.ts`.

<verification>
- `npx prisma validate` verde
- `npx prisma migrate status` sem migrations pendentes
- `npx prisma generate` sem erro; `UserRole.SUPERADMIN` disponivel no client
- `npm run test` verde (suite existente intacta, >= 876 testes)
- `npx tsc --noEmit` sai 0
- `npm run lint` sai 0
- `git diff package.json` vazio (zero pacotes nesta fase)
</verification>

<success_criteria>
- Um banco limpo que rode `npx prisma migrate deploy` termina com exatamente uma linha em `TermoDeUso` (versao 1) e a suite e2e continua podendo registrar pela UI
- Uma tentativa de `DELETE FROM "TermoDeUso"` numa versao com aceite e recusada pelo Postgres (FK Restrict)
- Nenhum teste existente precisou ser editado para continuar verde
</success_criteria>

<output>
Create `.planning/phases/06-termos-de-uso-e-aceite/06-01-SUMMARY.md` when done
</output>

