---
phase: 04-aplica-o-do-bloqueio
plan: 07
type: execute
wave: 3
depends_on: ["04-01", "04-05"]
files_modified:
  - app/[slug]/admin/(protected)/_components/aviso-carencia.tsx
  - app/[slug]/admin/(protected)/_components/aviso-carencia.test.tsx
  - app/[slug]/admin/(protected)/layout.tsx
autonomous: true
requirements: [ACC-01]
user_setup: []

must_haves:
  truths:
    - "Durante a carência, um banner em destaque aparece em TODAS as telas do admin, nos dois modos de interface"
    - "O banner mostra os dias restantes e um botão 'Pagar agora' que aciona o mesmo caminho de pagamento da tela de bloqueio"
    - "O banner não aparece em TRIAL, EM_DIA, VITALICIO nem BLOQUEADO/CANCELADO"
    - "O banner nunca aparece no catálogo público"
    - "O componente não lê o relógio: os dias restantes são calculados fora dele"
  artifacts:
    - path: "app/[slug]/admin/(protected)/_components/aviso-carencia.tsx"
      provides: "banner de carência (primeiro padrão de aviso do projeto)"
      exports: ["AvisoCarencia"]
    - path: "app/[slug]/admin/(protected)/layout.tsx"
      provides: "renderização do banner acima de {children} nos DOIS branches"
      contains: "AvisoCarencia"
  key_links:
    - from: "app/[slug]/admin/(protected)/layout.tsx"
      to: "lib/auth-guard.ts"
      via: "revalidarConta para ler statusAcesso e carenciaAte (mesma query do request, React.cache)"
      pattern: "revalidarConta"
    - from: "app/[slug]/admin/(protected)/layout.tsx"
      to: "lib/avaliar-acesso.ts"
      via: "diasRestantesDeCarencia"
      pattern: "diasRestantesDeCarencia"
    - from: "app/[slug]/admin/(protected)/layout.tsx"
      to: "app/[slug]/admin/_lib/assinatura-actions.ts"
      via: "iniciarPagamento.bind(null, slug)"
      pattern: "iniciarPagamento"
---

<objective>
Entregar o banner de carência (ACC-01) no único ponto que envolve 100% das telas protegidas — o
`(protected)/layout.tsx` — cobrindo os DOIS branches de render (SIMPLES e COMPLETO), e nunca o
catálogo público.

Purpose: durante a carência o catálogo continua totalmente funcional (D-03) e os clientes da loja
continuam comprando; o aviso é estritamente interno ao admin (D-02). O banner é o primeiro padrão de
aviso/alerta do projeto — não existe `Banner`/`Alert`/`Aviso` reutilizável hoje —, então esta fase
estabelece a forma, mas sem generalizar a partir de n=1.

Output: `AvisoCarencia` e sua renderização acima de `{children}` nos dois branches do layout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-aplica-o-do-bloqueio/04-CONTEXT.md
@.planning/phases/04-aplica-o-do-bloqueio/04-RESEARCH.md
@.planning/phases/04-aplica-o-do-bloqueio/04-PATTERNS.md
@.planning/phases/04-aplica-o-do-bloqueio/04-UI-SPEC.md
@.planning/phases/04-aplica-o-do-bloqueio/04-01-SUMMARY.md
@.planning/phases/04-aplica-o-do-bloqueio/04-05-SUMMARY.md

@app/[slug]/admin/(protected)/layout.tsx
@lib/auth-guard.ts
</context>

## Artifacts this phase produces (deste plano)

| Símbolo | Arquivo | Tipo |
|---------|---------|------|
| `AvisoCarencia` | `app/[slug]/admin/(protected)/_components/aviso-carencia.tsx` | server component exportado (arquivo NOVO) |
| `AvisoCarenciaProps` (`diasRestantes: number`, `pagarAction: () => Promise<void>`) | `app/[slug]/admin/(protected)/_components/aviso-carencia.tsx` | interface exportada |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Componente AvisoCarencia</name>

  <files>app/[slug]/admin/(protected)/_components/aviso-carencia.tsx, app/[slug]/admin/(protected)/_components/aviso-carencia.test.tsx</files>

  <read_first>
    - `app/[slug]/admin/_components/login-form.tsx` linhas 49-66 — o bloco de aviso inline cuja estrutura (`flex items-center gap-2 rounded-xl border ... px-3 py-2.5` + `<svg>` inline + `<span>`) é o esqueleto a re-tintar em âmbar
    - `app/[slug]/admin/_components/login-form.test.tsx` — analog do teste de componente (jsdom default, sem pragma)
    - `app/[slug]/admin/_components/pagar-button.tsx` — `PagarButton`, criado no plano 04-05; a variante a usar aqui é `banner`
    - `app/[slug]/admin/(protected)/_components/admin-nav.tsx` linhas 114-124 — precedente de `<form action={...}>` envolvendo o botão
    - `.planning/phases/04-aplica-o-do-bloqueio/04-UI-SPEC.md` §Component Contracts → 1. `AvisoCarencia` (estrutura completa e binding rules), §Copywriting Contract → "Grace banner copy (ACC-01)" (as três variantes de cópia), §Color (rampa âmbar), §Iconography (o `path d` do triângulo de aviso), §Accessibility (`role="status"`, nunca `role="alert"`)
    - `.planning/phases/04-aplica-o-do-bloqueio/04-RESEARCH.md` §Pitfall 4 e §Pitfall 5
  </read_first>

  <behavior>
    - `diasRestantes = 5` → corpo contém `Seu acesso vence em 5 dias. Depois disso, o painel e o catálogo da sua loja saem do ar.`
    - `diasRestantes = 2` → `...vence em 2 dias...`
    - `diasRestantes = 1` → `Seu acesso vence amanhã. Depois disso, o painel e o catálogo da sua loja saem do ar.`
    - `diasRestantes = 0` → `Seu acesso vence hoje. Depois disso, o painel e o catálogo da sua loja saem do ar.`
    - Título sempre `Pagamento pendente`
    - O elemento raiz tem `role="status"` e NÃO `role="alert"`
    - Renderiza um `<form>` com o `pagarAction` recebido e um botão `Pagar agora`
    - Não existe botão de fechar, dispensar ou adiar
  </behavior>

  <action>
    Criar `app/[slug]/admin/(protected)/_components/aviso-carencia.tsx` (arquivo NOVO, server
    component — sem `"use client"`; o único pedaço interativo é o `PagarButton`, que já é client).

    - `export interface AvisoCarenciaProps { diasRestantes: number; pagarAction: () => Promise<void>; }`
    - `export function AvisoCarencia({ diasRestantes, pagarAction }: AvisoCarenciaProps)`.
    - Estrutura exatamente como a UI-SPEC §Component Contracts → 1:
      `<section role="status" className="mb-6 flex flex-wrap items-center gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-4">`
      contendo o `<svg className="h-5 w-5 shrink-0 text-amber-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">`
      com o triângulo de aviso (`d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"`,
      `strokeLinecap="round" strokeLinejoin="round" strokeWidth={2}`), depois
      `<div className="min-w-0 flex-1">` com `<p className="text-xl/[1.2] font-semibold text-amber-900">Pagamento pendente</p>`
      e `<p className="mt-1 text-sm/[1.5] text-amber-800">{corpo}</p>`, e por fim
      `<form action={pagarAction} className="w-full sm:w-auto"><PagarButton variant="banner" /></form>`.
    - Selecionar o corpo por uma função local pura sobre `diasRestantes`, com os três casos da
      UI-SPEC (>= 2 / 1 / 0). As strings são travadas pela UI-SPEC — não parafrasear, não trocar a
      pontuação, não remover a menção ao catálogo (ela é verdadeira por ACC-03 e é o motivador
      honesto mais forte disponível).
    - PROIBIDO neste arquivo: `new Date()`, `Date.now()`, `toLocaleDateString`, qualquer aritmética de
      fuso, qualquer `useState`, qualquer `localStorage`, qualquer botão de fechar. Comentar por quê:
      manter o componente uma função pura de um número é o que o torna testável sem fake timers
      (Pitfall 5), e um controle de dispensar derrota o ACC-01, que exige o banner em *toda* tela.
    - Comentário sobre `role="status"` e não `role="alert"`: é um aviso persistente; interromper o
      leitor de tela a cada page load seria hostil.
    - Comentário sobre a limitação aceita (Pitfall 4 / A3): por partial rendering, layouts não
      re-renderizam em navegação client-side, então `diasRestantes` pode ficar stale dentro de uma
      sessão. É aceitável porque o boundary de autorização é o `requireAdminSession` de cada page, e
      porque o `successUrl` do Asaas é uma navegação de documento completa de volta para
      `/{slug}/admin`, que re-renderiza o layout inteiro. NÃO acrescentar polling nem timer de
      cliente para "corrigir" isso.

    Criar `app/[slug]/admin/(protected)/_components/aviso-carencia.test.tsx` (jsdom default) no padrão
    de `login-form.test.tsx`, cobrindo os oito casos de `<behavior>`. Para o `pagarAction`, passar um
    `vi.fn()`. Afirmar a ausência do controle de dispensar com
    `expect(screen.queryByRole("button", { name: /fechar|dispensar/i })).toBeNull()`.
  </action>

  <verify>
    <automated>npx vitest run "app/[slug]/admin/(protected)/_components/aviso-carencia.test.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run "app/[slug]/admin/(protected)/_components/aviso-carencia.test.tsx"` sai com código 0
    - `grep -n "new Date()\|Date.now()\|toLocaleDateString\|useState\|localStorage" "app/[slug]/admin/(protected)/_components/aviso-carencia.tsx"` não retorna nenhuma linha
    - `grep -c "role=\"status\"" "app/[slug]/admin/(protected)/_components/aviso-carencia.tsx"` retorna 1
    - `grep -c "role=\"alert\"" "app/[slug]/admin/(protected)/_components/aviso-carencia.tsx"` retorna 0
    - `grep -c "Pagamento pendente" "app/[slug]/admin/(protected)/_components/aviso-carencia.tsx"` retorna 1
    - `grep -c "vence hoje\|vence amanhã\|vence em" "app/[slug]/admin/(protected)/_components/aviso-carencia.tsx"` retorna 3
    - `npm run lint` sai com código 0
  </acceptance_criteria>

  <done>Existe um banner de carência que é uma função pura de um número, com as três variantes de cópia travadas pela UI-SPEC e sem controle de dispensar.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar o banner nos dois branches do layout protegido</name>

  <files>app/[slug]/admin/(protected)/layout.tsx</files>

  <read_first>
    - `app/[slug]/admin/(protected)/layout.tsx` — arquivo a modificar; as 44 linhas inteiras. Os DOIS `return` renderizam `<main className="flex-1 p-6">{children}</main>`; o banner entra acima de `{children}` nos dois, dentro do `<main>`
    - `lib/auth-guard.ts` linhas 9-22 e 48-49 — `ContaAtiva` já carrega `statusAcesso` e `carenciaAte`; `revalidarConta` é `React.cache`, então chamá-la de novo aqui não custa query (o comentário de `lib/api-auth.ts:20-24` diz isso explicitamente)
    - `lib/avaliar-acesso.ts` — `diasRestantesDeCarencia`, criada no plano 04-01
    - `app/[slug]/admin/_lib/assinatura-actions.ts` — `iniciarPagamento`, criada no plano 04-05
    - `app/[slug]/admin/(protected)/_components/aviso-carencia.tsx` — criado na Task 1
    - `.planning/phases/04-aplica-o-do-bloqueio/04-RESEARCH.md` §Code Examples → "Ler o status vivo dentro de um Server Component"
  </read_first>

  <action>
    Em `app/[slug]/admin/(protected)/layout.tsx`:

    1. Após `const session = await requireAdminSession(slug);`, acrescentar
       `const conta = await revalidarConta(session.sub, session.empresaId);`. Comentar que isso NÃO
       custa uma segunda query: `requireAdminSession` já chamou `revalidarConta` neste mesmo request
       e o `React.cache` devolve o mesmo resultado. Comentar também que `conta` nunca é `null` aqui —
       `requireAdminSession` já teria redirecionado —, mas o acesso é feito com optional chaining por
       segurança de tipos.
    2. Calcular, antes dos dois `return`:
       `const emCarencia = conta?.statusAcesso === StatusAcesso.CARENCIA && conta.carenciaAte !== null;`
       e `const diasRestantes = emCarencia ? diasRestantesDeCarencia(conta.carenciaAte, new Date()) : 0;`
       O `new Date()` mora AQUI, no chamador impuro, e é injetado na função pura — mesmo contrato de
       relógio de `revalidarConta`/`avaliarAcesso` (T-02-13). Nunca dentro do componente.
    3. Montar `const bannerCarencia = emCarencia ? <AvisoCarencia diasRestantes={diasRestantes}
       pagarAction={iniciarPagamento.bind(null, slug)} /> : null;` — usando o mesmo formato
       `.bind(null, slug)` que `logoutAction={logout.bind(null, slug)}` já usa nas linhas 25 e 39.
    4. Renderizar `{bannerCarencia}` imediatamente acima de `{children}` DENTRO do `<main>`, nos DOIS
       `return` (branch SIMPLES e branch COMPLETO). Cobrir os dois é requisito do D-02: ACC-01 exige
       o banner em *todas* as telas do admin, e o modo de interface é escolha do tenant.
    5. NÃO transformar este layout em boundary de autorização: ele não pode renderizar a tela de
       bloqueio no lugar de `{children}`. Segmentos filhos continuam executando e continuam no RSC
       Payload mesmo assim, e layouts não re-renderizam em navegação client-side. Acrescentar um
       comentário registrando isso, citando `node_modules/next/dist/docs/01-app/02-guides/authentication.md`
       e T-04-06. O bloqueio é trabalho do `requireAdminSession` (plano 04-06).
    6. NÃO importar `AvisoCarencia` em nenhum arquivo sob `app/[slug]/(catalogo)` — D-02; o gate de
       grep do plano 04-09 prova isso.
    7. Importar `StatusAcesso` de `@prisma/client`, `revalidarConta` de `@/lib/auth-guard`,
       `diasRestantesDeCarencia` de `@/lib/avaliar-acesso`, `iniciarPagamento` de
       `../_lib/assinatura-actions` (mesmo caminho relativo que `logout` já usa) e `AvisoCarencia` de
       `./_components/aviso-carencia`.
  </action>

  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit` e `npm run lint` saem com código 0
    - `grep -c "bannerCarencia" "app/[slug]/admin/(protected)/layout.tsx"` retorna 3 (1 definição + 2 usos, um por branch)
    - `grep -c "{children}" "app/[slug]/admin/(protected)/layout.tsx"` retorna 2 (os dois branches continuam existindo)
    - `grep -c "diasRestantesDeCarencia" "app/[slug]/admin/(protected)/layout.tsx"` retorna 2 (import + uso)
    - `grep -rn "AvisoCarencia\|aviso-carencia" "app/[slug]/(catalogo)"` não retorna nenhuma linha
    - `grep -n "Bloqueado\|bloqueado" "app/[slug]/admin/(protected)/layout.tsx" | grep -v "^\s*[0-9]*:\s*\(//\|\*\)"` não retorna nenhuma linha de código (o layout não é boundary de bloqueio)
  </acceptance_criteria>

  <done>O banner aparece nos dois modos de interface, acima de todo conteúdo do admin, e não existe nenhuma referência a ele sob o catálogo público.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout do admin → UI do tenant | superfície autenticada; o banner é advisory, não boundary |
| catálogo público → qualquer aviso de cobrança | fronteira que NÃO pode ser cruzada (D-02/D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | `(protected)/layout.tsx` | mitigate | O layout hospeda apenas UI. Comentário normativo proibindo substituir `{children}` por uma tela de bloqueio; o gate real fica em `requireAdminSession` (plano 04-06) |
| T-04-19 | Information Disclosure | catálogo público | mitigate | `AvisoCarencia` nunca importado sob `app/[slug]/(catalogo)`; gate de grep no acceptance criteria e no script do plano 04-09 |
| T-04-20 | Tampering | contagem regressiva | mitigate | `diasRestantesDeCarencia` é a única aritmética autorizada e vive em `lib/avaliar-acesso.ts` com relógio injetado; o componente é proibido de ler o relógio (gate de grep) |
| T-04-08 | Elevation of Privilege | leitura de status no layout | mitigate | `revalidarConta` via `React.cache` (por request); nenhum `unstable_cache`/`'use cache'` |
| T-04-SC | Tampering | instalação de pacotes | mitigate | Nenhum pacote novo: sem biblioteca de banner/toast (UI-SPEC §Registry Safety); SVG inline |
</threat_model>

<verification>
```bash
npx vitest run "app/[slug]/admin/(protected)/_components/aviso-carencia.test.tsx"
npm test
npx tsc --noEmit
npm run lint
grep -rn "AvisoCarencia\|aviso-carencia" "app/[slug]/(catalogo)"   # tem que sair vazio
git diff --stat package.json                                       # tem que sair vazio
```
</verification>

<success_criteria>
- Durante a carência, o banner aparece nos dois branches de render do layout protegido
- O banner mostra "vence hoje" / "vence amanhã" / "vence em N dias" corretamente
- O CTA aciona a mesma Server Action que a tela de bloqueio
- Nenhuma referência ao banner sob `app/[slug]/(catalogo)`
- O componente não lê o relógio
</success_criteria>

<output>
Create `.planning/phases/04-aplica-o-do-bloqueio/04-07-SUMMARY.md` when done
</output>
