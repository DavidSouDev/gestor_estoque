---
phase: 04-aplica-o-do-bloqueio
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-05"]
files_modified:
  - lib/session.ts
  - lib/session.test.ts
  - lib/api-auth.ts
  - lib/api-auth.test.ts
  - app/api/assinaturas/checkout/route.ts
  - app/api/assinaturas/checkout/route.test.ts
autonomous: true
requirements: [ACC-02, ACC-04]
user_setup: []

must_haves:
  truths:
    - "Qualquer navegação no admin de uma empresa bloqueada cai na tela de bloqueio, sem editar nenhum dos 59 call sites"
    - "Qualquer chamada a /api/* autenticada de uma empresa bloqueada é rejeitada com um status próprio, sem editar nenhum dos 29 handlers"
    - "O endpoint de checkout é o ÚNICO que opta por sair da guarda"
    - "O mesmo token que é rejeitado agora passa a ser aceito no request seguinte assim que acessoAte avança — sem novo login e sem esperar cache"
    - "TRIAL, EM_DIA, CARENCIA e VITALICIO continuam navegando e chamando a API normalmente"
  artifacts:
    - path: "lib/session.ts"
      provides: "gate de navegação dentro de requireAdminSession"
      contains: "acessoBloqueado"
    - path: "lib/api-auth.ts"
      provides: "OpcoesDeAuth com permitirEmpresaBloqueada e o gate de API"
      exports: ["requireAuth", "AuthError", "OpcoesDeAuth"]
    - path: "app/api/assinaturas/checkout/route.ts"
      provides: "único call site do opt-out"
      contains: "permitirEmpresaBloqueada"
  key_links:
    - from: "lib/session.ts"
      to: "app/[slug]/admin/bloqueado/page.tsx"
      via: "redirect(`/${slug}/admin/bloqueado`)"
      pattern: "admin/bloqueado"
    - from: "lib/api-auth.ts"
      to: "lib/avaliar-acesso.ts"
      via: "acessoBloqueado(conta.statusAcesso)"
      pattern: "acessoBloqueado"
    - from: "app/api/assinaturas/checkout/route.ts"
      to: "lib/api-auth.ts"
      via: "requireAuth(request, { permitirEmpresaBloqueada: true })"
      pattern: "permitirEmpresaBloqueada: true"
---

<objective>
Ligar os dois gates autoritativos, nos dois choke points do DAL que já existem: `requireAdminSession`
(59 chamadas em 19 arquivos de page/layout/action) e `requireAuth` (43 chamadas, 29 handlers que já
propagam `error.status` genericamente). Nenhum chamador é editado — esse é literalmente o critério
da Claude's Discretion do 04-CONTEXT.md.

Purpose: um layout NÃO é boundary de autorização. A documentação do Next 16.3.0 empacotada em
`node_modules/next/dist/docs/01-app/02-guides/authentication.md` é textual: segmentos filhos
continuam executando e continuam aparecendo no RSC Payload mesmo que o layout renderize outra coisa,
e layouts não re-renderizam em navegação client-side. A guarda tem que morar no DAL.

Output: `requireAdminSession` redirecionando para `/[slug]/admin/bloqueado`; `requireAuth` lançando
`AuthError` com status de assinatura suspensa; `OpcoesDeAuth.permitirEmpresaBloqueada` com
exatamente um consumidor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-aplica-o-do-bloqueio/04-CONTEXT.md
@.planning/phases/04-aplica-o-do-bloqueio/04-RESEARCH.md
@.planning/phases/04-aplica-o-do-bloqueio/04-PATTERNS.md
@.planning/phases/04-aplica-o-do-bloqueio/04-01-SUMMARY.md
@.planning/phases/04-aplica-o-do-bloqueio/04-05-SUMMARY.md

@lib/session.ts
@lib/api-auth.ts
@lib/auth-guard.ts
@app/api/assinaturas/checkout/route.ts
@proxy.ts
</context>

## Artifacts this phase produces (deste plano)

| Símbolo | Arquivo | Tipo |
|---------|---------|------|
| `OpcoesDeAuth` (`permitirEmpresaBloqueada?: boolean`) | `lib/api-auth.ts` | interface exportada (NOVA) |
| `requireAuth(request, opcoes?)` | `lib/api-auth.ts` | assinatura estendida com segundo parâmetro opcional |
| `requireAdminSession(slug)` | `lib/session.ts` | comportamento estendido: segundo `redirect` para a tela de bloqueio |
| `STATUS_ASSINATURA_SUSPENSA` | `lib/api-auth.ts` | const de módulo exportada com o código HTTP (402) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate de navegação em requireAdminSession</name>

  <files>lib/session.ts, lib/session.test.ts</files>

  <read_first>
    - `lib/session.ts` — arquivo a modificar; ler as 104 linhas inteiras. O JSDoc de `requireAdminSession` (70-89) documenta ordem deliberada (banco só depois de token + slug válidos) e o comentário da linha 98 sobre `redirect()` fora de try/catch; as linhas 50-55 registram o incidente de loop de redirect da Fase 1
    - `lib/session.test.ts` — arquivo a modificar; o `vi.hoisted` + `redirectMock` das linhas 1-32 já existe e é o que as asserções novas usam
    - `lib/auth-guard.ts` — `ContaAtiva.statusAcesso` já vem preenchido; o comentário das linhas 44-46 diz explicitamente que aplicar o bloqueio é trabalho desta fase
    - `lib/avaliar-acesso.ts` — `acessoBloqueado`, criado no plano 04-01
    - `app/[slug]/admin/bloqueado/page.tsx` — destino do redirect, criado no plano 04-05
    - `tests/setup/prisma-mock.ts` linhas 20-45 — o stub default devolve `trialFim: 2099-01-01` + `ultimoStatusAuditado: "TRIAL"` (combinação neutra); os testes de bloqueio sobrescrevem os 4 fatos
    - `.planning/phases/04-aplica-o-do-bloqueio/04-RESEARCH.md` §Pattern 1 e §Achado crítico 4
  </read_first>

  <behavior>
    - Sessão válida + empresa BLOQUEADO → redireciona para `/{slug}/admin/bloqueado`
    - Sessão válida + empresa CANCELADO → redireciona para `/{slug}/admin/bloqueado` (mesmo destino, D-06)
    - Sessão válida + TRIAL → devolve o payload, sem redirect
    - Sessão válida + EM_DIA → devolve o payload, sem redirect
    - Sessão válida + CARENCIA → devolve o payload, sem redirect (D-03: carência não bloqueia)
    - Sessão válida + VITALICIO → devolve o payload, sem redirect
    - Sem sessão → continua redirecionando para `/{slug}/admin/login` (comportamento existente inalterado)
    - O redirect de bloqueio acontece DEPOIS do de login: uma sessão inválida nunca vê a tela de bloqueio
  </behavior>

  <action>
    Em `lib/session.ts`, dentro de `requireAdminSession`, imediatamente ABAIXO do bloco
    `if (!session || !conta || conta.empresaSlug !== slug) { redirect(...) }` existente e ANTES do
    `return session`, acrescentar:
    `if (acessoBloqueado(conta.statusAcesso)) { redirect(`/${slug}/admin/bloqueado`); }`

    Importar `acessoBloqueado` de `@/lib/avaliar-acesso`. Nenhuma outra linha da função muda: a
    ordem "banco só depois de token válido e slug conferido" é preservada, e o `redirect` continua
    fora de qualquer `try/catch`.

    Estender o JSDoc de `requireAdminSession` com um parágrafo novo, no mesmo registro dos que já
    estão lá, documentando:
    - ACC-02 / D-04 / D-06: `BLOQUEADO` e `CANCELADO` recebem tratamento idêntico; a distinção de
      rótulo é só auditoria (D-08 da Fase 2)
    - por que o destino fica FORA do grupo `(protected)`: se `bloqueado` estivesse dentro, o layout
      daquele grupo chamaria esta mesma função, que redirecionaria de novo — `ERR_TOO_MANY_REDIRECTS`.
      É a mesma classe de loop que a Fase 1 já sofreu e que está registrada nas linhas 50-55 deste
      arquivo (T-04-14)
    - por que o gate mora AQUI e não no layout: `layout.tsx` não impede segmentos filhos de executar
      nem de entrar no RSC Payload, e não re-renderiza em navegação client-side. Citar
      `node_modules/next/dist/docs/01-app/02-guides/authentication.md` (T-04-06)
    - por que não `proxy.ts`: os docs do Next 16 dizem que o proxy "should not be used as a full
      session management or authorization solution"; acrescentar I/O de banco lá abriria conexão a
      cada request de asset

    Em `lib/session.test.ts`, acrescentar um `describe("requireAdminSession — gate de assinatura")`
    cobrindo os oito casos de `<behavior>`. Para produzir cada status, sobrescrever
    `prismaMock.usuario.findFirst` com os 4 fatos correspondentes, seguindo o formato do stub de
    `tests/setup/prisma-mock.ts`:
    - BLOQUEADO: `acessoAte: new Date("2020-01-01")`, `trialFim: null`, `canceladoEm: null`, `acessoVitalicio: false`
    - CANCELADO: os mesmos, com `canceladoEm: new Date("2020-06-01")`
    - CARENCIA: `acessoAte` alguns dias no passado, dentro dos 10 dias de carência (calcular a partir
      de `new Date()` menos 2 dias, nunca com literal fixo que envelheça)
    - TRIAL: o stub default já produz TRIAL
    - EM_DIA: `acessoAte: new Date("2099-01-01")`, `trialFim: null`
    - VITALICIO: `acessoVitalicio: true`
    Afirmar com `rejects.toThrow("REDIRECT:/empresa-teste/admin/bloqueado")` nos dois casos de
    bloqueio, e `resolves.toEqual(payload)` nos quatro casos liberados. Acrescentar também um caso
    afirmando que sem sessão o destino continua sendo `/empresa-teste/admin/login`, para provar a
    ordem dos dois redirects.
  </action>

  <verify>
    <automated>npx vitest run lib/session.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run lib/session.test.ts` sai com código 0
    - `grep -c "acessoBloqueado" lib/session.ts` retorna 2 (import + uso)
    - `grep -n "admin/bloqueado" lib/session.ts` retorna exatamente 1 linha
    - `grep -vE "^\s*(//|\*|/\*)" lib/session.ts | grep -c "unstable_cache\|use cache"` retorna 0
    - `grep -rn "requireAdminSession" app --include=*.tsx --include=*.ts | grep -v "\.test\." | wc -l` continua retornando 59 (nenhum call site foi editado)
    - `npm test` sai com código 0 (a suíte inteira: o stub neutro do prisma-mock tem que manter os testes existentes verdes)
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>

  <done>Toda page, layout e Server Action sob `(protected)` passa a rejeitar empresa bloqueada, sem uma única edição de call site, e sem possibilidade de loop de redirect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate de API em requireAuth com opt-out único do checkout</name>

  <files>lib/api-auth.ts, lib/api-auth.test.ts, app/api/assinaturas/checkout/route.ts, app/api/assinaturas/checkout/route.test.ts</files>

  <read_first>
    - `lib/api-auth.ts` — arquivo a modificar; as 51 linhas inteiras. `AuthError` já carrega `.status`; o JSDoc das linhas 20-24 registra que quem precisa dos dados frescos chama `revalidarConta` direto e sai de graça
    - `lib/api-auth.test.ts` — arquivo a modificar; o `buildRequest` local e o caso "aplica uma mudança no banco já no request seguinte, com o mesmo token" (linhas ~74-88) é o analog exato do teste de ACC-04
    - `app/api/assinaturas/checkout/route.ts` — arquivo a modificar; a linha 16 (`const auth = await requireAuth(request);`) e o comentário de cabeçalho das linhas 6-13, que esta fase torna falso
    - `app/api/assinaturas/checkout/route.test.ts` — arquivo a modificar
    - `app/api/webhooks/asaas/route.ts` — NÃO modificar; confirmar que ele NÃO chama `requireAuth` (usa comparação time-safe de token próprio) e portanto já está fora da guarda por construção (D-05)
    - `.planning/phases/04-aplica-o-do-bloqueio/04-RESEARCH.md` §Pattern 2, §Decisão de status HTTP e §Pitfall 1
  </read_first>

  <behavior>
    - Bearer válido + empresa BLOQUEADO → lança `AuthError` com `status: 402` e mensagem `"Assinatura suspensa por falta de pagamento."`
    - Bearer válido + empresa CANCELADO → mesmo erro, mesmo status, mesma mensagem
    - Bearer válido + TRIAL / EM_DIA / CARENCIA / VITALICIO → resolve com o payload
    - Bearer válido + empresa BLOQUEADO + `{ permitirEmpresaBloqueada: true }` → resolve com o payload
    - Conta inexistente/inativa continua lançando 401 `"Sessão inválida."` (comportamento existente)
    - Sem token continua lançando 401 `"Não autenticado."` (comportamento existente)
    - ACC-04: o MESMO token rejeitado com 402 passa a resolver assim que o mock de `acessoAte` avança para o futuro, no request seguinte, sem novo login
    - `POST /api/assinaturas/checkout` de empresa bloqueada NÃO devolve 402 — chega a chamar `criarCheckout`
    - Uma rota qualquer que use `requireAuth` sem opções devolve 402 com `{ message: "Assinatura suspensa por falta de pagamento." }` sem edição do handler
  </behavior>

  <action>
    Em `lib/api-auth.ts`:

    1. Exportar `const STATUS_ASSINATURA_SUSPENSA = 402;` como const de módulo, com JSDoc explicando
       a escolha: 402 Payment Required é semanticamente específico para "acesso negado exclusivamente
       porque a assinatura venceu", em oposição a 403 ("autenticado mas sem permissão — pagar não
       resolve"). O chamador já está autenticado como a própria empresa inadimplente, então não há
       vazamento cross-tenant nesta resposta, e D-05 pede um erro claro. Registrar que MDN ainda
       classifica 402 como não-padrão e que trocar para 403 custa uma linha — é decisão do planner,
       não fato normativo.
    2. `export interface OpcoesDeAuth { permitirEmpresaBloqueada?: boolean }` com o JSDoc normativo
       do 04-RESEARCH.md §Pattern 2: existe para UM caso e só um — o endpoint de checkout. Bloquear
       o caminho de pagamento tranca o cliente do lado de fora sem como voltar (Pitfall 1 /
       T-04-10). Qualquer segunda ocorrência desta flag é um bug, e o gate de grep do plano 04-09
       prova que é única (T-04-13).
    3. Estender a assinatura para `requireAuth(request: Request, opcoes: OpcoesDeAuth = {})`. O
       default vazio é o que torna a guarda fail-CLOSED: uma rota nova nasce protegida. Comentar que
       o anti-padrão descartado foi criar um `requireAuthComAssinatura` separado adotado rota a rota
       — aquilo é fail-open por default.
    4. Depois do `if (!conta) { throw new AuthError("Sessão inválida.", 401); }` existente,
       acrescentar `if (!opcoes.permitirEmpresaBloqueada && acessoBloqueado(conta.statusAcesso)) {
       throw new AuthError("Assinatura suspensa por falta de pagamento.", STATUS_ASSINATURA_SUSPENSA); }`.
       Importar `acessoBloqueado` de `@/lib/avaliar-acesso`.
    5. Acrescentar ao JSDoc da função um parágrafo sobre ACC-04: o status é derivado de
       `revalidarConta` a cada request, memoizado apenas por `React.cache` (por request, nunca
       cross-request). Envolver esta leitura em `unstable_cache` ou `'use cache'` congelaria a
       autorização e faria a reativação automática parar de funcionar (T-04-08); a decisão `[01-02]`
       do STATE.md já proíbe isso.
    6. Registrar no JSDoc que `POST /api/webhooks/asaas` fica fora desta guarda por construção — não
       chama `requireAuth`, usa token próprio (D-05).

    Em `app/api/assinaturas/checkout/route.ts`:
    - Trocar a linha 16 por `const auth = await requireAuth(request, { permitirEmpresaBloqueada: true });`
      com um comentário de duas linhas: este é o ÚNICO call site autorizado a passar esta flag; o
      caminho de pagamento tem que sobreviver ao bloqueio, senão o cliente que quer pagar não
      consegue (Pitfall 1 / T-04-10 / T-04-13).
    - Atualizar o comentário de cabeçalho das linhas 6-13: a afirmação "Não existe tela, botão ou
      Server Action chamando este endpoint hoje" ficou FALSA — a Fase 4 construiu a Server Action
      `iniciarPagamento` (`app/[slug]/admin/_lib/assinatura-actions.ts`), que chama
      `assinaturaService.criarCheckout` DIRETO, sem passar por HTTP. Registrar por quê: o browser
      não tem Bearer token (o cookie é `httpOnly`), então uma UI não consegue autenticar este
      endpoint. Ele continua sendo o endpoint canônico para consumidores com token — a Fase 7 pode
      usá-lo.

    Nos testes: estender `lib/api-auth.test.ts` com um `describe` cobrindo os oito primeiros casos de
    `<behavior>`, montando os 4 fatos de billing como descrito na Task 1. Para o caso ACC-04, seguir
    literalmente o formato do teste existente "aplica uma mudança no banco já no request seguinte":
    primeiro `rejects.toMatchObject({ status: 402 })`, depois trocar o `mockResolvedValue` para
    `acessoAte` no futuro e afirmar `resolves.toEqual(payload)` com o MESMO token. Estender
    `app/api/assinaturas/checkout/route.test.ts` com um caso "empresa bloqueada consegue iniciar o
    checkout": sobrescrever o stub para BLOQUEADO, afirmar que a resposta NÃO é 402 e que
    `assinaturaService.criarCheckout` foi chamado.
  </action>

  <verify>
    <automated>npx vitest run lib/api-auth.test.ts app/api/assinaturas/checkout/route.test.ts app/api/produtos/route.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run lib/api-auth.test.ts app/api/assinaturas/checkout/route.test.ts app/api/produtos/route.test.ts` sai com código 0
    - `grep -rl "permitirEmpresaBloqueada" app --include=*.ts --include=*.tsx | grep -v "\.test\." | wc -l` retorna exatamente 1, e o arquivo é `app/api/assinaturas/checkout/route.ts`
    - `grep -c "acessoBloqueado" lib/api-auth.ts` retorna 2 (import + uso)
    - `grep -n "requireAuth" app/api/webhooks/asaas/route.ts` não retorna nenhuma linha
    - `grep -vE "^\s*(//|\*|/\*)" lib/api-auth.ts | grep -c "unstable_cache\|use cache"` retorna 0
    - `grep -rn "instanceof AuthError" -A 2 app/api --include=route.ts | grep -c "error.status"` continua retornando 29 (nenhum handler foi editado para hardcodar status)
    - `npm test` sai com código 0
    - `npx tsc --noEmit` e `npm run lint` saem com código 0
  </acceptance_criteria>

  <done>Todas as rotas `/api/*` autenticadas rejeitam empresa bloqueada com 402, sem edição de handler, e exatamente um arquivo do repositório opta por sair da guarda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (cookie `admin_session`) → páginas e Server Actions do admin | atravessa `requireAdminSession` |
| cliente com Bearer JWT → `/api/*` | atravessa `requireAuth` |
| Asaas → `POST /api/webhooks/asaas` | token próprio, deliberadamente FORA de `requireAuth` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | boundary de autorização do admin | mitigate | Gate no DAL (`requireAdminSession`/`requireAuth`), nunca em `layout.tsx` nem em `proxy.ts`. Docs do Next 16.3.0 empacotados citados no JSDoc |
| T-04-13 | Elevation of Privilege | `permitirEmpresaBloqueada` | mitigate | Flag opt-out fail-closed por default; gate de grep provando ocorrência única fora de testes (no acceptance criteria e no script do plano 04-09) |
| T-04-10 | Denial of Service (auto-infligido) | caminho de pagamento | mitigate | O checkout opta por sair da guarda; `logout` e a tela de login continuam sem gate de status |
| T-04-08 | Elevation of Privilege | `lib/session.ts`, `lib/api-auth.ts`, `lib/auth-guard.ts` | mitigate | Somente `React.cache` (por request). Gate de grep com filtro de linhas de comentário — o JSDoc novo MENCIONA `unstable_cache`, então o gate cru se auto-invalidaria |
| T-04-14 | Denial of Service | destino do redirect de bloqueio | mitigate | Destino fora de `(protected)`; teste unitário afirma a URL exata; e2e do plano 04-08 prova ausência de loop |
| T-04-18 | Denial of Service | carga no banco por auth | mitigate | Ordem preservada: banco só é consultado depois de token válido e slug conferido; nenhuma query nova em `proxy.ts` |
| T-04-SC | Tampering | instalação de pacotes | mitigate | Nenhum pacote novo neste plano |
</threat_model>

<verification>
```bash
npm test
npx tsc --noEmit
npm run lint
grep -rl "permitirEmpresaBloqueada" app --include=*.ts --include=*.tsx | grep -v "\.test\." | wc -l   # 1
grep -rn "requireAdminSession" app --include=*.tsx --include=*.ts | grep -v "\.test\." | wc -l        # 59
```
</verification>

<success_criteria>
- `requireAdminSession` redireciona BLOQUEADO e CANCELADO para `/[slug]/admin/bloqueado` e libera os outros quatro status
- `requireAuth` lança 402 para BLOQUEADO e CANCELADO e libera os outros quatro status
- Exatamente um arquivo de produção passa `permitirEmpresaBloqueada`
- O mesmo token rejeitado é aceito no request seguinte quando `acessoAte` avança (ACC-04)
- Nenhum dos 59 call sites de `requireAdminSession` e nenhum dos 29 handlers foi editado
- A suíte inteira (`npm test`) continua verde
</success_criteria>

<output>
Create `.planning/phases/04-aplica-o-do-bloqueio/04-06-SUMMARY.md` when done
</output>
