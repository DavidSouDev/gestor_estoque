---
phase: 05-worker-di-rio-de-reconcilia-o
plan: 05
type: execute
wave: 4
depends_on: ["05-03", "05-04"]
files_modified:
  - scripts/gates-fase-05.mjs
  - package.json
  - .planning/phases/05-worker-di-rio-de-reconcilia-o/05-VALIDATION.md
autonomous: true
requirements: [WRK-01, WRK-02]
user_setup: []

must_haves:
  truths:
    - "Um comando único (`npm run gates:fase-05`) falha o build se alguém acoplar o worker a uma plataforma específica de hosting"
    - "O gate de grep de `ultimoStatusAuditado` da Fase 2 passa a ser executável, com o worker na allowlist e nenhum arquivo fora dela"
    - "O gate falha se alguém trocar `Promise.allSettled` por `Promise.all`, reintroduzir `agendarPosResposta` no worker, ou fazer o worker importar o módulo do Asaas"
    - "O gate falha se um pacote for instalado nesta fase sem verificação de legitimidade"
    - "A suíte inteira (unit, e2e, tipos, lint, gates das Fases 4 e 5) está verde no mesmo estado do repositório"
  artifacts:
    - path: "scripts/gates-fase-05.mjs"
      provides: "os seis gates estáticos da fase como código executável"
      min_lines: 180
    - path: "package.json"
      provides: "script gates:fase-05"
      contains: "gates:fase-05"
  key_links:
    - from: "package.json"
      to: "scripts/gates-fase-05.mjs"
      via: "npm run gates:fase-05"
      pattern: "gates:fase-05"
    - from: "scripts/gates-fase-05.mjs"
      to: "app/api/cron/reconciliacao-diaria/route.ts"
      via: "leitura de linhas de código e busca das agulhas proibidas"
      pattern: "reconciliacao-diaria"
---

<objective>
Transformar em código executável as invariantes desta fase que hoje só existem como prosa nos
documentos de planejamento — e fechar a fase com a suíte inteira verde.

Purpose: quatro das decisões desta fase são invisíveis em code review porque a violação é uma
linha curta e plausível. Trocar `Promise.allSettled` por `Promise.all` parece simplificação;
importar `@vercel/functions` parece conveniência; reintroduzir `agendarPosResposta` parece
consistência com o webhook; acrescentar `ultimoStatusAuditado` a um `select` novo parece
inofensivo. As quatro quebram uma decisão registrada, e nenhuma quebra um teste. A Fase 4 já
resolveu essa classe de problema com `scripts/gates-fase-04.mjs`; esta fase reusa a forma.

Output: `scripts/gates-fase-05.mjs`, o script `gates:fase-05` no `package.json` e
`05-VALIDATION.md` fechado com evidência real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-CONTEXT.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-RESEARCH.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-VALIDATION.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-03-SUMMARY.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-04-SUMMARY.md
@scripts/gates-fase-04.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/gates-fase-05.mjs — os seis gates estáticos da fase</name>

  <files>scripts/gates-fase-05.mjs, package.json</files>

  <read_first>
    - `scripts/gates-fase-04.mjs` inteiro (364 linhas). O cabeçalho 1-32 explica as três convenções
      do arquivo: (a) linhas de comentário são descartadas antes de contar, senão os JSDoc normativos
      que citam os identificadores proibidos invalidariam o próprio gate; (b) as agulhas proibidas são
      montadas por concatenação para o gate não ser a própria primeira violação; (c) o código de saída
      é 1 a partir da primeira violação, mas os gates seguintes ainda rodam. As funções
      `linhasDeCodigo`, `ocorrencias`, `arquivosSob`, `ehFonte`, `ok` e `falha` (linhas 33-103) são
      reusadas com a mesma forma. O `gate6` (linhas 308-348) é o modelo literal do gate de pacotes
    - `app/api/cron/reconciliacao-diaria/route.ts` (plano 05-03) — o alvo dos gates 1 a 4
    - `app/services/reconciliacao.service.ts` (plano 05-02) — entra na allowlist do gate 5
    - `.planning/phases/02-modelo-de-dados-e-motor-de-acesso/02-06-PLAN.md` linha 314 — a versão em
      prosa do gate de `ultimoStatusAuditado` da Fase 2, com a lista de arquivos permitidos que esta
      task transforma em código
    - `package.json` — o bloco `scripts`, onde `gates:fase-04` já vive; e as contagens atuais de
      `dependencies` (11) e `devDependencies` (20)
    - `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-RESEARCH.md` §"Achado crítico 2"
      (a tabela "Onde o Vercel-específico realmente vazaria") e §"Wave 0 Gaps" (as duas últimas linhas)
  </read_first>

  <action>
    Criar `scripts/gates-fase-05.mjs` copiando a estrutura de `scripts/gates-fase-04.mjs`: mesmas
    helpers (`ok`, `falha`, `linhasDeCodigo`, `ocorrencias`, `arquivosSob`, `ehFonte`), mesma
    convenção de agulhas por concatenação, mesmo `process.exitCode = 1` a partir da primeira falha
    com os gates seguintes ainda rodando, mesmo bloco final imprimindo o placar.

    Cabeçalho do arquivo registrando POR QUE cada gate existe — o mesmo registro que o cabeçalho do
    arquivo da Fase 4 faz. Em particular, a frase de abertura: as quatro invariantes desta fase são
    violáveis por uma linha curta e plausível que nenhum teste pega.

    Constante de módulo `const DIRETORIO_DO_WORKER = "app/api/cron"` e
    `const ROTA_DO_WORKER = "app/api/cron/reconciliacao-diaria/route.ts"`.

    **Gate 1 — neutralidade de plataforma (D-07, critério de sucesso #3, T-05-22).**
    Agulhas montadas por concatenação: `"x-vercel" + "-cron"`, `"vercel" + "-cron"`,
    `"@vercel" + "/functions"`. Varrer todos os arquivos-fonte (não-teste) sob `app/` e `lib/` e
    falhar se qualquer linha de CÓDIGO contiver uma delas. Falhar TAMBÉM se `vercel.json` existir na
    raiz do repositório. Mensagem de falha explicando: o critério de sucesso #3 exige que o endpoint
    funcione igual atrás de Vercel Cron ou de um crontab de VPS SEM mudança de código; ler o
    user-agent ou um header de schedule específico da plataforma quebra isso silenciosamente, e
    commitar `vercel.json` toma a decisão de hosting que D-07 adia.

    **Gate 2 — o worker aguarda as escritas (Pitfall 4, T-05-17).**
    Agulha: `"agendar" + "PosResposta"`. Falhar se aparecer em qualquer linha de código de um arquivo
    sob `DIRETORIO_DO_WORKER`. Mensagem: o corpo JSON é a única superfície de observabilidade
    autorizada por D-04; agendar as escritas para depois da resposta reportaria contagens vazias com
    as transições aparecendo no banco depois.

    **Gate 3 — o worker não fala com o gateway (D-06).**
    Agulhas: `"asaas"` e `"Asaas"`. Falhar se aparecerem em qualquer linha de código sob
    `DIRETORIO_DO_WORKER`. Mensagem: D-06 escopa o worker para reavaliar exclusivamente os 4 fatos de
    billing locais; qualquer import do módulo do gateway é a reconciliação ativa que a fase descartou,
    entrando pela porta dos fundos.

    **Gate 4 — forma do handler (Pitfall 3, Pattern 4, T-05-05).**
    Sobre `ROTA_DO_WORKER`, verificar nas linhas de CÓDIGO:
    - `export async function GET` aparece exatamente 1 vez (um handler `POST`-only não funciona atrás
      de Vercel Cron e aparece no painel com 405)
    - `Promise.all(` aparece 0 vezes e `Promise.allSettled` aparece 1 vez (`all` aborta na primeira
      rejeição e violaria D-05; sem lotes, satura o pool de `max = 10`)
    - `new Date()` aparece exatamente 1 vez (um único `agora` para toda a execução: ler o relógio por
      empresa faria uma virada de meia-noite no meio do laço produzir resultado dependente da ordem
      do `findMany`)
    - `deletedAt: null` aparece exatamente 1 vez (empresas soft-deleted diluiriam o denominador do
      freio e cairiam como transição `null → BLOQUEADO`, contadas como perigosas — Pitfall 8)
    Falhar nomeando qual das quatro condições quebrou e a linha encontrada.

    **Gate 5 — `ultimoStatusAuditado` não vaza para caminho de decisão (T-02-03, T-05-09).**
    Esta é a versão EXECUTÁVEL do gate que a Fase 2 deixou em prosa dentro do `02-06-PLAN.md`.
    Allowlist, como constante nomeada e comentada:
    `app/services/acesso.service.ts`, `app/services/empresa.service.ts`,
    `app/services/webhook-asaas.service.ts`, `lib/auth-guard.ts`,
    `app/services/reconciliacao.service.ts` e `app/api/cron/reconciliacao-diaria/route.ts`.
    Varrer os arquivos-fonte (não-teste) sob `app/` e `lib/`, e falhar se algum arquivo FORA da
    allowlist contiver o identificador em linha de código. Mensagem: a coluna é bookkeeping do
    compare-and-swap, nunca fonte da verdade de acesso — quem decide é `avaliarAcesso` sobre os 4
    fatos (BILL-01). Registrar no comentário que os dois últimos arquivos da lista foram acrescentados
    pela Fase 5 e que o uso deles é exclusivamente como `anterior` do CAS.

    **Gate 6 — nenhum pacote instalado nesta fase (T-05-SC).**
    Copiar a forma do `gate6` da Fase 4, com os mesmos números medidos: 11 `dependencies` e 20
    `devDependencies`. Mensagem de falha idêntica em espírito: a Fase 5 declarou não instalar nada
    (`05-RESEARCH.md` §Standard Stack e §Package Legitimacy Audit, que está vazia — nenhum pacote
    `[ASSUMED]` ou `[SUS]`); um install que aparece no meio de uma fase que declarou não instalar nada
    é o sinal exato de slopsquatting. Pare, verifique a legitimidade no registro, e só então ajuste o
    número junto com o registro da verificação.

    No `package.json`, acrescentar `"gates:fase-05": "node scripts/gates-fase-05.mjs"` ao bloco
    `scripts`, imediatamente abaixo de `gates:fase-04`. Nenhuma dependência é adicionada — o script
    usa só `node:fs` e `node:path`, igual ao da Fase 4.
  </action>

  <verify>
    <automated>npm run gates:fase-05 &amp;&amp; npm run gates:fase-04</automated>
  </verify>

  <acceptance_criteria>
    - `npm run gates:fase-05` sai com código 0 e imprime "6/6 gates OK"
    - `npm run gates:fase-04` continua saindo 0 com 6/6
    - `grep -c "gates:fase-05" package.json` retorna 1
    - `grep -cE '"dependencies"|"devDependencies"' package.json` inalterado — nenhum pacote novo
    - Prova de não-vacuidade de cada gate, registrada no SUMMARY com a mensagem de falha observada.
      Injetar temporariamente, uma de cada vez, e reverter: (1) uma linha de código com o
      identificador de header de cron da Vercel em `app/api/cron/reconciliacao-diaria/route.ts`
      → Gate 1 falha; (2) uma chamada a `agendarPosResposta` no mesmo arquivo → Gate 2 falha;
      (3) um import de `@/lib/billing/asaas/config` no mesmo arquivo → Gate 3 falha;
      (4) trocar `Promise.allSettled` por `Promise.all` → Gate 4 falha; (5) acrescentar
      `ultimoStatusAuditado: true` a um `select` de `app/services/produto.service.ts` → Gate 5 falha;
      (6) trocar `DEPENDENCIAS_ESPERADAS` para 12 → Gate 6 falha
    - Prova de que o gate não se auto-invalida: os comentários de `scripts/gates-fase-05.mjs` citam
      literalmente as agulhas proibidas, e `npm run gates:fase-05` continua verde — é o que a
      concatenação e o descarte de linhas de comentário garantem
    - `npm run lint` sai com código 0
  </acceptance_criteria>

  <done>As seis invariantes estáticas da fase rodam por um comando, falham de verdade quando violadas, e não acusam a si mesmas.</done>
</task>

<task type="auto">
  <name>Task 2: Gate completo da fase e fechamento de 05-VALIDATION.md</name>

  <files>.planning/phases/05-worker-di-rio-de-reconcilia-o/05-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-VALIDATION.md` inteiro — o frontmatter
      (`status`, `nyquist_compliant`, `wave_0_complete`), o Per-Task Verification Map já preenchido
      pelo planner, a seção Wave 0 Requirements e a tabela Manual-Only Verifications
    - `.planning/phases/04-aplica-o-do-bloqueio/04-VALIDATION.md` — o formato de um VALIDATION fechado
      neste projeto (`status: approved`, nenhuma linha pendente)
    - Os SUMMARY dos planos 05-01 a 05-04 — de onde vêm os números reais a transcrever
  </read_first>

  <action>
    Rodar, no mesmo estado do repositório e nesta ordem, registrando a saída real de cada um:
    1. `npm run lint`
    2. `npx tsc --noEmit`
    3. `npm test`
    4. `npm run test:e2e`
    5. `npm run gates:fase-04`
    6. `npm run gates:fase-05`
    7. `npx prisma db execute --file prisma/checks/worker-diario-enum.sql`
    8. `npx prisma migrate status`

    Se qualquer um falhar, corrigir a causa antes de tocar o `05-VALIDATION.md` — este documento
    registra evidência medida, nunca intenção.

    Em `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-VALIDATION.md`:
    - marcar as quatro caixas de § Wave 0 Requirements, cada uma com o caminho do arquivo que a
      satisfez
    - marcar as seis caixas de § Validation Sign-Off
    - trocar o frontmatter para `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`
    - trocar `**Approval:** pending` por uma linha com a data e os números MEDIDOS: contagem de testes
      unitários, contagem de specs e2e, `tsc` 0, `lint` 0, gates 6/6 + 6/6. Não inventar nenhum
      número: transcrever o que os comandos imprimiram
    - acrescentar, ao final da tabela § Manual-Only Verifications, uma linha registrando que o
      comportamento atrás de um agendador REAL (Vercel Cron ou crontab) continua sem verificação
      porque o hosting não existe (D-07), e que a verificação a fazer no dia do deploy é: configurar o
      agendador, disparar uma execução por ele, e confirmar no histórico um 200 com o corpo JSON
      esperado — nunca um 3xx (cron não segue redirect) nem um 404 (path errado ainda é cobrado e
      falha em silêncio)

    Não alterar o Per-Task Verification Map: ele já foi preenchido pelo planner com os IDs reais de
    task. Apenas trocar a coluna `Status` de `⬜ pending` para `✅` nas linhas cujo comando
    correspondente saiu verde nesta execução.
  </action>

  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test &amp;&amp; npm run test:e2e &amp;&amp; npm run gates:fase-04 &amp;&amp; npm run gates:fase-05 &amp;&amp; npx prisma db execute --file prisma/checks/worker-diario-enum.sql</automated>
  </verify>

  <acceptance_criteria>
    - Os oito comandos listados saem com código 0
    - `npm test` reporta pelo menos 876 testes (a linha de base da Fase 4) mais os testes novos dos planos 05-02 e 05-03
    - `npm run test:e2e` reporta 24 specs
    - `npm run gates:fase-04` e `npm run gates:fase-05` reportam 6/6 cada
    - O frontmatter de `05-VALIDATION.md` tem `status: approved`, `nyquist_compliant: true` e `wave_0_complete: true`
    - `grep -c "pending" .planning/phases/05-worker-di-rio-de-reconcilia-o/05-VALIDATION.md` retorna 0
    - `grep -c "⬜" .planning/phases/05-worker-di-rio-de-reconcilia-o/05-VALIDATION.md` retorna 0
    - A tabela Manual-Only Verifications tem uma linha nova sobre a verificação diferida do agendador real
  </acceptance_criteria>

  <done>A fase fecha com evidência medida em vez de intenção: suíte, tipos, lint, e2e, os dois conjuntos de gates estáticos e o gate SQL do enum, todos verdes no mesmo estado do repositório.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts deste plano (05-05)

| Símbolo / arquivo | Tipo | Consumido por |
|---|---|---|
| `scripts/gates-fase-05.mjs` | script de gates estáticos | `npm run gates:fase-05`, CI futuro e `/gsd-verify-work` |
| `gates:fase-05` | script npm | operador e gate de fase |
| `05-VALIDATION.md` com `status: approved` | artefato de validação | `/gsd-verify-work` e o fechamento da fase |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → build/CI | os gates são a única barreira automatizada contra as violações de decisão desta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Tampering | acoplamento a plataforma de hosting | mitigate | Gate 1: agulhas `x-vercel-cron`, `vercel-cron`, `@vercel/functions` proibidas em linhas de código de `app/` e `lib/`, e `vercel.json` proibido na raiz — a prova executável do critério de sucesso #3 e de D-07 |
| T-05-17 | Tampering | resposta antes das escritas | mitigate | Gate 2: `agendarPosResposta` proibido sob `app/api/cron` |
| T-05-23 | Tampering | reconciliação ativa entrando pela porta dos fundos | mitigate | Gate 3: nenhuma referência ao Asaas sob `app/api/cron` (D-06) |
| T-05-05 | Denial of Service | saturação do pool | mitigate | Gate 4: `Promise.all(` proibido e `Promise.allSettled` obrigatório no handler |
| T-05-24 | Denial of Service | endpoint inalcançável pelo agendador | mitigate | Gate 4: `export async function GET` obrigatório — um handler `POST`-only aparece no painel do Vercel com 405 e o job falha em silêncio |
| T-05-09 | Elevation of Privilege | `ultimoStatusAuditado` lido como verdade | mitigate | Gate 5: allowlist de 6 arquivos; qualquer sétimo falha o build. Versão executável do gate que a Fase 2 deixou em prosa |
| T-05-SC | Tampering | npm installs | mitigate | Gate 6: 11 `dependencies` e 20 `devDependencies` medidos no início da fase; a `05-RESEARCH.md` § Package Legitimacy Audit está vazia e nenhum pacote `[ASSUMED]`/`[SUS]` existe, então qualquer install nesta fase exige ciclo de verificação de legitimidade ANTES de ajustar o número |
</threat_model>

<verification>
- `npm run gates:fase-05` sai 0 com 6/6
- `npm run gates:fase-04` sai 0 com 6/6
- `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run test:e2e` saem 0
- `npx prisma db execute --file prisma/checks/worker-diario-enum.sql` sai 0
- `npx prisma migrate status` sem migration pendente
- `05-VALIDATION.md` com `status: approved` e nenhuma linha pendente
</verification>

<success_criteria>
- As seis invariantes estáticas da fase rodam por um comando e falham de verdade quando violadas (não-vacuidade demonstrada uma a uma)
- O gate de `ultimoStatusAuditado` da Fase 2 deixa de ser prosa e passa a ser código
- Nenhum arquivo do repositório acopla o worker a uma plataforma de hosting específica
- A fase fecha com a suíte inteira verde no mesmo estado do repositório e com os números transcritos, nunca inventados
</success_criteria>

<output>
Create `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-05-SUMMARY.md` when done
</output>
