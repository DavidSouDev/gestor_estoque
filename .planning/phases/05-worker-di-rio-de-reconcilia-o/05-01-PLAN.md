---
phase: 05-worker-di-rio-de-reconcilia-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_worker_diario_causa/migration.sql
  - prisma/checks/worker-diario-enum.sql
  - app/services/webhook-asaas.service.test.ts
  - .planning/STATE.md
autonomous: true
requirements: [WRK-01]
user_setup: []

must_haves:
  truths:
    - "O enum CausaTransicaoAcesso do banco real aceita o valor WORKER_DIARIO — provado por consulta a pg_enum, não por leitura do schema"
    - "O Prisma Client gerado expõe CausaTransicaoAcesso.WORKER_DIARIO, então o worker do plano 05-03 compila"
    - "Nenhum dos três lugares do repositório continua prometendo que a Fase 5 drena a fila EventoWebhookAsaas com processadoEm IS NULL"
    - "A fila de retrabalho `processadoEm IS NULL` fica registrada como débito conhecido, sem dreno, em STATE.md § Blockers"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "quinto valor do enum CausaTransicaoAcesso"
      contains: "WORKER_DIARIO"
    - path: "prisma/checks/worker-diario-enum.sql"
      provides: "gate SQL executável que falha se o valor do enum não existir no banco"
      contains: "RAISE EXCEPTION"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/*_add_worker_diario_causa/migration.sql"
      via: "ALTER TYPE ... ADD VALUE"
      pattern: "ALTER TYPE \"CausaTransicaoAcesso\" ADD VALUE 'WORKER_DIARIO'"
    - from: "app/services/acesso.service.ts"
      to: "CausaTransicaoAcesso.WORKER_DIARIO"
      via: "params.causa do registrarTransicao chamado pelo worker (plano 05-03)"
      pattern: "WORKER_DIARIO"
---

<objective>
Abrir o ponto de extensão que a Fase 2 reservou em D-14: acrescentar `WORKER_DIARIO` ao enum
`CausaTransicaoAcesso`, aplicar a migration contra o Postgres real e provar por consulta a
`pg_enum` que o valor existe no banco — não só no arquivo. E, no mesmo movimento, apagar a
dívida documental que a Fase 3 deixou: três lugares do repositório afirmam que **esta** fase
drenaria a fila `EventoWebhookAsaas WHERE processadoEm IS NULL`, e a decisão D-06 do
`05-CONTEXT.md` revoga essa promessa (o worker só reavalia fatos locais, nunca chama o Asaas).

Purpose: sem o valor de enum aplicado no banco, o plano 05-03 não compila (`tsc` falha em
`CausaTransicaoAcesso.WORKER_DIARIO`) e qualquer escrita do worker estoura em runtime. E uma
promessa órfã em três arquivos é pior que nenhuma documentação: quem ler `prisma/schema.prisma`
depois desta fase concluirá que a fila tem dreno, e ela não tem.

Output: `prisma/schema.prisma` com o quinto valor do enum, uma migration própria (sem nenhum
DML que use o valor — Pitfall 7 da Fase 3), `prisma/checks/worker-diario-enum.sql` como gate de
regressão, e os três comentários órfãos corrigidos (D-10).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-CONTEXT.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-RESEARCH.md
@.planning/phases/05-worker-di-rio-de-reconcilia-o/05-PATTERNS.md
@prisma/schema.prisma
@prisma/migrations/20260831230000_add_asaas_webhook_ledger/migration.sql
@prisma/checks/backfill-billing.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quinto valor do enum + migration própria + gate SQL de regressão</name>

  <files>prisma/schema.prisma, prisma/migrations/*_add_worker_diario_causa/migration.sql, prisma/checks/worker-diario-enum.sql</files>

  <read_first>
    - `prisma/schema.prisma` linhas 33-42 — o bloco `enum CausaTransicaoAcesso` e o comentário normativo de D-14 logo acima dele ("apenas os valores que existem HOJE. Novas causas entram por migration propria na fase em que passarem a existir"). A linha sobre a Fase 3 (`WEBHOOK_PAGAMENTO entrou por migration propria`) é o modelo da frase a acrescentar
    - `prisma/migrations/20260831230000_add_asaas_webhook_ledger/migration.sql` linhas 1-14 — a forma EXATA a replicar: header em ASCII sem acento explicando fase/requisito, o parágrafo de Pitfall 7 sobre `ALTER TYPE` dentro de transação, e o marcador `-- AlterEnum` no estilo gerado pelo Prisma
    - `prisma/checks/backfill-billing.sql` — o idioma de gate SQL versionado deste projeto (bloco PL/pgSQL com `RAISE EXCEPTION`, executado por `npx prisma db execute --file`)
    - `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-RESEARCH.md` §"Migration do novo valor de enum" — inclui o aviso operacional A2 do Assumptions Log
    - `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-PATTERNS.md` §`prisma/migrations/<ts>_add_worker_diario_causa/migration.sql`
  </read_first>

  <action>
    (1) Em `prisma/schema.prisma`, acrescentar `WORKER_DIARIO` como ÚLTIMA linha do bloco
    `enum CausaTransicaoAcesso`, depois de `WEBHOOK_PAGAMENTO`. A ordem histórica dos quatro
    valores existentes é preservada — `ALTER TYPE ... ADD VALUE` sem `BEFORE`/`AFTER` acrescenta
    no fim, e divergir disso faria o schema descrever uma ordem que o banco não tem.

    No comentário normativo acima do enum, acrescentar uma frase no mesmo registro da que já
    documenta a Fase 3, dizendo que a Fase 5 (WRK-01) exerceu o mesmo ponto de extensão e que
    `WORKER_DIARIO` é a causa gravada pelo endpoint `/api/cron/reconciliacao-diaria`. Escrever
    sem acento, como o resto dos comentários do schema.

    (2) Gerar a migration com `npx prisma migrate dev --create-only --name add_worker_diario_causa`.
    O `--create-only` é obrigatório: ele produz o SQL sem aplicar, e o SQL gerado precisa receber o
    header normativo antes de rodar. NÃO escrever o diretório da migration à mão — o timestamp tem
    que ser o do Prisma para a ordenação com `20260831230000_add_asaas_webhook_ledger` ficar correta.

    (3) No `migration.sql` recém-gerado, acrescentar acima do `-- AlterEnum` um header em ASCII sem
    acento, replicando a estrutura do precedente da Fase 3:
    - primeira linha nomeando a fase e o requisito: `Fase 5 (WRK-01): causa de transicao gravada pelo worker diario de reconciliacao.`
    - o parágrafo de Pitfall 7, adaptado: esta migration APENAS adiciona o valor `WORKER_DIARIO`
      ao enum; nao ha (e nao pode haver) nenhum INSERT/UPDATE que USE esse valor aqui, porque o
      `prisma migrate` roda cada migration em transacao e o Postgres recusa usar um valor de enum
      adicionado dentro da mesma transacao. Um backfill que precise da nova causa vai em uma
      SEGUNDA migration.
    - uma linha registrando que nenhuma coluna de status e criada: status de acesso continua
      derivado por `avaliarAcesso()` sobre os fatos de billing, nunca armazenado (BILL-01 / D-16).

    O corpo executável tem que ser exatamente uma instrução:
    `ALTER TYPE "CausaTransicaoAcesso" ADD VALUE 'WORKER_DIARIO';` sob o marcador `-- AlterEnum`.
    Nenhuma outra instrução, nenhuma tabela, nenhum índice.

    (4) Criar `prisma/checks/worker-diario-enum.sql` como gate de regressão executável, no idioma
    de `prisma/checks/backfill-billing.sql`: um bloco `DO $$ ... $$;` em PL/pgSQL que consulta
    `pg_enum e JOIN pg_type t ON t.oid = e.enumtypid WHERE t.typname = 'CausaTransicaoAcesso' AND
    e.enumlabel = 'WORKER_DIARIO'` e, quando `NOT EXISTS`, executa
    `RAISE EXCEPTION 'INV-05-01 falhou: WORKER_DIARIO ausente do enum CausaTransicaoAcesso'`.
    Comentário de cabeçalho em ASCII sem acento explicando que este gate existe porque ler o
    `schema.prisma` prova apenas a intencao: o unico fato que importa para o worker e o valor
    existir no catalogo do Postgres alvo.

    NÃO aplicar a migration nesta task — isso é a Task 2, e a separação é deliberada: o arquivo
    tem que estar revisado antes de tocar o banco.
  </action>

  <verify>
    <automated>test -f prisma/checks/worker-diario-enum.sql && grep -q "WORKER_DIARIO" prisma/schema.prisma && ls prisma/migrations/*_add_worker_diario_causa/migration.sql</automated>
  </verify>

  <acceptance_criteria>
    - `grep -vE '^\s*(//|--|\*|/\*)' prisma/schema.prisma | grep -c "WORKER_DIARIO"` retorna 1 (o valor do enum, não o comentário)
    - `ls -d prisma/migrations/*_add_worker_diario_causa` devolve exatamente 1 diretório
    - `grep -c "ALTER TYPE" prisma/migrations/*_add_worker_diario_causa/migration.sql` retorna 1
    - `grep -cE "^(INSERT|UPDATE|DELETE|CREATE TABLE)" prisma/migrations/*_add_worker_diario_causa/migration.sql` retorna 0 (Pitfall 7: nenhum DML usa o valor novo na mesma transação)
    - `grep -c "RAISE EXCEPTION" prisma/checks/worker-diario-enum.sql` retorna 1
    - `grep -c "pg_enum" prisma/checks/worker-diario-enum.sql` retorna 1
  </acceptance_criteria>

  <done>O schema declara o quinto valor, a migration existe com a forma exata do precedente da Fase 3 (enum-only, sem DML), e existe um gate SQL versionado capaz de falhar se alguém aplicar o schema num banco onde o valor não entrou.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a migration contra o Postgres real e regenerar o Prisma Client</name>

  <files>prisma/migrations/*_add_worker_diario_causa/migration.sql</files>

  <read_first>
    - O `migration.sql` escrito na Task 1 — confirmar, antes de aplicar, que o corpo executável é
      exatamente uma instrução `ALTER TYPE` e que não há nenhum DML abaixo dela
    - `prisma/checks/worker-diario-enum.sql` — o gate que esta task executa contra o banco
    - `prisma/migrations/20260831230000_add_asaas_webhook_ledger/migration.sql` — o precedente que já
      rodou com sucesso neste mesmo banco; é a primeira coisa a comparar se a aplicação falhar
    - `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-RESEARCH.md` §"Runtime State Inventory"
      (linha de Build artifacts: o `prisma generate` é obrigatório, senão `CausaTransicaoAcesso.WORKER_DIARIO`
      não existe no tipo gerado e o `tsc` do plano 05-03 falha) e §Assumptions Log A2
  </read_first>

  <action>
    Esta task é [BLOCKING]: nenhum plano posterior desta fase compila sem ela, porque
    `CausaTransicaoAcesso.WORKER_DIARIO` só existe no Prisma Client depois do `generate` que segue a
    migration. Executar os cinco comandos abaixo nesta ordem exata, registrando a saída real de cada
    um no SUMMARY:

    1. `npx prisma migrate dev` — aplica a migration pendente e roda `prisma generate` em seguida.
    2. `npx prisma db execute --file prisma/checks/worker-diario-enum.sql` — prova, por `pg_enum` e
       contra o banco REAL, que o valor entrou. Sai 0 quando existe; sai não-zero com a mensagem
       `INV-05-01 falhou` quando não.
    3. `node -e "const c=require('@prisma/client'); if (c.CausaTransicaoAcesso.WORKER_DIARIO !== 'WORKER_DIARIO') { process.exit(1) } console.log('client ok')"`
       — prova que o Client REGERADO expõe o valor.
    4. `npx prisma migrate status` — confirma que não sobrou migration pendente.
    5. `npx tsc --noEmit` e `npm test` — a suíte inteira continua verde. Nenhum teste existente deve
       depender de o enum ter só quatro valores; se algum depender, é sinal de asserção exaustiva a
       corrigir no teste, nunca de migration errada.

    Se o passo 1 falhar com `ALTER TYPE ... ADD cannot run inside a transaction block` (risco A2 do
    Assumptions Log), NÃO improvisar: abrir
    `prisma/migrations/20260831230000_add_asaas_webhook_ledger/migration.sql`, que já rodou com
    sucesso neste mesmo banco, e comparar a forma dos dois arquivos antes de qualquer outra hipótese.

    Nenhum arquivo é editado por esta task além do `migration.sql` já existente, e só se a comparação
    com o precedente exigir. O diretório `.prisma/client` gerado não é versionado.
  </action>

  <verify>
    <automated>npx prisma db execute --file prisma/checks/worker-diario-enum.sql &amp;&amp; node -e "const c=require('@prisma/client'); if (c.CausaTransicaoAcesso.WORKER_DIARIO !== 'WORKER_DIARIO') { process.exit(1) }" &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx prisma db execute --file prisma/checks/worker-diario-enum.sql` sai com código 0
    - `node -e "const c=require('@prisma/client'); if (c.CausaTransicaoAcesso.WORKER_DIARIO !== 'WORKER_DIARIO') { process.exit(1) }"` sai com código 0
    - `npx prisma migrate status` não reporta migration pendente
    - `npx tsc --noEmit` sai com código 0
    - `npm test` sai com código 0 com 876 testes ou mais, sem nenhuma regressão
    - Prova de não-vacuidade do gate SQL, registrada no SUMMARY: rodar
      `prisma/checks/worker-diario-enum.sql` com o literal `'WORKER_DIARIO'` temporariamente trocado
      por um valor inexistente faz o gate falhar com a mensagem `INV-05-01 falhou`. Reverter em seguida
  </acceptance_criteria>

  <done>O banco local aceita `WORKER_DIARIO`, o Prisma Client regenerado expõe o valor, e o gate SQL versionado prova os dois fatos de forma repetível.</done>
</task>

<task type="auto">
  <name>Task 3: D-10 — apagar a promessa órfã de dreno da fila de webhooks nos três lugares</name>

  <files>prisma/schema.prisma, app/services/webhook-asaas.service.test.ts, .planning/STATE.md</files>

  <read_first>
    - `prisma/schema.prisma` linhas 352-360 — o comentário do model `EventoWebhookAsaas`: "Interface de recuperacao consumida pela FASE 5 (WRK-01): a fila de retrabalho e WHERE \"processadoEm\" IS NULL. Esta fase entrega apenas as colunas e o indice — DRENAR a fila nao e escopo daqui (Pitfall 10)."
    - `app/services/webhook-asaas.service.test.ts` linhas 238-242 ("a ausencia de `processadoEm` aqui e o que mantem o evento na fila de retrabalho `WHERE processadoEm IS NULL` (Fase 5 / WRK-01)"), linha ~662 ("O evento fica na fila `processadoEm IS NULL` para a Fase 5."), linhas ~1137-1140 ("perderia a corrida com o worker da Fase 5 em producao") e ~1159-1161 ("Deixá-lo na fila de retrabalho faria a Fase 5 tentar de novo para sempre.")
    - `.planning/STATE.md` linha 164 — `[03-07] Reenvio pelo painel do Asaas NAO reprocessa evento ja registrado ... recuperacao e trabalho do worker da Fase 5 sobre processadoEm IS NULL, nao do botao`
    - `.planning/STATE.md` § Blockers/Concerns — a lista onde o débito passa a viver
    - `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-CONTEXT.md` §D-06 e §D-10 — a decisão que revoga a promessa e a instrução explícita de corrigir os comentários sem implementar o dreno
    - `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-RESEARCH.md` §"Achado crítico 5"
  </read_first>

  <action>
    Esta task NÃO implementa o dreno. D-06 é explícito: o worker só reavalia os 4 fatos de billing
    locais e nunca chama o Asaas — e reprocessar um evento passa por `webhookAsaasService.processar`,
    que re-busca `GET /v3/payments/{id}` no gateway. Implementar o dreno aqui contrariaria a decisão
    do usuário. O trabalho é corrigir o que está escrito, nos três lugares nomeados por D-10.

    (1) `prisma/schema.prisma`, comentário do bloco `processadoEm`/`erro`/`tentativas` do model
    `EventoWebhookAsaas`: substituir a atribuição à Fase 5 por uma formulação que descreva o que a
    coluna É (interface de recuperacao: a fila de retrabalho e `WHERE "processadoEm" IS NULL`, e o
    indice existe para ela) e registre, sem acento, que a Fase 5 (WRK-01) NAO consome esta fila —
    a decisao D-06 daquela fase escopou o worker para reavaliar apenas os 4 fatos de billing locais,
    sem nenhuma chamada ao Asaas, e drenar a fila exige `webhookAsaasService.processar`, que re-busca
    o pagamento no gateway. Terminar registrando que a fila segue SEM dreno e que quem a drenar sera
    uma fase futura. Manter a segunda metade do comentário existente (por que `erro` precisa ser
    gravado e não só logado) intacta — ela continua verdadeira.

    (2) `app/services/webhook-asaas.service.test.ts`: nas QUATRO ocorrências listadas em
    `<read_first>`, trocar a referência à "Fase 5" por uma que não prometa nada. As asserções e o
    código dos testes NÃO mudam — só a prosa dos comentários. Formulações a usar:
    - onde o comentário justifica a AUSÊNCIA de `processadoEm` (linhas ~240 e ~662): dizer que a
      ausência é o que mantém o evento na fila de retrabalho `WHERE processadoEm IS NULL`, e que
      essa fila hoje não tem consumidor — o worker diário da Fase 5 não a toca (D-06 do
      `05-CONTEXT.md`).
    - onde o comentário cita corrida com "o worker da Fase 5" (linha ~1139): a corrida real é entre
      DUAS entregas de webhook concorrentes; o `updateMany` condicional com `OR: [{ acessoAte: null },
      { acessoAte: { lt: ... } }]` é o que a resolve. Reescrever citando entregas concorrentes do
      Asaas, não o worker — o worker da Fase 5 não escreve `acessoAte`, só `ultimoStatusAuditado`
      via `registrarTransicao`.
    - onde o comentário diz que deixar o evento na fila faria "a Fase 5 tentar de novo para sempre"
      (linha ~1159): reescrever para dizer que marcar `processadoEm` é o que impede o evento de
      ficar eternamente pendente numa fila que ninguém drena.

    Verificar por grep, depois da edição, que a string `Fase 5` não sobrevive em nenhuma das quatro
    posições como promessa de dreno. Se ela aparecer, tem que ser numa frase que NEGA o dreno.

    (3) `.planning/STATE.md`:
    - Na linha 164 (decisão `[03-07]`), reescrever a segunda metade: hoje ela diz que a recuperação
      é trabalho do worker da Fase 5 sobre `processadoEm IS NULL`. Trocar por: a recuperacao NAO e
      do botao de reenvio e NAO e do worker da Fase 5 (D-06 escopou o worker para fatos locais);
      segue sem dono.
    - Em § Blockers/Concerns, acrescentar uma entrada nova: "Fila de retrabalho
      `EventoWebhookAsaas WHERE processadoEm IS NULL` segue SEM dreno depois da Fase 5 (D-06/D-10):
      um cliente que pagou e cujo webhook falhou permanece bloqueado ate intervencao manual.
      Candidata a fase futura — exige `webhookAsaasService.processar`, que re-busca no Asaas."
  </action>

  <verify>
    <automated>npx vitest run app/services/webhook-asaas.service.test.ts &amp;&amp; npx prisma validate</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run app/services/webhook-asaas.service.test.ts` sai com código 0 e com a MESMA contagem de testes de antes da edição (nenhuma asserção foi tocada)
    - `npx prisma validate` sai com código 0
    - `grep -c "processadoEm IS NULL" prisma/schema.prisma` retorna 1 (a descrição da fila sobrevive; o que muda é quem a consome)
    - `grep -n "FASE 5\|Fase 5" prisma/schema.prisma` — toda linha devolvida nega o dreno (nenhuma atribui a drenagem à fase)
    - `grep -n "Fase 5" app/services/webhook-asaas.service.test.ts` — toda linha devolvida nega o dreno ou fala de entregas concorrentes; nenhuma promete reprocessamento
    - `grep -c "sem dreno\|SEM dreno" .planning/STATE.md` retorna 1 ou mais (a entrada nova em Blockers existe)
    - `npm test` sai com código 0
  </acceptance_criteria>

  <done>Nenhum dos três lugares continua prometendo um dreno que esta fase não entrega, e o débito passa a estar registrado como bloqueador conhecido em vez de como promessa cumprida.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts deste plano (05-01)

| Símbolo / arquivo | Tipo | Consumido por |
|---|---|---|
| `CausaTransicaoAcesso.WORKER_DIARIO` | valor de enum (Prisma Client + Postgres) | `app/api/cron/reconciliacao-diaria/route.ts` (plano 05-03) ao chamar `acessoService.registrarTransicao` |
| `prisma/migrations/*_add_worker_diario_causa/migration.sql` | migration | `npx prisma migrate deploy` no CI e no deploy |
| `prisma/checks/worker-diario-enum.sql` | gate SQL versionado | `npx prisma db execute --file` — gate da fase (plano 05-05) |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → Postgres (DDL) | esta é a única passagem deste plano; nenhum dado de usuário atravessa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | `migration.sql` do enum | mitigate | Migration enum-only, sem nenhum INSERT/UPDATE que use `WORKER_DIARIO`: o Postgres recusa usar um valor de enum adicionado na mesma transação, e o `prisma migrate` roda cada migration em transação (Pitfall 7 da Fase 3). Gate de aceite: `grep -cE "^(INSERT\|UPDATE\|DELETE\|CREATE TABLE)"` retorna 0 |
| T-05-12 | Repudiation | trilha `AuditoriaAcesso` | mitigate | Causa própria e distinta (`WORKER_DIARIO`) em vez de reusar `AVALIACAO_SESSAO`: sem ela é impossível distinguir, na investigação de um bloqueio, uma transição observada por request de uma aplicada pelo worker sem ninguém logado |
| T-05-13 | Tampering | schema aplicado divergindo do arquivo | mitigate | `prisma/checks/worker-diario-enum.sql` consulta `pg_enum` do banco alvo e faz `RAISE EXCEPTION` quando o valor não existe — ler o `schema.prisma` prova intenção, não estado |
| T-05-SC | Tampering | npm installs | mitigate | Esta fase não instala nenhum pacote (`05-RESEARCH.md` § Package Legitimacy Audit está vazia, nenhum `[ASSUMED]`/`[SUS]`). `npm run gates:fase-04` Gate 6 continua exigindo 11 deps / 20 devDeps e falha o build se algo entrar |
</threat_model>

<verification>
- `npx prisma migrate status` sem migration pendente
- `npx prisma db execute --file prisma/checks/worker-diario-enum.sql` sai 0
- `node -e "const c=require('@prisma/client'); if (c.CausaTransicaoAcesso.WORKER_DIARIO !== 'WORKER_DIARIO') { process.exit(1) }"` sai 0
- `npx tsc --noEmit` sai 0
- `npm test` sai 0
- `npm run gates:fase-04` continua 6/6
</verification>

<success_criteria>
- O banco local aceita `WORKER_DIARIO` no enum `CausaTransicaoAcesso`, provado por `pg_enum`
- O Prisma Client regenerado expõe o valor, destravando a compilação do plano 05-03
- Os três lugares de D-10 (schema, teste do webhook, STATE.md) não prometem mais o dreno da fila
- A fila `processadoEm IS NULL` está registrada como débito conhecido em STATE.md § Blockers
- Nenhuma regressão: `npm test` e `npx tsc --noEmit` verdes
</success_criteria>

<output>
Create `.planning/phases/05-worker-di-rio-de-reconcilia-o/05-01-SUMMARY.md` when done
</output>
