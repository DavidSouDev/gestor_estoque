---
phase: 03-gateway-asaas-e-ingest-o-de-webhooks
plan: 06
type: execute
wave: 3
depends_on: ["03-05"]
files_modified:
  - app/services/webhook-asaas.service.ts
  - app/services/webhook-asaas.service.test.ts
autonomous: true
requirements: [GTW-03, GTW-04]

must_haves:
  truths:
    - "Um webhook de pagamento confirmado estende o acessoAte da empresa em um ciclo mensal e encerra a carencia em curso"
    - "A empresa afetada e sempre resolvida pelo mapa local, nunca por um identificador vindo do payload"
    - "Um evento antigo reentregue fora de ordem nao reduz nem avanca o acessoAte e nao gera linha de auditoria"
    - "A mesma confirmacao de pagamento entregue duas vezes produz exatamente o mesmo acessoAte"
    - "A transicao de status fica registrada em auditoria com causa WEBHOOK_PAGAMENTO"
    - "Um payload forjado nao estende acesso porque o pagamento e reconferido na API do Asaas antes da escrita"
  artifacts:
    - path: "app/services/webhook-asaas.service.ts"
      provides: "resolucao de tenant por mapa local, escrita monotonica de acessoAte e auditoria da transicao"
      exports: ["webhookAsaasService"]
  key_links:
    - from: "app/services/webhook-asaas.service.ts"
      to: "prisma.empresa.updateMany"
      via: "guarda monotonica no WHERE, nunca em if de aplicacao"
      pattern: "acessoAte: \\{ lt:"
    - from: "app/services/webhook-asaas.service.ts"
      to: "app/services/acesso.service.ts"
      via: "acessoService.registrarTransicao com causa WEBHOOK_PAGAMENTO"
      pattern: "registrarTransicao"
    - from: "app/services/webhook-asaas.service.ts"
      to: "lib/billing/asaas/client.ts"
      via: "asaasClient.buscarPagamento antes de conceder acesso"
      pattern: "buscarPagamento"
    - from: "app/services/webhook-asaas.service.ts"
      to: "lib/billing/asaas/datas.ts"
      via: "acessoAteAposPagamento(payment.dueDate)"
      pattern: "acessoAteAposPagamento"
---

<objective>
Fechar GTW-03 e a metade "fora de ordem" de GTW-04: preencher os quatro ramos mutadores que o plano 03-05 deixou deliberadamente na fila de retrabalho — resolucao de tenant pelo mapa local, captura dos identificadores do Asaas, e a escrita **monotonica** de `acessoAte` derivada do vencimento da cobranca, com auditoria pela causa `WEBHOOK_PAGAMENTO`.

Purpose: e aqui que dinheiro vira acesso. Tres invariantes precisam ser estruturais, nao defensivas: (a) a empresa afetada vem do mapa local, porque aceitar um `sub_...` do payload como autorizacao e IDOR; (b) `acessoAte` e funcao pura do `dueDate` da cobranca, porque derivar de `Date.now()` faz cada reentrega avancar o acesso e desalinhar permanentemente o nosso calendario do calendario de cobranca; (c) a guarda de monotonicidade mora no `WHERE` do `updateMany`, porque um `if` em memoria perde a corrida com o worker da Fase 5.
Output: `webhook-asaas.service.ts` completo — nenhum evento fica sem handler.

**Este plano modifica um arquivo criado no plano 03-05** e por isso roda na wave seguinte. Antes de comecar, confirmar que nao restou nenhum `TODO(03-06)` sem endereco.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md
@.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-PATTERNS.md
@.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-05-SUMMARY.md
@app/services/acesso.service.ts
@app/services/acesso.service.test.ts
@lib/avaliar-acesso.ts
</context>

## Artifacts this phase produces

Simbolos adicionados por ESTE plano a `app/services/webhook-asaas.service.ts`:

| Simbolo | Assinatura / forma |
|---------|--------------------|
| `webhookAsaasService.resolverEmpresaId(dados)` | `({ subscription?, checkoutId?, customer?, externalReference? }) => Promise<string \| null>` |
| `webhookAsaasService.aplicarPagamentoConfirmado(eventoId, payment)` | `(string, AsaasPayment) => Promise<void>` |
| `webhookAsaasService.capturarAssinatura(eventoId, subscription)` | `(string, AsaasSubscription) => Promise<void>` |
| `webhookAsaasService.capturarCheckoutPago(eventoId, checkout)` | `(string, AsaasCheckout & { customer?: string }) => Promise<void>` |

Consumido: `Empresa.asaasSubscriptionId`/`asaasCustomerId`, `CheckoutAsaas`, `CausaTransicaoAcesso.WEBHOOK_PAGAMENTO` (03-01); `asaasClient.buscarPagamento`/`buscarAssinatura`, `acessoAteAposPagamento` (03-02); `pagamentoSchema`/`checkoutSchema`/`assinaturaSchema` (03-03); `marcarProcessado`/`marcarErro`/`processar` (03-05); `acessoService.registrarTransicao` e `avaliarAcesso` (Fase 2).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolverEmpresaId e captura dos identificadores do Asaas</name>
  <files>app/services/webhook-asaas.service.ts, app/services/webhook-asaas.service.test.ts</files>
  <read_first>
    - `app/services/webhook-asaas.service.ts` (criado em 03-05, integral) — o `switch` de despacho e os metodos `marcarProcessado`/`marcarErro` a estender
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md` § Pattern 5 (ordem exata de resolucao), § Pitfall 8 (o `checkout.subscription` do payload NAO tem o id `sub_...`), § Open Questions 1 e 2
    - `tests/helpers/asaas.ts` (03-03) — `envelopeCheckout()` foi construido de proposito **sem** `checkout.subscription.id`, reproduzindo o Pitfall 8
    - `lib/billing/asaas/eventos.ts` (03-03) — `checkoutSchema`, `assinaturaSchema`, `pagamentoSchema`
    - `lib/billing/asaas/client.ts` (03-02) — `asaasClient.buscarAssinatura(id)`
    - `prisma/schema.prisma` — `Empresa.asaasSubscriptionId @unique`, `Empresa.asaasCustomerId`, `CheckoutAsaas.asaasCheckoutId @unique`
  </read_first>
  <behavior>
    - `resolverEmpresaId({ subscription: "sub_1" })` com uma Empresa cujo `asaasSubscriptionId === "sub_1"` devolve o id dela.
    - `resolverEmpresaId({ checkoutId: "chk_1" })` com um `CheckoutAsaas` correspondente devolve o `empresaId` dele.
    - Ordem de precedencia: com `subscription` **e** `checkoutId` presentes, a consulta por `asaasSubscriptionId` acontece primeiro; o `CheckoutAsaas` so e consultado se a primeira nao resolver.
    - `externalReference` sozinho **nunca** resolve: `resolverEmpresaId({ externalReference: "empresa-9" })` devolve `null`, e nenhuma consulta usou `externalReference` como `where` de autorizacao.
    - `externalReference` divergente do resultado do mapa local produz `console.error` com prefixo `[webhook-asaas]` e o mapa local prevalece (cross-check, nunca fonte).
    - Nada resolve → devolve `null`; o chamador marca `erro` e **nao** cria empresa nem adivinha.
    - `capturarAssinatura` grava `Empresa.asaasSubscriptionId` e `asaasCustomerId` para a empresa resolvida e chama `marcarProcessado` com esse `empresaId`.
    - `capturarAssinatura` com empresa nao resolvida chama `marcarErro` e **nao** escreve em nenhuma `Empresa`.
    - `capturarCheckoutPago` atualiza `CheckoutAsaas.status` para o status recebido e grava `Empresa.asaasCustomerId` quando `checkout.customer` esta presente; **nunca** le `checkout.subscription.id` (Pitfall 8).
    - `capturarAssinatura` e idempotente: rodar duas vezes com o mesmo payload nao produz estado diferente.
  </behavior>
  <action>
Estender `app/services/webhook-asaas.service.ts`.

`resolverEmpresaId(dados: { subscription?: string | null; checkoutId?: string | null; customer?: string | null; externalReference?: string | null }): Promise<string | null>` — implementa a ordem do § Pattern 5, exatamente:
1. `dados.subscription` presente → `prisma.empresa.findFirst({ where: { asaasSubscriptionId: dados.subscription, deletedAt: null }, select: { id: true } })`.
2. Nao resolveu e `dados.checkoutId` presente → `prisma.checkoutAsaas.findUnique({ where: { asaasCheckoutId: dados.checkoutId }, select: { empresaId: true } })`.
3. Nao resolveu e `dados.customer` presente → `prisma.empresa.findFirst({ where: { asaasCustomerId: dados.customer, deletedAt: null }, select: { id: true } })`.
4. `dados.externalReference` e usado **apenas como cross-check**: se um id foi resolvido e diverge do `externalReference`, emitir `console.error` com prefixo `[webhook-asaas]` citando os dois valores. Se **nada** foi resolvido, `externalReference` **nao** pode ser usado para resolver — devolver `null`.
5. Nada resolveu → `null`.

Comentario obrigatorio e longo acima do metodo, no calibre de `acesso.service.ts`, explicando por que a ordem importa: o payload de webhook e entrada nao confiavel e **nenhum identificador vindo dele autoriza nada**; a autoridade e sempre uma coluna que **nos** gravamos (`asaasSubscriptionId`, `CheckoutAsaas.asaasCheckoutId`). Registrar que aceitar `externalReference` como fonte unica seria IDOR direto: qualquer um que forje um webhook escolheria a empresa alvo (C-08, ASVS V4). Registrar tambem a suposicao A3 do Assumptions Log (nao esta confirmado que `externalReference` propaga do checkout para as cobrancas) — motivo adicional para ele ser so cross-check.

`capturarAssinatura(eventoId: string, subscription: AsaasSubscription): Promise<void>` — ramo de `SUBSCRIPTION_CREATED`:
- Resolver a empresa por `resolverEmpresaId({ customer: subscription.customer, externalReference: subscription.externalReference })`. Se `null`, tentar o fallback documentado na Open Question 2: nenhuma resolucao → `marcarErro(eventoId, "empresa nao resolvida para SUBSCRIPTION_CREATED")` e retornar (o evento fica na fila da Fase 5).
- Resolvida → `prisma.empresa.update({ where: { id: empresaId }, data: { asaasSubscriptionId: subscription.id, asaasCustomerId: subscription.customer } })` e `marcarProcessado(eventoId, empresaId)`.
- Comentario citando Pitfall 8: este e o **unico** evento que entrega o id `sub_...`; sem capturar aqui, nenhuma cobranca futura da assinatura seria reconhecida.
- Esta escrita **nao** passa por `empresaService.update` — o allowlist da Fase 2 (D-02-02) proibe campos de billing por aquele caminho, e a decisao registrada na Fase 2 e que escritas legitimas de webhook exigem metodo dedicado. Registrar isso em comentario.

`capturarCheckoutPago(eventoId: string, checkout): Promise<void>` — ramo de `CHECKOUT_PAID`:
- Resolver por `resolverEmpresaId({ checkoutId: checkout.id, customer: checkout.customer, externalReference: checkout.externalReference })`.
- `null` → `marcarErro` e retornar.
- Resolvida → `prisma.checkoutAsaas.update({ where: { asaasCheckoutId: checkout.id }, data: { status: checkout.status } })`; se `checkout.customer` presente, `prisma.empresa.update({ where: { id: empresaId }, data: { asaasCustomerId: checkout.customer } })`; `marcarProcessado(eventoId, empresaId)`.
- **Proibido** ler `checkout.subscription.id` — comentario citando Pitfall 8: esse objeto traz apenas a configuracao da recorrencia (`cycle`, `nextDueDate`, `endDate`), nunca o id da assinatura criada. Este ramo nao estende `acessoAte`: quem paga o acesso e o `PAYMENT_CONFIRMED` correspondente.

Ligar os dois ramos no `switch` de `processar`, substituindo os `marcarErro` provisorios de `SUBSCRIPTION_CREATED` e `CHECKOUT_PAID`. Validar o sub-objeto do payload com `assinaturaSchema.safeParse` / `checkoutSchema.safeParse` antes de usar; `success: false` → `marcarErro` + retorno, nunca excecao.

Estender `app/services/webhook-asaas.service.test.ts` cobrindo todo o bloco `<behavior>`. O teste de IDOR (`externalReference` sozinho nao resolve) e obrigatorio e deve citar C-08 no nome.
  </action>
  <acceptance_criteria>
    - `npx vitest run app/services/webhook-asaas.service.test.ts` passa com >= 23 casos (os >= 14 do plano 03-05 mais >= 9 novos).
    - Existe um caso de teste cujo nome cita `C-08` provando que `resolverEmpresaId({ externalReference })` devolve `null`.
    - Nenhuma consulta usa `externalReference` como filtro: `grep -cE "where:.*externalReference|externalReference:.*where" app/services/webhook-asaas.service.ts` == 0.
    - `grep -cE "checkout\.subscription\.id|subscription\?\.id.*checkout" app/services/webhook-asaas.service.ts` == 0 (Pitfall 8).
    - `grep -c "empresaService" app/services/webhook-asaas.service.ts` == 0 (escrita de billing nao passa pelo allowlist da Fase 2).
    - Existe um teste provando que `capturarAssinatura` com empresa nao resolvida chama `marcarErro` e que `prismaMock.empresa.update` **nao** foi chamado.
    - `npx tsc --noEmit` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run app/services/webhook-asaas.service.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>`resolverEmpresaId` resolve exclusivamente pelo mapa local com `externalReference` apenas como cross-check, e os eventos `SUBSCRIPTION_CREATED`/`CHECKOUT_PAID` capturam os identificadores do Asaas sem nunca ler `checkout.subscription.id`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: aplicarPagamentoConfirmado — re-fetch autoritativo, escrita monotonica e auditoria (GTW-03)</name>
  <files>app/services/webhook-asaas.service.ts, app/services/webhook-asaas.service.test.ts</files>
  <read_first>
    - `app/services/acesso.service.ts` linhas 36-61 — o compare-and-swap a **chamar**, nao copiar: `registrarTransicao({ empresaId, anterior, novo, causa })`, com a guarda `anterior === novo` retornando antes de abrir transacao (D-16) e o `updateMany` condicional sobre `ultimoStatusAuditado`
    - `lib/avaliar-acesso.ts` — `avaliarAcesso(fatos: FatosDeAcesso, agora: Date): ResultadoAcesso`; a comparacao e `agora < acessoAte` (limite superior exclusivo)
    - `lib/billing/asaas/datas.ts` (03-02) — `acessoAteAposPagamento(dueDate: string): Date`
    - `lib/billing/asaas/client.ts` (03-02) — `asaasClient.buscarPagamento(id): Promise<AsaasPayment>` e `AsaasApiError`
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md` § Pattern 3, § Code Examples #4, § Pitfall 1 (re-fetch como controle compensatorio), § Pitfall 5, § Referencia: eventos (por que `PAYMENT_CONFIRMED` e o gatilho e nao `PAYMENT_RECEIVED`)
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-PATTERNS.md` § `app/services/webhook-asaas.service.ts` — o `updateMany` monotonico e "o mesmo idioma" do CAS: guarda no `WHERE`, decisao via `count === 0`, no-op silencioso
  </read_first>
  <behavior>
    - `PAYMENT_CONFIRMED` chama `asaasClient.buscarPagamento(payment.id)` **antes** de qualquer escrita em `Empresa`.
    - O `dueDate` usado para calcular `acessoAte` vem da resposta do **re-fetch**, nao do payload do webhook.
    - Se o re-fetch devolver `status` fora de `["CONFIRMED", "RECEIVED", "RECEIVED_IN_CASH"]`, nenhuma escrita acontece e o evento e marcado com `erro`.
    - Se o re-fetch lancar `AsaasApiError`, nenhuma escrita acontece, `marcarErro` e chamado e a excecao nao propaga.
    - `prisma.empresa.updateMany` e chamado com `where` contendo `id: empresaId` e `OR: [{ acessoAte: null }, { acessoAte: { lt: novoAcessoAte } }]`, e `data: { acessoAte: novoAcessoAte }` — assercao **exata**.
    - `count === 0` (evento antigo fora de ordem) → `acessoService.registrarTransicao` **nao** e chamado e `marcarProcessado` **e** chamado (o evento foi tratado, apenas nao mudou nada).
    - `count === 1` → `acessoService.registrarTransicao` chamado com `causa: CausaTransicaoAcesso.WEBHOOK_PAGAMENTO`, `anterior: <ultimoStatusAuditado lido antes>` e `novo: <status derivado por avaliarAcesso sobre os fatos JA atualizados>`.
    - Nenhum literal de status e escrito: o `novo` vem de `avaliarAcesso`, nunca de um `if (pago) "EM_DIA"`.
    - `PAYMENT_RECEIVED` do mesmo `dueDate` apos um `PAYMENT_CONFIRMED` ja aplicado produz `count === 0` — no-op, sem segunda auditoria.
    - Empresa nao resolvida → `marcarErro`, nenhuma escrita, nenhuma empresa criada.
    - Determinismo: dois eventos distintos com o mesmo `dueDate` produzem exatamente o mesmo `novoAcessoAte`.
    - Prova de encaixe com a Fase 2: apos a escrita, `avaliarAcesso` (real) sobre os fatos resultantes devolve `status: "EM_DIA"` e `carenciaAte: null` — a carencia em curso e encerrada como efeito da derivacao, sem nenhuma coluna de carencia (GTW-03).
  </behavior>
  <action>
Adicionar `aplicarPagamentoConfirmado(eventoId: string, payment: AsaasPayment): Promise<void>` a `WebhookAsaasService` e liga-lo aos ramos `PAYMENT_CONFIRMED` e `PAYMENT_RECEIVED` do `switch`, substituindo os `marcarErro` provisorios do plano 03-05.

Sequencia obrigatoria:
1. **Re-fetch autoritativo:** `const autoritativo = await asaasClient.buscarPagamento(payment.id);` dentro de try/catch. Comentario obrigatorio explicando que este passo e **controle de seguranca, nao redundancia** (Pitfall 1): o Asaas nao assina o corpo, e um bearer estatico e replayavel por quem o capturar — um payload forjado nao sobrevive ao re-fetch. `AsaasApiError` → `marcarErro` + retorno.
2. **Guarda de status:** aceitar apenas `CONFIRMED`, `RECEIVED` e `RECEIVED_IN_CASH` em `autoritativo.status`. Qualquer outro → `marcarErro` + retorno. Comentario registrando que `PAYMENT_CONFIRMED` (e nao `PAYMENT_RECEIVED`) e o gatilho de acesso: no cartao de credito o fluxo e `CREATED → CONFIRMED → (~30 dias) → RECEIVED`, e esperar por `RECEIVED` deixaria o cliente pagante bloqueado por um mes.
3. **Resolver a empresa** por `resolverEmpresaId({ subscription: autoritativo.subscription, customer: autoritativo.customer, externalReference: autoritativo.externalReference })`. `null` → `marcarErro` + retorno.
4. **Ler os fatos atuais** com `select` explicito de `{ acessoAte, trialFim, canceladoEm, acessoVitalicio, ultimoStatusAuditado }` (C-07). Ausente → `marcarErro` + retorno.
5. **Calcular** `const novoAcessoAte = acessoAteAposPagamento(autoritativo.dueDate);`. **Proibido** derivar de `new Date()`/`Date.now()` — comentario citando Pitfall 5: a pureza em relacao ao evento e o que faz a reentrega produzir exatamente o mesmo valor, tornando a escrita monotonica um no-op em vez de "avancar o acesso a cada entrega".
6. **Escrita monotonica** — `prisma.empresa.updateMany({ where: { id: empresaId, OR: [{ acessoAte: null }, { acessoAte: { lt: novoAcessoAte } }] }, data: { acessoAte: novoAcessoAte } })`. Comentario obrigatorio explicando por que a guarda mora no `WHERE` e nao em `if`: um `PAYMENT_CONFIRMED` de janeiro reentregue em marco produz um `novoAcessoAte` de fevereiro, que e menor que o valor corrente → `count === 0` → nenhuma escrita e nenhuma auditoria, **sem** `if`, sem coluna de versao e sem lock; um `if` em memoria perderia a corrida com o worker da Fase 5 (GTW-04).
7. `count === 0` → `marcarProcessado(eventoId, empresaId)` e retornar. O evento foi tratado corretamente; simplesmente nao havia o que mudar.
8. `count === 1` → derivar `const novo = avaliarAcesso({ ...fatos, acessoAte: novoAcessoAte }, new Date()).status;` e chamar `acessoService.registrarTransicao({ empresaId, anterior: fatos.ultimoStatusAuditado, novo, causa: CausaTransicaoAcesso.WEBHOOK_PAGAMENTO })`. **Nunca** escrever um literal de `StatusAcesso`: status e derivado dos fatos, jamais armazenado (BILL-01/D-16). **Nunca** reimplementar o compare-and-swap — `registrarTransicao` ja o tem, e duplicar produz linhas duplicadas sob concorrencia (T-02-15).
9. `marcarProcessado(eventoId, empresaId)`.

Todo o corpo dentro do try/catch geral de `processar` (03-05) — nenhuma excecao pode propagar para fora de `agendarPosResposta`.

Validar `payment` com `pagamentoSchema.safeParse` antes do passo 1; `success: false` → `marcarErro` + retorno.

Estender o teste cobrindo todo o bloco `<behavior>`. O teste de encaixe com a Fase 2 importa `avaliarAcesso` **real**. As assercoes sobre `updateMany` e `registrarTransicao` devem ser **exatas** (`toHaveBeenCalledWith`), no precedente de `acesso.service.test.ts` linhas 74-83, com comentario explicando que a exatidao e o que prova a invariante.
  </action>
  <acceptance_criteria>
    - `npx vitest run app/services/webhook-asaas.service.test.ts` passa com >= 34 casos.
    - Existe um teste com assercao exata de `prismaMock.empresa.updateMany` incluindo `OR: [{ acessoAte: null }, { acessoAte: { lt: <Date> } }]`.
    - Existe um teste nomeando `GTW-04` que, com `updateMany` devolvendo `{ count: 0 }`, prova `expect(acessoServiceMock.registrarTransicao).not.toHaveBeenCalled()` e `marcarProcessado` chamado.
    - Existe um teste que prova que `asaasClient.buscarPagamento` foi invocado **antes** de `prismaMock.empresa.updateMany` (ordem de invocacao), e outro que prova que uma rejeicao do re-fetch resulta em zero chamadas a `updateMany`.
    - `grep -cE "new Date\(\)|Date\.now\(\)" app/services/webhook-asaas.service.ts` — toda ocorrencia esta em `marcarProcessado` ou no argumento `agora` de `avaliarAcesso`; **nenhuma** dentro do calculo de `novoAcessoAte`, verificavel por um teste que congela o relogio com `vi.setSystemTime` em dois instantes diferentes e assere o mesmo `novoAcessoAte`.
    - `grep -vE '^\s*(//|\*)' app/services/webhook-asaas.service.ts | grep -cE "\"EM_DIA\"|'EM_DIA'|StatusAcesso\.EM_DIA"` == 0 (status nunca e escrito como literal).
    - `grep -c "CausaTransicaoAcesso.WEBHOOK_PAGAMENTO" app/services/webhook-asaas.service.ts` >= 1.
    - `grep -cE "auditoriaAcesso\.create" app/services/webhook-asaas.service.ts` == 0 (a auditoria passa por `registrarTransicao`, nunca direto).
    - `npx tsc --noEmit` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run app/services/webhook-asaas.service.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Pagamento confirmado reconfere a cobranca na API do Asaas, estende `acessoAte` de forma monotonica e determinista a partir de `dueDate`, e registra a transicao por `acessoService.registrarTransicao` com causa `WEBHOOK_PAGAMENTO` — sem nunca escrever um status literal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: matriz de idempotencia e fora-de-ordem (GTW-04) + fechamento do despacho</name>
  <files>app/services/webhook-asaas.service.ts, app/services/webhook-asaas.service.test.ts</files>
  <read_first>
    - `app/services/webhook-asaas.service.ts` (estado apos as Tasks 1 e 2) — o `switch` completo
    - `app/services/acesso.service.test.ts` linhas 132-156 — `it.each` para varrer valores de enum, o idioma a aplicar na matriz de eventos
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-VALIDATION.md` § Per-Task Verification Map — as linhas `03-04-01`, `03-04-02`, `03-04-03` desta fase
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md` § Pattern 3 e § Pattern 4 (as duas metades de GTW-04)
  </read_first>
  <behavior>
    - Matriz `it.each` sobre os 12 eventos de `EVENTOS_ASSINADOS` mais um evento desconhecido: **nenhum** deles faz `processar` rejeitar, e **todos** terminam com `processadoEm` preenchido ou `erro` preenchido — nunca ambos vazios.
    - Nenhum evento restante cai em `marcarErro("handler de mutacao pendente")`: aquele marcador nao existe mais no codigo.
    - Reentrega: `processar` sobre um registro cujo `processadoEm` ja esta preenchido retorna sem nenhuma escrita e sem chamar `asaasClient`.
    - Fora de ordem, cenario completo: aplicar `PAYMENT_CONFIRMED` de `dueDate: "2026-10-01"` (acesso ate 01/11) e depois um `PAYMENT_CONFIRMED` reentregue de `dueDate: "2026-08-01"` (acesso ate 01/09) → `updateMany` devolve `count: 0` → `acessoAte` permanece o de novembro, nenhuma auditoria nova, e o evento antigo e marcado como processado.
    - Uma empresa em dia nunca e bloqueada por reentrega: apos o cenario acima, `avaliarAcesso` (real) sobre os fatos resultantes devolve `EM_DIA`.
    - `PAYMENT_OVERDUE` chegando **depois** de um `PAYMENT_CONFIRMED` do mesmo mes nao altera nenhum fato de billing (a combinacao mais perigosa do Pitfall 4).
    - Idempotencia de valor: `PAYMENT_CONFIRMED` e `PAYMENT_RECEIVED` do mesmo `dueDate` produzem o mesmo `novoAcessoAte`; o segundo vira `count: 0`.
  </behavior>
  <action>
Fechar o despacho e provar GTW-04 de ponta a ponta no nivel de servico.

1. Revisar o `switch` de `processar` e remover qualquer resquicio de `TODO(03-06)` e do marcador `"handler de mutacao pendente"` deixado pelo plano 03-05. Confirmar que os 12 eventos de `EVENTOS_ASSINADOS` tem ramo explicito e que o `default` cobre o desconhecido.
2. Garantir, por leitura do codigo, a invariante de terminacao: todo caminho de `processar` termina em `marcarProcessado` **ou** em `marcarErro`. Se algum ramo puder sair sem chamar nenhum dos dois, corrigir — um evento sem `processadoEm` e sem `erro` seria invisivel tanto para a fila de retrabalho quanto para o operador.
3. Adicionar a matriz `it.each` do bloco `<behavior>` cobrindo os 13 casos (12 assinados + 1 desconhecido).
4. Adicionar o cenario completo de fora-de-ordem descrito em `<behavior>`, com as datas literais `2026-10-01` e `2026-08-01`, e a assercao final por `avaliarAcesso` real.
5. Adicionar o cenario `PAYMENT_OVERDUE` apos `PAYMENT_CONFIRMED` do mesmo mes, com as tres assercoes de ausencia (`empresa.update`, `empresa.updateMany`, `$transaction`) no ramo do `OVERDUE`.
6. Adicionar o cenario `PAYMENT_CONFIRMED` seguido de `PAYMENT_RECEIVED` do mesmo `dueDate`, provando o mesmo `novoAcessoAte` e o `count: 0` no segundo.
7. Atualizar `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-VALIDATION.md`: substituir os IDs placeholder pelos IDs reais de task desta fase, marcar as linhas cobertas como verdes, e setar `nyquist_compliant: true` **apenas se** toda linha do mapa tiver um comando automatizado verde ou estiver explicitamente listada como manual (a linha `03-H1`, que e o checkpoint de sandbox do plano 03-07).
  </action>
  <acceptance_criteria>
    - `npx vitest run app/services/webhook-asaas.service.test.ts` passa com >= 42 casos.
    - `grep -cE "handler de mutacao pendente|TODO\(03-06\)" app/services/webhook-asaas.service.ts` == 0.
    - Existe um `it.each` com >= 13 entradas provando que `processar` nunca rejeita e sempre termina com `processadoEm` ou `erro`.
    - Existe um teste nomeando `GTW-04` com as datas literais `2026-10-01` e `2026-08-01` cuja assercao final e sobre `acessoAte` permanecer o de novembro.
    - Existe um teste provando que `PAYMENT_OVERDUE` apos `PAYMENT_CONFIRMED` nao chamou `empresa.update`, `empresa.updateMany` nem `$transaction`.
    - `03-VALIDATION.md` tem `nyquist_compliant: true` e nenhuma linha do mapa com status `⬜ pending` fora da linha manual `03-H1`.
    - `npm run test` e `npm run lint` verdes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run app/services/webhook-asaas.service.test.ts &amp;&amp; npm run test &amp;&amp; npm run lint</automated>
  </verify>
  <done>Todos os 12 eventos assinados mais o desconhecido tem ramo explicito e terminacao garantida; reentrega e evento fora de ordem sao provados no-op; uma empresa em dia nao pode ser bloqueada por reentrega; o mapa de validacao esta sincronizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload de webhook → identidade da empresa | Identificador sugerido pelo terceiro nunca autoriza |
| Payload de webhook → concessao de acesso pago | Um bearer estatico e replayavel; o payload sozinho nao pode conceder acesso |
| Evento fora de ordem → estado de billing | Entrega tardia nao pode reverter um estado mais recente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-31 | Elevation of Privilege | `resolverEmpresaId` | mitigate | Resolucao exclusiva por colunas gravadas por nos (`asaasSubscriptionId`, `CheckoutAsaas.asaasCheckoutId`, `asaasCustomerId`); `externalReference` so como cross-check. Gate de grep proibindo `externalReference` em clausula `where` (ASVS V4, C-08) |
| T-03-32 | Spoofing | webhook forjado concede acesso gratis | mitigate | `asaasClient.buscarPagamento(payment.id)` antes de qualquer escrita, com guarda de `status` — um payload forjado nao sobrevive ao re-fetch. Provado por teste de ordem de invocacao (compensa a ausencia de HMAC, T-03-23) |
| T-03-33 | Tampering | reentrega avanca o acesso | mitigate | `acessoAte` e funcao pura de `dueDate` (Pitfall 5) + guarda monotonica no `WHERE` do `updateMany`. Teste com relogio congelado em dois instantes prova o determinismo |
| T-03-34 | Tampering | evento antigo reverte estado novo | mitigate | `OR: [{ acessoAte: null }, { acessoAte: { lt: novoAcessoAte } }]` faz o evento antigo virar `count === 0`, sem `if`, sem coluna de versao, sem lock. Cenario completo com datas literais em teste (GTW-04) |
| T-03-35 | Tampering | mass assignment de billing | mitigate | A escrita usa `prisma.empresa.update`/`updateMany` dedicados; **nao** passa por `empresaService.update`, cujo allowlist da Fase 2 proibe campos de billing por design. Gate de grep com 0 ocorrencias de `empresaService` |
| T-03-36 | Repudiation | transicao sem rastro | mitigate | `acessoService.registrarTransicao` com `causa: WEBHOOK_PAGAMENTO` e compare-and-swap da Fase 2 reusado; gate de grep proibindo `auditoriaAcesso.create` direto (evitaria linhas duplicadas sob concorrencia, T-02-15) |
| T-03-37 | Denial of Service | falha do gateway derruba o processamento | mitigate | `AsaasApiError` capturado, gravado em `erro`, nunca propagado — `processar` roda depois do 200 e uma rejeicao nao teria para onde ir. Invariante de terminacao (`processadoEm` ou `erro`, nunca ambos vazios) verificada na Task 3 |
| T-03-38 | Elevation of Privilege | status escrito em vez de derivado | mitigate | `avaliarAcesso` e a unica origem do `novo`; gate de grep proibindo literais `EM_DIA` no arquivo. Preserva a invariante BILL-01/D-16 da Fase 2 |
| T-03-SC | Tampering | npm installs | mitigate | Este plano nao instala pacote nenhum. O gate de `zod` esta no plano 03-03 |
</threat_model>

<verification>
- `npx vitest run app/services/webhook-asaas.service.test.ts` verde com >= 42 casos.
- `npm run test` e `npm run lint` verdes.
- `npx tsc --noEmit` sai 0.
- Gates de grep: zero `externalReference` em `where`, zero `checkout.subscription.id`, zero `empresaService`, zero `auditoriaAcesso.create`, zero literal `EM_DIA`, zero `TODO(03-06)`.
- `03-VALIDATION.md` com `nyquist_compliant: true`.
</verification>

<success_criteria>
- Pagamento confirmado estende `acessoAte` em um ciclo mensal derivado de `dueDate` e encerra a carencia por derivacao, sem nenhuma coluna de carencia.
- A empresa afetada e sempre resolvida pelo mapa local; `externalReference` sozinho nunca resolve.
- Evento antigo reentregue produz `count === 0`: sem escrita, sem auditoria, empresa em dia permanece em dia.
- A mesma confirmacao entregue duas vezes produz o mesmo `acessoAte`.
- Toda transicao aplicada gera auditoria com causa `WEBHOOK_PAGAMENTO` via o CAS da Fase 2.
- Nenhum dos 12 eventos assinados fica sem handler; todo caminho termina com `processadoEm` ou `erro`.
</success_criteria>

<output>
Create `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-06-SUMMARY.md` when done
</output>
