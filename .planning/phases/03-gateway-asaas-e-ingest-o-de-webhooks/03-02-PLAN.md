---
phase: 03-gateway-asaas-e-ingest-o-de-webhooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/billing/asaas/config.ts
  - lib/billing/asaas/config.test.ts
  - lib/billing/asaas/datas.ts
  - lib/billing/asaas/datas.test.ts
  - lib/billing/asaas/tipos.ts
  - lib/billing/asaas/client.ts
  - lib/billing/asaas/client.test.ts
  - .env.example
autonomous: true
requirements: [GTW-01, GTW-03]

must_haves:
  truths:
    - "Uma variavel de ambiente ASAAS_* ausente falha com erro nomeado, nunca vira string vazia silenciosa"
    - "acessoAte derivado de um mesmo dueDate produz sempre o mesmo instante, independente de quando o evento chega"
    - "31/01 + 1 ciclo mensal resulta em 28/02 (ou 29/02 em ano bissexto), nunca 02/03 ou 03/03"
    - "Uma data-calendario 'YYYY-MM-DD' do Asaas vira o dia certo em Sao Paulo, sem perder um dia na virada"
    - "Uma chamada ao Asaas que falha vira HttpError 502 com mensagem generica; corpo de resposta nunca vai inteiro para o log"
  artifacts:
    - path: "lib/billing/asaas/config.ts"
      provides: "acessores lazy fail-fast das env vars ASAAS_* e APP_BASE_URL"
      exports: ["asaasApiUrl", "asaasApiKey", "asaasWebhookToken", "asaasCheckoutBaseUrl", "appBaseUrl", "VALOR_PLANO_MENSAL"]
    - path: "lib/billing/asaas/datas.ts"
      provides: "aritmetica pura de datas do Asaas"
      exports: ["comoDiaEmSaoPaulo", "acessoAteAposPagamento", "primeiraCobrancaEmSaoPaulo"]
    - path: "lib/billing/asaas/client.ts"
      provides: "cliente fetch tipado da API Asaas v3"
      exports: ["asaasClient", "AsaasApiError"]
    - path: "lib/billing/asaas/tipos.ts"
      provides: "tipos TS dos objetos Asaas consumidos"
      exports: ["AsaasCheckout", "AsaasPayment", "AsaasSubscription", "CriarCheckoutInput"]
  key_links:
    - from: "lib/billing/asaas/datas.ts"
      to: "lib/fuso-sao-paulo.ts"
      via: "import de meiaNoiteEmSaoPaulo (nunca re-derivar)"
      pattern: "from \"@/lib/fuso-sao-paulo\""
    - from: "lib/billing/asaas/client.ts"
      to: "lib/billing/asaas/config.ts"
      via: "leitura de baseUrl e access_token"
      pattern: "asaasApiKey\\(\\)"
---

<objective>
Construir a fundacao isolada e testavel de `lib/billing/asaas/`: leitura fail-fast das credenciais, aritmetica pura de datas do Asaas, tipos dos objetos da API v3 e o cliente HTTP tipado sobre `fetch`. Nenhum outro arquivo do projeto conhecera a URL, o header ou o formato de data do Asaas.

Purpose: as tres armadilhas mais silenciosas da fase moram aqui — segredo lido como `?? ""` (o projeto ja tem esse defeito duas vezes, com `JWT_SECRET` e R2), `new Date("2021-01-01")` perdendo um dia no fuso de Sao Paulo, e `setMonth` transformando 31/01 em 03/03. Todas as tres so falham na virada de dia/mes, que e exatamente onde GTW-03 importa.
Output: quatro modulos de `lib/billing/asaas/` com testes unitarios que provam o clamp de fim de mes, a ancoragem de data-calendario e o fail-fast de configuracao.

Sem `zod` e sem Prisma: este plano roda em paralelo com 03-01 e 03-03 e nao depende de nenhum dos dois.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md
@.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-PATTERNS.md
@lib/fuso-sao-paulo.ts
@lib/avaliar-acesso.ts
@lib/http-error.ts
@lib/storage/r2.ts
</context>

## Artifacts this phase produces

Simbolos criados por ESTE plano:

| Simbolo | Assinatura / forma | Arquivo |
|---------|--------------------|---------|
| `asaasApiUrl()` | `(): string` — throw se ausente | `lib/billing/asaas/config.ts` |
| `asaasApiKey()` | `(): string` — throw se ausente | `lib/billing/asaas/config.ts` |
| `asaasWebhookToken()` | `(): string` — throw se ausente | `lib/billing/asaas/config.ts` |
| `asaasCheckoutBaseUrl()` | `(): string` — throw se ausente | `lib/billing/asaas/config.ts` |
| `appBaseUrl()` | `(): string` — throw se ausente | `lib/billing/asaas/config.ts` |
| `VALOR_PLANO_MENSAL` | `const = 29.9` (D-02) | `lib/billing/asaas/config.ts` |
| `comoDiaEmSaoPaulo(dataAsaas: string)` | `(string) => Date` | `lib/billing/asaas/datas.ts` |
| `acessoAteAposPagamento(dueDate: string)` | `(string) => Date` | `lib/billing/asaas/datas.ts` |
| `primeiraCobrancaEmSaoPaulo(trialFim: Date \| null)` | `(Date\|null) => string` formato `"YYYY-MM-DD HH:mm:ss"` | `lib/billing/asaas/datas.ts` |
| `AsaasApiError` | `class extends Error { status: number; codigos: string[] }` | `lib/billing/asaas/client.ts` |
| `asaasClient.criarCheckout(input)` | `(CriarCheckoutInput) => Promise<AsaasCheckout>` | `lib/billing/asaas/client.ts` |
| `asaasClient.buscarPagamento(id)` | `(string) => Promise<AsaasPayment>` | `lib/billing/asaas/client.ts` |
| `asaasClient.buscarAssinatura(id)` | `(string) => Promise<AsaasSubscription>` | `lib/billing/asaas/client.ts` |
| `AsaasCheckout` / `AsaasPayment` / `AsaasSubscription` / `CriarCheckoutInput` | interfaces TS | `lib/billing/asaas/tipos.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.ts — acessores lazy fail-fast das credenciais Asaas</name>
  <files>lib/billing/asaas/config.ts, lib/billing/asaas/config.test.ts, .env.example</files>
  <read_first>
    - `lib/storage/r2.ts` (integral, 50 linhas) — copiar a forma de lazy-init (linhas 8-23) e a classe de erro dedicada (linha 6); **INVERTER** o anti-padrao `process.env.X ?? ""` das linhas 14-18
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-PATTERNS.md` § `lib/billing/asaas/config.ts (config)` — o anti-padrao literal a nao copiar
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md` § Code Examples #1 e § Pattern 1 (pontos nao-negociaveis)
    - `lib/avaliar-acesso.ts` linhas 1-25 — formato de constante de dominio exportada com docblock (`DIAS_DE_CARENCIA`, `DIAS_DE_TRIAL`)
  </read_first>
  <behavior>
    - `asaasApiKey()` com `ASAAS_API_KEY` definida devolve o valor exato.
    - `asaasApiKey()` com `ASAAS_API_KEY` ausente lanca `Error` cuja mensagem contem `[asaas]` e o nome literal `ASAAS_API_KEY`.
    - `asaasApiKey()` com `ASAAS_API_KEY=""` (string vazia) lanca — vazio e tratado como ausente.
    - Mesmo comportamento para `asaasApiUrl`, `asaasWebhookToken`, `asaasCheckoutBaseUrl`, `appBaseUrl`.
    - Importar o modulo com NENHUMA env var definida **nao** lanca (o throw so acontece na chamada do acessor).
    - `VALOR_PLANO_MENSAL === 29.9`.
  </behavior>
  <action>
Criar `lib/billing/asaas/config.ts`.

Funcao interna `obrigatorio(nome: string): string` que le `process.env[nome]`, e lanca `new Error(\`[asaas] variavel de ambiente ausente: ${nome}\`)` quando o valor e `undefined` ou string vazia. Nunca `?? ""`.

Exportar cinco acessores que chamam `obrigatorio`: `asaasApiUrl()` (`ASAAS_API_URL`), `asaasApiKey()` (`ASAAS_API_KEY`), `asaasWebhookToken()` (`ASAAS_WEBHOOK_TOKEN`), `asaasCheckoutBaseUrl()` (`ASAAS_CHECKOUT_BASE_URL`), `appBaseUrl()` (`APP_BASE_URL`).

**Decisao explicita (diverge de RESEARCH Code Example #1, que faz `throw` no topo do modulo):** os valores sao lidos por **acessor**, nao por `const` de nivel de modulo. Motivo, a documentar em comentario no arquivo: `app/api/webhooks/asaas/route.ts` e importado por `route.test.ts`, e um throw no import tornaria impossivel testar o caminho de 401 sem configurar o ambiente inteiro; `lib/storage/r2.ts` ja estabelece esse idioma de lazy-init no projeto. A semantica fail-fast e preservada — a diferenca e o momento (primeira leitura em vez de import), nao a rigidez. Nao ha valor default para nenhuma delas.

Exportar tambem `export const VALOR_PLANO_MENSAL = 29.9;` com docblock citando **D-02** (preco mensal do plano unico, R$29,90, substituindo a suposicao de R$49,90 usada na pesquisa de comparacao de taxas) no mesmo formato de `DIAS_DE_CARENCIA` em `lib/avaliar-acesso.ts`.

Criar `lib/billing/asaas/config.test.ts` com header `// @vitest-environment node`, salvando e restaurando `process.env` em `beforeEach`/`afterEach` (`vi.stubEnv`/`vi.unstubAllEnvs` ou snapshot manual do objeto), cobrindo os casos do bloco `<behavior>`.

Criar `.env.example` na raiz (o projeto ainda nao tem um) listando **todas** as variaveis que a aplicacao le, com comentario por bloco: as ja existentes (`DATABASE_URL`, `JWT_SECRET`, `R2_*`) e as novas (`ASAAS_API_URL=https://api-sandbox.asaas.com/v3` per **D-06**, `ASAAS_API_KEY`, `ASAAS_WEBHOOK_TOKEN`, `ASAAS_CHECKOUT_BASE_URL`, `APP_BASE_URL`). Valores de exemplo apenas — nenhum segredo real. Comentar em `ASAAS_WEBHOOK_TOKEN` que ele e gerado por nos com `node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"` (64 chars, dentro do limite 32-255 do Asaas) e registrado no Asaas, nao obtido do painel. Comentar em `ASAAS_CHECKOUT_BASE_URL` que o host de sandbox e uma suposicao rastreada (A1 do Assumptions Log) confirmada no plano 03-07.
  </action>
  <acceptance_criteria>
    - `npx vitest run lib/billing/asaas/config.test.ts` passa com >= 6 casos.
    - `grep -c '?? ""' lib/billing/asaas/config.ts` == 0.
    - `grep -v '^\s*//' lib/billing/asaas/config.ts | grep -c "process.env"` >= 1 e todas as leituras passam por `obrigatorio`.
    - `grep -v '^\s*#' .env.example | grep -c "ASAAS_"` == 4 e `grep -v '^\s*#' .env.example | grep -c "APP_BASE_URL"` == 1.
    - `.env.example` nao contem nenhum valor que pareca segredo real: `grep -cE '\$aact_|postgres://[^ ]*:[^ ]*@' .env.example` == 0.
    - Um teste prova que `import("@/lib/billing/asaas/config")` com env vazio **nao** lanca.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run lib/billing/asaas/config.test.ts</automated>
  </verify>
  <done>Cinco acessores fail-fast existem, nenhum default silencioso, o import e livre de efeito colateral, `VALOR_PLANO_MENSAL` = 29.9 per D-02, e `.env.example` documenta todas as env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: datas.ts — aritmetica pura e determinista das datas do Asaas</name>
  <files>lib/billing/asaas/datas.ts, lib/billing/asaas/datas.test.ts</files>
  <read_first>
    - `lib/fuso-sao-paulo.ts` (integral) — o aviso literal das linhas 1-5 ("Nao re-derive este algoritmo") e a assinatura `meiaNoiteEmSaoPaulo(instante: Date, deslocamentoDias = 0): Date` (linha 49), cuja semantica e **limite superior exclusivo** do dia local
    - `lib/fuso-sao-paulo.test.ts` — formato de teste a espelhar, incluindo os casos historicos de DST
    - `lib/avaliar-acesso.ts` — `FatosDeAcesso`, `ResultadoAcesso`, `avaliarAcesso(fatos, agora)`, `DIAS_DE_CARENCIA = 10`; a comparacao usada e `agora < acessoAte`
    - `app/services/empresa.service.ts` linhas 57-62 — precedente de comentario justificando o off-by-one de `meiaNoiteEmSaoPaulo` (D-18)
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md` § Pitfall 5, § Pitfall 6 e § Code Examples #5
  </read_first>
  <behavior>
    - `comoDiaEmSaoPaulo("2021-01-01")` produz um `Date` cujo dia-calendario em `America/Sao_Paulo` e 01/01/2021 (nao 31/12/2020).
    - `acessoAteAposPagamento("2021-01-01")` === `meiaNoiteEmSaoPaulo` do dia 01/02/2021 (limite superior exclusivo).
    - `acessoAteAposPagamento("2021-01-31")` cai em 28/02/2021 — clamp de fim de mes, nunca 02/03 ou 03/03.
    - `acessoAteAposPagamento("2020-01-31")` cai em 29/02/2020 (ano bissexto).
    - `acessoAteAposPagamento("2021-03-31")` cai em 30/04/2021.
    - `acessoAteAposPagamento("2021-12-15")` cai em 15/01/2022 — virada de ano.
    - `acessoAteAposPagamento("2021-10-16")` e `("2018-02-17")` produzem instantes corretos atravessando as transicoes historicas de horario de verao do Brasil.
    - Determinismo: chamar `acessoAteAposPagamento` com o mesmo argumento duas vezes, com `Date.now()` diferente entre as chamadas, devolve exatamente o mesmo instante (`getTime()` identico).
    - `primeiraCobrancaEmSaoPaulo(trialFim)` devolve string no formato `"YYYY-MM-DD HH:mm:ss"`.
    - `primeiraCobrancaEmSaoPaulo(null)` devolve o dia-calendario de hoje em Sao Paulo (fallback quando a empresa nao tem trial).
    - Alimentar `avaliarAcesso({ acessoAte: acessoAteAposPagamento("2021-01-01"), trialFim: null, canceladoEm: null, acessoVitalicio: false }, new Date("2021-01-20T12:00:00Z"))` devolve `status: "EM_DIA"` e `carenciaAte: null`.
  </behavior>
  <action>
Criar `lib/billing/asaas/datas.ts`. Importar `meiaNoiteEmSaoPaulo` de `@/lib/fuso-sao-paulo` — **nunca** re-implementar aritmetica de fuso (o arquivo tem aviso normativo explicito).

`comoDiaEmSaoPaulo(dataAsaas: string): Date` — recebe `"YYYY-MM-DD"` e devolve `new Date(\`${dataAsaas}T12:00:00Z\`)`. Docblock obrigatorio explicando, no calibre do comentario D-18 de `empresa.service.ts`: `new Date("2021-01-01")` e UTC midnight = 31/12 21:00 em Sao Paulo, e passar isso para `meiaNoiteEmSaoPaulo` devolveria a meia-noite do dia **anterior**, custando um dia inteiro de acesso ao cliente; a ancora de meio-dia UTC (09:00 em SP) mantem o dia-calendario correto e e imune a qualquer transicao de offset (Pitfall 6).

`acessoAteAposPagamento(dueDate: string): Date` — funcao **pura do evento**, jamais de `Date.now()`. Decompor `dueDate` em ano/mes/dia por `split("-").map(Number)`; calcular o ultimo dia do mes seguinte com `new Date(Date.UTC(ano, mes, 0)).getUTCDate()` (com `mes` ja 1-based, `Date.UTC(ano, mes, 0)` e o ultimo dia do mes seguinte); usar `Math.min(dia, ultimoDia)` como dia alvo; remontar a string `"YYYY-MM-DD"` do mes seguinte com padding de dois digitos e rollover de ano quando `mes === 12`; devolver `meiaNoiteEmSaoPaulo(comoDiaEmSaoPaulo(alvo))`. **Proibido** `Date.prototype.setMonth` (overflow silencioso: 31/01 vira 02/03 ou 03/03) e **proibido** somar 30 dias em milissegundos (o ciclo do Asaas e `MONTHLY`, e 30 dias desalinham em ~5 meses). Docblock citando GTW-03, Pitfall 5 e explicando que a pureza e o que torna a escrita monotonica idempotente sob reentrega.

`primeiraCobrancaEmSaoPaulo(trialFim: Date | null): string` — devolve o `nextDueDate` a enviar ao Asaas no formato do exemplo oficial `"YYYY-MM-DD HH:mm:ss"` (A4 do Assumptions Log: usar o formato do exemplo, nao `"YYYY-MM-DD"` sozinho). Deriva o dia-calendario de Sao Paulo de `trialFim` (per **D-03**: a primeira cobranca cai no fim do trial, nao no registro); quando `trialFim` e `null` ou ja passou, usa o dia-calendario de hoje em Sao Paulo. Formatar via `Intl.DateTimeFormat("en-CA", { timeZone: "America/Sao_Paulo", ... })` para obter as partes de data em SP — nao por `toISOString().slice(0,10)`, que devolve o dia UTC. Hora fixa `"00:00:00"`.

Criar `lib/billing/asaas/datas.test.ts` com `// @vitest-environment node`, cobrindo cada item de `<behavior>`. O teste de determinismo deve provar a independencia de `Date.now()` explicitamente (duas chamadas com `vi.setSystemTime` diferente entre elas). O teste de encaixe com `avaliarAcesso` importa a funcao real de `@/lib/avaliar-acesso` (nao mock) — e a prova de que a saida desta funcao satisfaz o motor da Fase 2.
  </action>
  <acceptance_criteria>
    - `npx vitest run lib/billing/asaas/datas.test.ts` passa com >= 11 casos.
    - `grep -c "setMonth" lib/billing/asaas/datas.ts` == 0.
    - `grep -c "Date.now()" lib/billing/asaas/datas.ts` == 0 dentro de `acessoAteAposPagamento` (a funcao e pura do argumento).
    - `grep -c "from \"@/lib/fuso-sao-paulo\"" lib/billing/asaas/datas.ts` == 1 e nenhuma aritmetica de offset manual: `grep -cE "3 \* 60 \* 60|10800000|getTimezoneOffset" lib/billing/asaas/datas.ts` == 0.
    - Existe um caso de teste nomeado citando `2021-01-31` cuja assercao e sobre 28/02.
    - Existe um caso de teste nomeado citando `2020-01-31` cuja assercao e sobre 29/02.
    - O teste de encaixe com `avaliarAcesso` importa a implementacao real (`grep -c "from \"@/lib/avaliar-acesso\"" lib/billing/asaas/datas.test.ts` == 1).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run lib/billing/asaas/datas.test.ts</automated>
  </verify>
  <done>`acessoAteAposPagamento` e determinista, faz clamp de fim de mes corretamente (incluindo bissexto e virada de ano), delega todo fuso a `meiaNoiteEmSaoPaulo`, e o resultado alimentado em `avaliarAcesso` devolve `EM_DIA`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tipos.ts + client.ts — cliente fetch tipado da API Asaas v3</name>
  <files>lib/billing/asaas/tipos.ts, lib/billing/asaas/client.ts, lib/billing/asaas/client.test.ts</files>
  <read_first>
    - `lib/storage/r2.ts` (integral) — unico cliente de servico externo do projeto; copiar a traducao de erro externo para erro de dominio (linhas 38-49) e a classe de erro dedicada (linha 6)
    - `lib/http-error.ts` (integral, 8 linhas) — `HttpError(message, status = 400)` com propriedade `status`
    - `app/services/empresa.service.ts` linhas 12-50 — formato do bloco de DTOs/interfaces no topo do arquivo
    - `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-RESEARCH.md` § Code Examples #1 e #2, § Pattern 1 (pontos nao-negociaveis), § Referencia: eventos e estados do Asaas (enum de `payment.status`, status de checkout e de assinatura)
  </read_first>
  <behavior>
    - Toda chamada envia o header `access_token` com o valor de `asaasApiKey()` — **nunca** `Authorization: Bearer`.
    - Toda chamada envia `AbortSignal.timeout` (15000 ms) — uma chamada pendurada dentro de um `after()` prenderia a invocacao.
    - Resposta 2xx: devolve o JSON tipado.
    - Resposta 4xx/5xx: lanca `AsaasApiError` com `status` da resposta e `codigos` extraidos de `errors[].code`; o `console.error` emitido contem o prefixo `[asaas]`, o metodo, o caminho e o status — e **no maximo 500 caracteres** do corpo, nunca o corpo inteiro.
    - Erro de rede / abort: lanca `AsaasApiError` com `status: 0` e nao vaza a exception original para o chamador.
    - `criarCheckout` faz `POST` em `/checkouts` e devolve `{ id, link, status }`.
    - `buscarPagamento("pay_x")` faz `GET` em `/payments/pay_x`.
    - `buscarAssinatura("sub_x")` faz `GET` em `/subscriptions/sub_x`.
    - Uma resposta 429 e logada com o prefixo `[asaas]` e a palavra `429` antes de virar `AsaasApiError`.
  </behavior>
  <action>
Criar `lib/billing/asaas/tipos.ts` com as interfaces dos objetos Asaas que o projeto consome — apenas os campos usados, nao o DTO inteiro do fornecedor:
- `AsaasCheckout { id: string; link: string | null; status: string }`
- `AsaasPayment { id: string; status: string; value: number; dueDate: string; subscription?: string | null; customer?: string | null; externalReference?: string | null; billingType?: string | null }`
- `AsaasSubscription { id: string; customer: string; status: string; cycle: string; nextDueDate: string; externalReference?: string | null }`
- `CriarCheckoutInput { empresaId: string; nomeItem: string; descricaoItem: string; valor: number; nextDueDate: string; successUrl: string; cancelUrl: string; expiredUrl: string }`
Nenhum campo `creditCard*`, `customerData`, `cpfCnpj`, `pixTransaction`, `bankSlipUrl`, `invoiceUrl` ou `transactionReceiptUrl` pode aparecer nestes tipos — se nao ha tipo, nao ha caminho acidental para persistencia (GTW-01, Pitfall 9).

Criar `lib/billing/asaas/client.ts`:
- `export class AsaasApiError extends Error { status: number; codigos: string[] }` — precedente: `UploadError` em `lib/storage/r2.ts` e `AuthError` em `lib/api-auth.ts`.
- Funcao privada `chamar<T>(caminho: string, init?: RequestInit): Promise<T>` que monta `${asaasApiUrl()}${caminho}`, injeta os headers `access_token: asaasApiKey()` e `"Content-Type": "application/json"`, e `signal: AbortSignal.timeout(15_000)`. Em `!resposta.ok`: ler o corpo com `.text().catch(() => "")`, tentar extrair `errors[].code` via `JSON.parse` dentro de try/catch, emitir `console.error` com prefixo `[asaas]` incluindo metodo, caminho, status e `detalhe.slice(0, 500)`, e lancar `AsaasApiError`. Envolver o `fetch` em try/catch para converter falha de rede/timeout em `AsaasApiError` com `status: 0`.
- Classe `AsaasClient` com os metodos `criarCheckout(input: CriarCheckoutInput): Promise<AsaasCheckout>`, `buscarPagamento(id: string): Promise<AsaasPayment>`, `buscarAssinatura(id: string): Promise<AsaasSubscription>`; exportar o singleton `export const asaasClient = new AsaasClient();` (C-02).
- `criarCheckout` monta o corpo com `billingTypes: ["CREDIT_CARD"]`, `chargeTypes: ["RECURRENT"]`, `minutesToExpire: 60`, `externalReference: input.empresaId`, `callback: { successUrl, cancelUrl, expiredUrl }`, `items: [{ name, description, quantity: 1, value }]` e `subscription: { cycle: "MONTHLY", nextDueDate: input.nextDueDate }`. **`endDate` e omitido de proposito** — assinatura aberta ate cancelamento (SUB-02, Fase 7); documentar isso em comentario com a referencia A5 do Assumptions Log. Cartao de credito e obrigatorio para `RECURRENT` (per **D-01/D-06**: tokenizacao client-side esta proibida nesta fase; so checkout hospedado).
- Nenhum metodo de tokenizacao de cartao, nenhum endpoint `/creditCard/tokenize`.

Criar `lib/billing/asaas/client.test.ts` com `// @vitest-environment node`, usando `vi.stubGlobal("fetch", vi.fn())` e `vi.stubEnv` para as `ASAAS_*`, cobrindo cada item de `<behavior>`. Espionar `console.error` para provar que o corpo logado e truncado.
  </action>
  <acceptance_criteria>
    - `npx vitest run lib/billing/asaas/client.test.ts` passa com >= 8 casos.
    - `grep -c "Authorization" lib/billing/asaas/client.ts` == 0.
    - `grep -v '^\s*//' lib/billing/asaas/client.ts | grep -c "access_token"` >= 1.
    - `grep -v '^\s*//' lib/billing/asaas/client.ts | grep -c "AbortSignal.timeout"` >= 1.
    - `grep -cE "creditCard|cpfCnpj|customerData|pixTransaction|bankSlipUrl|invoiceUrl|transactionReceiptUrl" lib/billing/asaas/tipos.ts` == 0.
    - `grep -v '^\s*//' lib/billing/asaas/client.ts | grep -c "chargeTypes"` == 1 e o valor e `["RECURRENT"]`.
    - Um teste prova que `console.error` recebeu string contendo `[asaas]` e que o argumento de corpo tem no maximo 500 caracteres.
    - `npx tsc --noEmit` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run lib/billing/asaas/client.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>`asaasClient` chama a API v3 com header `access_token` e timeout, traduz qualquer falha em `AsaasApiError`, nunca loga o corpo inteiro, e os tipos nao tem nenhum campo de dado de pagamento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nosso backend → API do Asaas | Saida autenticada por segredo estatico (`access_token`) sobre HTTPS |
| Ambiente (`process.env`) → codigo | Segredo ausente nao pode virar string vazia |
| Resposta do Asaas → nossos logs | Corpo de resposta pode conter dados do pagador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Elevation of Privilege | `lib/billing/asaas/config.ts` | mitigate | `obrigatorio()` lanca em `undefined` **e** string vazia; gate de grep proibindo `?? ""`. Corrige, para o terceiro segredo do projeto, o defeito ja documentado em `JWT_SECRET` e R2 |
| T-03-07 | Information Disclosure | `console.error` do cliente | mitigate | Log restrito a metodo, caminho, status e `detalhe.slice(0, 500)`; teste espiona `console.error` e assere o truncamento |
| T-03-08 | Information Disclosure | `lib/billing/asaas/tipos.ts` | mitigate | Tipos nao declaram nenhum campo de dado de pagamento/PII; gate de grep com 0 ocorrencias. Sem tipo nao ha caminho de persistencia acidental (GTW-01) |
| T-03-09 | Denial of Service | `chamar()` dentro de `after()` | mitigate | `AbortSignal.timeout(15_000)` obrigatorio em toda chamada — sem ele, uma chamada pendurada prende a invocacao serverless indefinidamente |
| T-03-10 | Tampering | `acessoAteAposPagamento` | mitigate | Funcao pura do `dueDate`, sem `Date.now()`: reentrega do mesmo evento produz o mesmo instante, o que e o que faz a escrita monotonica do plano 03-06 ser idempotente. Gate de grep proibindo `setMonth` |
| T-03-11 | Spoofing | tokenizacao de cartao | transfer | Nenhum metodo de tokenizacao no cliente. Captura de cartao fica 100% nas paginas do Asaas (D-06), mantendo o projeto fora do escopo PCI SAQ-A-EP |
| T-03-SC | Tampering | npm installs | mitigate | Este plano nao instala pacote nenhum — usa `fetch`, `node:crypto` e `Intl` nativos. Pacotes `asaas` e `asaas-sdk` do npm estao proibidos na fase (abandonados) |
</threat_model>

<verification>
- `npx vitest run lib/billing` verde.
- `npx tsc --noEmit` sai 0.
- `npm run lint` sem novos erros.
- Gates de grep dos `<acceptance_criteria>` de cada task.
</verification>

<success_criteria>
- Nenhuma env var do Asaas pode ser lida como string vazia.
- `acessoAteAposPagamento` e uma funcao pura, com clamp de fim de mes provado por teste em 31/01, 31/03 e 29/02 bissexto.
- O cliente usa header `access_token`, timeout explicito e nunca loga corpo completo.
- Nenhum tipo ou codigo desta fundacao referencia dado de cartao/PII.
- `.env.example` existe e documenta as cinco variaveis novas.
</success_criteria>

<output>
Create `.planning/phases/03-gateway-asaas-e-ingest-o-de-webhooks/03-02-SUMMARY.md` when done
</output>
